# SYSTEM STATE — SOURCE OF TRUTH

## CURRENT PHASE
Phase 5 — ✅ CLOSED 2026-05-07 (canonical migration `20260507140000_phase5_creatives.sql` deployed via `supabase db push`; PR #6 merged into main commit `f5aa8ff`; 3 canonical tables — brand_kits, creative_generations, creatives — + 2 organizations columns — plan_type, vault_byok_openrouter_secret_id — + 1 private storage bucket `creatives` live in production; `/creatives/*` + `/brand-kit/*` 503 gates lifted; existing backend services + routes + 5 frontend mocked-shell pages now reach live schema). Phase 0 + Phase 1 + Phase 2 + Phase 3 (linear) + Phase 4 minimal + Phase 4 Part 2 + Phase 5 + 13 backend hardening passes ALL CLOSED. · Next governed item per PATCH QUEUE: Phase X broader (MCP / tool governance / DB log-sink fan-out / strategy_tag enum) — REQUIRES EXPLICIT AUTHORIZATION. Phase 7 substrate columns added in Phase 5 are MINIMAL forward-positioning for FR-011 BYOK gate; full Phase 7 monetization (Stripe, billing UI, plan upgrade, credits ledger) remains DEFERRED-BY-GOVERNANCE.

---

## SYSTEM STATUS

* Frontend: SHELLED — Stitch UI shells completed across all routing-map pages; live API wiring is partial. Wired-and-now-live (Phase 2): `app/integrations/page.tsx`, `app/integrations/connect/page.tsx`, `app/dashboard/overview/page.tsx`, `app/dashboard/channels/page.tsx`, `app/api/integrations/callback/[platform]/route.ts`. Wired-but-deferred-target: `app/decisions/[id]/page.tsx` (waiting on Phase 3 anomaly-engine unlock). Mocked: 33 pages governance-bound to Phase 3 anomaly engine / Phase 4 Part 2 / Phase 5 / Phase 6 frontend / Phase 7 unlocks per Phases.md. Mocked-shell state is INTENTIONAL, not drift.
* Backend: OPERATIONAL — Phase 0 closed; Phase 1 closed (active surface); Phase 2 closed; Phase 3 closed; Phase 4 (minimal slice) closed; 4 real action handlers live
* Integrations: ✅ READY — OAuth + sync infrastructure live behind /api/v1/integrations and /api/v1/metrics; per-org credential storage via Supabase Vault is operational. Real platform credentials (META_APP_ID/SECRET, GOOGLE_ADS_CLIENT_ID/SECRET/DEVELOPER_TOKEN, SHOPIFY_API_KEY/SECRET, OAUTH_REDIRECT_BASE_URL) must be populated in env before user-facing OAuth flows succeed; missing values produce explicit runtime errors per route, not startup-fail (Phase 2 OAuth env intentionally NOT in startup-fail list — connect routes only consume them on demand).
* AI: WORKING (real OpenRouter, validated, persisted)
* Execution: WORKING (idempotent, logged, audit-complete via impact_snapshot + trace_id)

---

## PHASE COMPLETION STATUS

### Phase 0 — Architecture Lock

Status: ✅ CLOSED (2026-05-07)

Deliverables (all met):
- [x] request tracing_id middleware — `backend/src/middleware/tracing.ts` mints UUID per HTTP request, sets `c.get('requestId')`, echoes `X-Request-ID` header; honors valid incoming UUIDs
- [x] centralized structured request logger — `backend/src/middleware/request-logger.ts` emits `[req] in/out` with request_id; mounted on `*` at app level (`backend/src/index.ts:167-168`)
- [x] log-line correlator chain — `[req]`, `[err]`, `[exec]`, `[AI]`, `[auth]`, `[clerk-webhook]` lines all carry `request_id` and (when authenticated) `user_id` + `org_id`. Verified across 13 backend runtime hardening passes.

Patch Type: Backend middleware (SAFE) — landed

Exit Gate (✅ all satisfied):
✔ كل request فيه tracing_id  
✔ كل log فيه user_id + org_id  

---

### Phase 1 — Foundation

Status: ✅ CLOSED for active surface (2026-05-07); metadata-JSONB requirement is governance-bound to deferred phases (see below)

Active-surface deliverables (all met):
- [x] standard response envelope — `backend/src/utils/response.ts` exports `ok()` / `fail()` emitting canonical `{ success, data, error: { message, code? }, request_id }`. 77 active-route call sites (14 ok + 63 fail) plus errorHandler + deferredPhase wrapper all conform.
- [x] created_by / updated_by audit columns — populated server-side from Clerk JWT `userId` in `auth.ts` JIT (organizations + users), `createCampaign`, `updateCampaign`. Never read from request body.
- [x] Clerk webhook audit — `created_by` populated from `data.created_by` in `organization.created` event handler.

Governance-deferred deliverable (NOT a current defect):
- [ ] metadata JSONB on `decisions`, `creatives`, `automation_runs` (per Phases.md Phase 1 list) — all three tables are governed by deferred phases:
  - `decisions` → Phase 3 anomaly engine (legacy table malformed; canonical AI surface migrated to `ai_decisions`)
  - `creatives` → Phase 5 (DEFERRED)
  - `automation_runs` → Phase 4 Part 2 (DEFERRED)
  This deliverable lands at the corresponding phase unlock. Not classified as Phase 1 incomplete.

Patch Type: DB + Middleware (SAFE) — active-surface portion landed

Exit Gate (active surface ✅):
✔ كل responses بنفس الفورمات  
✔ audit fields موجودة على active write paths
✔ metadata JSONB → governance-bound to phase unlocks (NOT a Phase 1 blocker)

---

### Phase 2 — Data Ingestion

Status: ✅ CLOSED (2026-05-07)

Deliverables (all met):
- [x] Backend code: `connect.ts`, `integrations.ts`, `metrics.ts`, `vault.ts`, `oauth-state.ts`, `services/sync/{meta,google,shopify,index}.ts`, `jobs/inngest.ts` (per `specs/002-data-ingestion/tasks.md` T001–T021)
- [x] Frontend OAuth callback: `app/api/integrations/callback/[platform]/route.ts` (T010)
- [x] Frontend integrations + dashboard wiring: `app/integrations/page.tsx`, `app/integrations/connect/page.tsx`, `app/dashboard/overview/page.tsx`, `app/dashboard/channels/page.tsx` (T011, T012, T024, T025)
- [x] Canonical migration AUTHORED: `supabase/migrations/20260507120000_phase2_data_ingestion.sql` (Phase 2 unlock-prep step, 2026-05-07 continuation #2). Authored from `specs/002-data-ingestion/data-model.md` authority + one runtime-evidenced amendment (`campaign_metrics.integration_id` column required by `services/sync/{meta,google,shopify}.ts`). Tables: `integrations`, `ad_accounts`, `campaign_metrics` (PARTITIONED BY date, 8 quarterly + default partitions), `sync_logs`; all with RLS + org_id-scoped policies + indexes.
- [x] Migration DEPLOYED via `supabase db push` (operator-confirmed 2026-05-07); 4 tables verified live in production
- [x] 503 gates LIFTED on `/integrations/*` and `/metrics/*` in `backend/src/routes/v1/index.ts` (2026-05-07 continuation #3)
- [x] Multi-tenant credential storage operational: `integrations.vault_refresh_token_secret_id` references Supabase Vault; per-org tokens replace the legacy single shared `META_TEST_ACCESS_TOKEN` sandbox env var for newly-connected orgs

Patch Type: DB migration + route gate-lift (SAFE — additive; tables orthogonal to all existing closed-phase tables; gates lifted only on schema-backed routes)

Exit Gate (✅ all satisfied):
✔ Migration deployed and 4 tables + 4 RLS policies live in production
✔ 503 gates lifted on `/integrations/*` and `/metrics/*`
✔ Phase 2 wired frontend (`app/integrations/page.tsx`, dashboard overview/channels) reaches live backend instead of hitting 503
✔ Phase 4 Part 2 unlock condition partially satisfied: per-org Meta tokens addressable from `executeAction` once tokens flow through `vault_refresh_token_secret_id` (vs the shared sandbox env). Google/Shopify per-org tokens identically addressable.

Known follow-ups (NOT Phase 2 blockers; tracked for future patches):
- Phase 2 routes (`integrations.ts`, `connect.ts`, `metrics.ts`) emit LEGACY response envelopes (bare arrays, `{error:'...'}`, `{error,message}`) rather than the canonical Phase 1 `{success, data, error:{message,code}, request_id}` shape used by the active hardened surface (auth, ai, actions, history, campaigns). The wired frontend (`api-client` + `integrations/page.tsx` + dashboard pages) currently consumes the LEGACY shape directly. Migrating these routes onto the canonical envelope is a future Phase-1-cross-cutting patch requiring coordinated frontend changes; **deferred to keep Phase 2 ship without breaking the wired frontend.** Tagged as `PHASE2_ENVELOPE_FOLLOWUP` in this doc.
- Phase 2 OAuth env vars (META_APP_ID/SECRET, GOOGLE_ADS_*, SHOPIFY_API_KEY/SECRET, OAUTH_REDIRECT_BASE_URL) are NOT in the startup-fail-fast list. Missing values produce per-route runtime errors when users attempt OAuth flows. Promoting these to startup-fail-fast (matching the LIVE_FLAG_DEPENDENCIES pattern) is a future hardening task; tagged as `PHASE2_OAUTH_ENV_FAILFAST_FOLLOWUP`.

---

### Phase 3 — Intelligence Layer

Status: ✅ CLOSED (2026-05-03)

Deliverables (all met):
- [x] AI validation layer (`backend/src/utils/aiValidator.ts`)
- [x] reasoning_steps (validated; required; min length 1; non-empty step+insight)
- [x] AI logging (passive logger with structured AILogEntry, console sink active)
- [x] confidence handling (deriveStatus single-source-of-truth at NEEDS_REVIEW_THRESHOLD = 0.7)
- [x] AI persistence (`backend/src/services/ai/persistence.ts`, type-gated to require validated AIResponse)
- [x] Unified execution flow (`backend/src/services/ai/execute-ai-decision.ts`)
- [x] HTTP entry point (`POST /api/v1/ai/execute`)
- [x] Live row in `ai_decisions` from real Clerk-authenticated end-to-end run
- [x] CLOSING_AUDIT.md exists in repo root

Exit Gate (✅ all satisfied):
✔ AI returns valid output (contract enforced both at type-system and runtime)  
✔ Every decision is persisted in DB  
✔ AI logging works (prompt + response + latency)  
✔ Decisions carry confidence_score  
✔ Decisions < 0.7 → status='needs_review' derived in validator  

---

### Phase 4 (minimal slice) — Execution Layer

Status: ✅ CLOSED (2026-05-03)

Closing audit: `CLOSING_AUDIT_PHASE4.md` at repo root

Deliverables (all met):
- [x] `actions_library` table (system reference, RLS authenticated-read, 5 templates seeded, UNIQUE on platform+action_type)
- [x] `decision_history` table (per-org audit log, RLS by org_id, FK to organizations + ai_decisions)
- [x] `execution_id` idempotency (NULLABLE column + partial unique index on `(org_id, execution_id)`)
- [x] `impact_snapshot` (JSONB column populated from each handler's after-state)
- [x] `executed_by` (CHECK ∈ `{manual, automation}`; default `manual`)
- [x] `executeAction(input)` service — validates template + params, logs lifecycle, idempotency-aware, dispatches handler, inserts decision_history (`backend/src/services/execution/action-executor.ts`)
- [x] `POST /api/v1/actions/:id/execute` route — accepts `params`, optional `ai_decision_id`, `trace_id`, `execution_id` (idempotency key)
- [x] **4 real-mode action handlers behind feature flags + token + org allowlist:**
  - [x] `meta.pause_campaign` (Meta Graph API single POST)
  - [x] `meta.decrease_budget` (Meta Graph API GET-then-POST, money-DOWN direction)
  - [x] `meta.increase_budget` (Meta Graph API GET-then-POST, money-UP direction, `META_INCREASE_BUDGET_MAX_PERCENT` server-side cap)
  - [x] `send_alert_email` (Resend, recipients server-computed from org admins, placeholder filter)
- [x] Structured `[exec]` console logger emitting phases: `exec.start`, `exec.api_call`, `exec.api_response`, `exec.end`, `exec.error`
- [x] Live verification: idempotency rejects duplicate `(org_id, execution_id)` (`SQLSTATE 23505`); cross-org reuse permitted; NULL execution_id allows multiple inserts
- [x] Type-check clean (`tsc --noEmit -p backend/tsconfig.json` → 0 errors at every step)
- [x] CONSTITUTION compliance: org isolation, fail-loud, no silent failures, no schema-bypass

Exit Gate (✅ all satisfied):
✔ All non-deferred Phases.md Phase 4 explicit deliverables met (actions_library, decision_history, real APIs, idempotent execution, log every result, data snapshot, org_id enforced, execution_id, impact_snapshot, execution_mode)  
✔ Idempotency live-verified at the DB constraint level  
✔ Server-side max-cap on the only money-UP real action  
✔ Multi-action handler set proven by handler-shape SQL smoke (success / simulated / failed-handler)  

---

### Phase 4 Part 2 — Automation Engine + Multi-Platform

Status: ✅ CLOSED (2026-05-07) — schema deployed via `supabase db push`; PR #5 merged into main (commit `df2f243`); `/automation/*` 503 gate lifted; manual-execute path operational. Auto-firing on AI decision stream RESOLVED 2026-05-09 via continuation #23 (post-persist hook landed) + continuation #24 (Path F AI Output Contract category formalization activated the hook); see updated "Governance-RESOLVED" subsection below.

Authored / shipped 2026-05-07:
- [x] `automation_rules` table (org-scoped, RLS, trigger_type enum, min_confidence_threshold, action_template_id FK, action_params JSONB, run counters)
- [x] `automation_runs` table (org-scoped, RLS; uses `ai_decision_id` REFERENCES ai_decisions(id) — substituted for spec's deprecated `decisions(id)` per CANONICAL AI SYSTEM resolution)
- [x] `decision_history` extended with nullable `automation_rule_id` + `automation_run_id` columns (additive; preserves Phase 4 minimal close)
- [x] Canonical migration: `supabase/migrations/20260507130000_phase4_part2_automation.sql` — authored from `specs/004-execution-layer/data-model.md` with one runtime-evidenced amendment (substituted `ai_decisions(id)` for deprecated `decisions(id)` per "Do NOT reactivate the deprecated legacy decisions table" instruction)
- [x] Automation engine rewritten (`backend/src/services/execution/automation-engine.ts`): exports `evaluateRulesForAIDecision(orgId, aiDecisionId)`, `executeRule(orgId, ruleId, aiDecisionId?)`, and a legacy-shim `dispatchAutomation(orgId, runId)` retained for spec-conformance but explicitly dormant. Uses canonical `ai_decisions` exclusively; does not reference deprecated `decisions` table. Confidence comparison normalizes ai_decisions.confidence_score (NUMERIC 0–1) against rule.min_confidence_threshold (INTEGER 0–100).
- [x] Automation router canonicalized (`backend/src/routes/v1/automation.ts`): GET/POST/PATCH/DELETE `/rules`, GET `/runs`, plus new `POST /rules/:id/execute` for manual rule firing. Canonical `ok()/fail()` envelope, UUID + body-shape + LIST validation parity with the rest of the active hardened surface.
- [x] `google.pause_campaign` real-mode handler added to `action-executor.ts` (`realGooglePauseCampaign`). Behind `GOOGLE_PAUSE_CAMPAIGN_LIVE` flag + `GOOGLE_LIVE_ORG_ALLOWLIST` (defaults OFF; mirrors Meta pattern). Reads per-org refresh token via Phase 2 Vault (`integrations.vault_refresh_token_secret_id` → `readSecret`), refreshes OAuth at `oauth2.googleapis.com/token`, resolves customer_id from `ad_accounts.platform_account_id`, calls Google Ads API `customers/{cid}/campaigns:mutate` with `developer-token` header. Tokens never logged.
- [x] Per-org execution rate limit added to `executeAction`: env-configurable `ACTION_EXECUTION_MAX_PER_MINUTE` (default 60); DB-backed count of `decision_history` inserts in last 60s; throws `code: 'RATE_LIMITED'` with `retryAfterSeconds`. Idempotent replays do NOT count (early-returned upstream of the guard). Set to 0 to disable.
- [x] `executeAction` `ExecuteActionInput` extended with optional `automationRuleId` + `automationRunId` for Phase 4 Part 2 audit linkage. Both written into `decision_history` INSERT (NULLABLE; manual executions leave NULL).
- [x] `LIVE_FLAG_DEPENDENCIES` startup-fail-fast extended with `GOOGLE_PAUSE_CAMPAIGN_LIVE → [GOOGLE_ADS_DEVELOPER_TOKEN, GOOGLE_ADS_CLIENT_ID, GOOGLE_ADS_CLIENT_SECRET]`.

Completed (continuation #5, 2026-05-07):
- [x] `supabase db push` — deployed migration to production
- [x] Verified deploy via PR #5 merge into main + canonical migration in `/supabase/migrations/20260507130000_phase4_part2_automation.sql` (10664 bytes)
- [x] Lifted `/automation/*` 503 gate in `backend/src/routes/v1/index.ts` (single-line removal + governance comment block; 4 remaining deferred gates preserved)

Pending (DEFERRED-BY-GOVERNANCE — separate phase scopes):
- [ ] Frontend wiring of `/automation/*` mocked surfaces (Phase 7 frontend-integration scope)

Governance-RESOLVED (cross-phase items previously blocked, now resolved):
- [x] Auto-firing of automation_rules on every `ai_decisions` INSERT — RESOLVED 2026-05-09 via continuation #23 (Path 3 selected: post-persist hook in execute-ai-decision.ts) + continuation #24 (Path F: AI Output Contract `category` formalization — additive nullable column + validator/persistence/extractCategory two-stage shim + prompt emission + visibility bridge). Both options (b) + (c) from the prior blocker were ultimately combined into Path F's hybrid approach. AI decisions now stream-trigger matching enabled rules via `evaluateRulesForAIDecision`; manual path via `POST /api/v1/automation/rules/:id/execute` preserved for admin-on-demand firing.

Governance-BLOCKED (still require explicit cross-phase authorization):
- [ ] `meta.*_budget` for non-test orgs (still single-tenant via shared `META_TEST_ACCESS_TOKEN` — Phase 2 vault flow is in place; per-org Meta refresh-token migration is a future hardening matching the google.pause_campaign pattern). NOTE: per-org Vault Meta token resolution for `pause_campaign` + CREATE handlers added in continuation #21 Part B (Vault-first preference; sandbox fallback); `*_budget` handlers retain single-tenant `META_TEST_ACCESS_TOKEN` until separately authorized.

- [ ] `process.exit(1)` softening on `unhandledRejection` (cross-cutting concern, broader than Phase 4 — separate governance discussion).

Spec-vs-runtime adaptation log (governance-driven):
- Spec data-model.md `automation_runs.decision_id REFERENCES decisions(id)` → migrated to `ai_decision_id REFERENCES ai_decisions(id)` per "Do NOT reactivate decisions table" + Phase 4 minimal close pattern.
- Spec data-model.md `decision_history.decision_id REFERENCES decisions(id)` → NOT added; current schema already uses `ai_decision_id REFERENCES ai_decisions(id)` (Phase 4 minimal close); legacy `decision_id` column was deliberately omitted there.
- Spec data-model.md `ALTER TABLE decision_runs ADD COLUMN rules_executed` → NOT added; `decision_runs` table does not exist in canonical schema (Phase 3 anomaly DEFERRED).
- Spec dispatchAutomation `for-each (rule, active-decision-from-decision_runs-cycle)` pattern → REPLACED with `evaluateRulesForAIDecision(orgId, aiDecisionId)` (per-AI-decision invocation; no decision_runs cycle).
- Spec automation_rules trigger_type matching `decision.type` → originally adapted to match `ai_decisions.result.category` JSONB path (soft semantic dependency on AI Output Contract). FORMALIZED 2026-05-09 via continuation #24 Path F: AI Output Contract now carries optional top-level `category` field; persistence writes to dedicated `ai_decisions.category` nullable column; `extractCategory` is a two-stage shim (column-level first, JSONB `result.category` fallback). Rule still won't auto-fire if AI emits no category at either surface — the dormant-by-default safety property is preserved.

Patch Type: DB migration + backend code (SAFE — additive schema; canonical envelope preserved; HARD LOCK invariants preserved verbatim)

Exit Gate (✅ all satisfied):
✔ Migration deployed; 2 new tables + 2 added columns + 2 RLS policies live in production
✔ /automation/* 503 gate lifted (continuation #5)
✔ Auto-firing path RESOLVED 2026-05-09 via continuation #23 (post-persist hook) + continuation #24 (Path F category formalization); originally accepted as governance-deferred at #5 close
✔ Manual-execute path operational via `POST /api/v1/automation/rules/:id/execute`
✔ All HARD LOCK invariants from Phase 4 minimal close preserved verbatim
✔ Closed Phase 3 linear pipeline UNTOUCHED structurally; additively extended at #24 Path F (AIResponse optional `category` + ai_decisions.category nullable column) without reopening the closed slice
✔ Legacy `decisions` table NOT reactivated (canonical AI surface remains `ai_decisions`)

---

### Phase 5 — AI Creatives

Status: ✅ CLOSED (2026-05-07) — canonical schema migration deployed via `supabase db push`; PR #6 merged into main (commit `f5aa8ff`); `/creatives/*` + `/brand-kit/*` 503 gates lifted; existing backend scaffolding (services + routes + 5 frontend mocked-shell pages) now reaches live schema.

Deliverables (all met):
- [x] Backend services (pre-existing scaffolding): `services/creatives/{brand-kit,copy-generation,creative-generator,image-generation,storage,index}.ts` (per FR-001..FR-013)
- [x] Backend routes (pre-existing, behind 503): `routes/v1/{creatives,brand-kit}.ts`
- [x] Frontend mocked-shell pages (pre-existing): `app/creatives/{page,archive,brand-kit,editor,results}/page.tsx` (5 pages)
- [x] Canonical migration: `supabase/migrations/20260507140000_phase5_creatives.sql` — 3 new tables (`brand_kits`, `creative_generations`, `creatives`) + 2 organizations columns (`plan_type`, `vault_byok_openrouter_secret_id` — Phase 7 substrate required by Phase 5 FR-011 BYOK gate; documented as the source of currently-active 42703 in `creative-generator.ts:resolveApiKey` defensive comment) + 1 storage bucket (`creatives`, private, signed-URL access). All RLS + indexes per spec.

Spec-vs-runtime adaptation log (governance-driven):
- Spec functional requirements (FR-001..FR-013) → schema captures Brand Kit (one per org, FR-002), Creative Generation (job ledger, FR-005), Creative (per-output rows, FR-007/FR-009/FR-010), and FR-011 BYOK substrate.
- Spec assumes `creatives` Storage bucket exists ahead of time → migration creates it idempotently via `INSERT INTO storage.buckets ... ON CONFLICT DO NOTHING`.
- Code references `organizations.plan_type` + `vault_byok_openrouter_secret_id` (creative-generator.ts:19-22) → migration adds them as NULLABLE/`subscription`-default substrate. This is the MINIMAL forward-positioning of Phase 7 schema columns required by Phase 5 FR-011 — it does NOT implement Phase 7 monetization (no Stripe, no billing UI, no plan upgrade flow, no credits ledger).
- Frontend pages currently mocked-shells with no `apiClient` coupling → no PHASE5_ENVELOPE_FOLLOWUP risk; routes can be canonicalized in a future pass without breaking frontend.

Completed (continuation #7, 2026-05-07):
- [x] `supabase db push` — deployed migration to production
- [x] Verified deploy via PR #6 merge into main (commit `f5aa8ff`) + canonical migration in `/supabase/migrations/20260507140000_phase5_creatives.sql` (11722 bytes) in main worktree
- [x] Lifted `/creatives/*` + `/brand-kit/*` 503 gates in `backend/src/routes/v1/index.ts` (single multi-line removal + governance comment block; 2 remaining deferred gates preserved on `/decisions/*` + `/alerts/*`)

Pending (DEFERRED-BY-GOVERNANCE — separate phase scopes):
- [ ] Operator-side env (optional, for live image generation): `SILICONFLOW_API_KEY` per CLAUDE.md TECH STACK
- [ ] Frontend wiring of `app/creatives/*` mocked pages (Phase 7 frontend-integration scope)
- [x] PHASE5_ENVELOPE_FOLLOWUP — canonicalize `creatives.ts`/`brand-kit.ts` from legacy envelope to Phase 1 ok()/fail() (closed continuation #10, 2026-05-08; backend-only minimal diff; frontend not coupled)

Patch Type: DB migration + storage bucket + route gate-lift (SAFE — additive schema; existing backend services + routes operate against new schema; HARD LOCK invariants preserved)

Exit Gate (✅ all satisfied):
✔ Migration deployed; 3 new tables + 2 added columns + 1 storage bucket + 3 RLS policies live in production
✔ /creatives/* + /brand-kit/* 503 gates lifted (continuation #7)
✔ Phase 7 substrate columns (organizations.plan_type, vault_byok_openrouter_secret_id) live; resolveApiKey 42703 drift resolved
✔ All HARD LOCK invariants from Phase 4 minimal close preserved verbatim
✔ Closed Phase 4 Part 2 + Phase 4 minimal + Phase 3 linear + Phase 2 + Phase 1 + Phase 0 ALL UNTOUCHED
✔ Legacy `decisions` table NOT reactivated (canonical AI surface remains `ai_decisions`)
✔ NO Phase 7 monetization implementation (only the 2-column substrate Phase 5 FR-011 required)

---

### Phase 6 — Campaigns

Status: 🟡 PARTIAL (Sub-pass A authored 2026-05-08, continuation #11; deploy + Sub-pass B pending)

Sub-pass A deliverables (authored, deploy pending):
- [x] Backend campaigns surface already pre-existing + canonical-enveloped (continuation #10): 6 endpoints (GET / · POST / · GET /:id · PATCH /:id · POST /:id/ai-suggestions · POST /:id/push) at `backend/src/routes/v1/campaigns.ts`; service layer with metrics + 14d trend + decisions overlay (`backend/src/services/campaigns/{campaigns,ai-suggestions}.ts`); status state-machine with role gating; audit columns; PGRST116 discriminators; schema-drift tolerance for legacy `decisions` table read.
- [x] Canonical migration `supabase/migrations/20260508120000_phase6_create_campaign_templates.sql` AUTHORED + DEPLOYED (continuation #12, 2026-05-08) — purely additive: 2 INSERT rows (`meta.create_campaign` + `google.create_campaign`) into `actions_library` idempotent via existing `(platform, action_type)` UNIQUE; no schema/RLS/FK/index changes. Live registry version: `20260508175638_phase6_create_campaign_templates` (server timestamp). Post-deploy verified: 7 actions_library rows total; 2 create_campaign rows; natural-key lookup resolves to live ids (meta=`bd2faa18-d4a6-46e2-af1c-f13c33166ca8`, google=`7c639eb5-f2ef-47dd-80a5-f0346b036a5f`).
- [x] `pushCampaign` natural-key template resolution — replaced two hardcoded fictional UUID constants (`...000000009` / `...000000010` — never seeded) with `(platform, action_type='create_campaign')` lookup against `actions_library`. Closes silent 404 bug.
- [x] Phase 6 NEW invariant — cross-account ownership validation in `pushCampaign`: when `campaign.ad_account_id` is set, verify it resolves to an `ad_accounts` row with matching org_id; throw `CROSS_ACCOUNT_FORBIDDEN` (403) on mismatch. Closes the gap between bare `campaigns.ad_account_id UUID` column (NOT FK to `ad_accounts(id)` per 20260503100000:36) and the multi-tenant ownership requirement.
- [x] `create_campaign` simulated-only handler entry in `ACTION_HANDLERS` — returns `{ success: true, result_data: { simulated: true, ... } }`; matches established pause_campaign/increase_budget/decrease_budget simulated-fallthrough pattern. Decision_history reflects `result='success'`.

Sub-pass B — DONE (continuation #13, 2026-05-08):
- [x] Frontend wiring of `app/campaigns/{page, [id]/page, create/page}.tsx` against canonical envelope
- [x] `lib/api-client.ts` envelope-unwrap unification — detection-based, backwards-compatible with Phase 2 legacy shapes; folds in PHASE2_ENVELOPE_FOLLOWUP coordinated FE update axis
- [x] Save Draft path operational; Launch button intentionally UNWIRED until Sub-pass C
- [x] Backend `tsc --noEmit` → 0 errors; frontend `tsc --noEmit` → 0 errors

Sub-pass C (DEFERRED-BY-GOVERNANCE — pending operator authorization):
- [ ] Real-mode `realMetaCreateCampaign` + `realGoogleCreateCampaign` handlers behind LIVE_FLAG_DEPENDENCIES-gated flags + per-org allowlist (mirrors pause_campaign pattern)
- [ ] Real-mode handlers will land alongside Sub-pass B once frontend reveals real param shape

Out-of-scope (DEFERRED-BY-GOVERNANCE per holistic strategic recommendation):
- [ ] Realtime broadcast channels (`campaigns:{org_id}`, `execution_stream:{org_id}:{campaign_id}`) — Supabase Realtime infra deferred
- [ ] `adsets` / `recommendations` / `risks` / `ai_insights` / `execution_logs` / `risk_scores` tables — feature-layer deferrals
- [ ] Bulk actions backend endpoint
- [ ] Inline-edit on adsets
- [ ] `sort_by` / `order` query params on GET /campaigns
- [ ] Cache layer for campaign list

Patch Type: DB seed migration + service-layer hardening + simulated handler entry (SAFE — additive seed; no schema changes; HARD LOCK invariants preserved verbatim)

Strategic deviation justification: deviation from PATCH QUEUE strict order #9 (Phase X broader) operator-authorized on grounds of (a) runtime evidence override — silent `pushCampaign` bug; (b) Phase 6 NEW invariant unmet; (c) Phase X broader value-prerequisites (real user load) not yet met. Documented in continuation #11 entry.

---

### Phase X — AI Orchestration

Status: 🔄 SPLIT

- "Linear pipeline" portion (single trace through validate → log → persist via `executeAIDecision`): ✅ shipped as Phase 3 close
- "Broader" portion (MCP routing, tool-governance, DB log-sink fan-out for ai_logs, strategy_tag enum): 🔒 LOCKED until Phase 4 Part 2 stable

Unlock Condition for the broader Phase X:
✔ Phase 4 Part 2 stable + automation engine working with ai_decisions linkage (✅ now satisfied)

Documentation:
- `specs/009-ai-orchestration/spec.md` describes the full Phase X surface; only the linear-pipeline portion is implemented today.

---

## CANONICAL AI SYSTEM (RESOLUTION)

| Concept | Canonical | Status |
|---|---|---|
| AI Output Contract storage | **`ai_decisions`** | active, working, has rows |
| AI lifecycle audit | **`ai_logs`** (schema present; DB sink wiring deferred to broader Phase X) | console-active, DB-pending |
| Decision-to-action audit | **`decision_history`** | active; idempotent; carries impact_snapshot + trace_id + ai_decision_id |
| Legacy anomaly engine | **`decisions`** table | DEPRECATED — malformed in live DB; not used by current pipeline; future decision: revive separately or retire entirely |

All Phase 4+ links to AI use `ai_decisions(id)` and `ai_decisions.trace_id`. The legacy `decisions` table is no longer referenced by any Phase 4 code.

---

## PATCH QUEUE (EXECUTION RULE)

Priority Order:

1. ✅ Phase 3 (core) — DONE
2. ✅ Phase 4 minimal slice — DONE
3. ✅ Phase 0 patch (tracing_id + structured logging) — DONE (2026-05-07)
4. ✅ Phase 1 patch (envelope + audit columns) — DONE (2026-05-07) for active surface; metadata-JSONB on `decisions`/`creatives`/`automation_runs` is bound to those phases' unlocks (NOT a Phase 1 blocker)
5. **13 backend runtime-hardening passes** — DONE (cumulative, closed) — UUID/body/path validation parity, LIST validation parity, PGRST116 discriminator parity, request_id triple-sink, ai-suggestions silent-write fix, pushCampaign discriminator, auth.ts canonical normalization, AI empty-prompt protection, body-limit cap, smoke-flag gate, LIVE-flag dependency fail-fast, deferred-router 503 gating
6. ✅ Phase 2 (data ingestion) — DONE (2026-05-07): migration `20260507120000_phase2_data_ingestion.sql` deployed via `supabase db push`; 4 canonical tables live; 503 gates lifted on `/integrations/*` and `/metrics/*`; wired frontend (integrations + dashboard overview/channels) reaches live backend. Multi-tenant credential storage operational via Supabase Vault.
7. ✅ Phase 4 Part 2 (automation engine + multi-platform real handlers) — DONE (2026-05-07): canonical migration `20260507130000_phase4_part2_automation.sql` deployed via `supabase db push`; PR #5 merged into main (commit `df2f243`); 2 canonical tables (automation_rules, automation_runs) + 2 nullable decision_history columns live in production; `/automation/*` 503 gate lifted; automation engine + google.pause_campaign + per-org rate limiting operational. AUTO-FIRING on AI decision stream RESOLVED 2026-05-09 via continuation #23 + #24 (see PATCH QUEUE position #16); manual-execute path via `POST /automation/rules/:id/execute` preserved for admin-on-demand firing.
8. ✅ Phase 5 (AI Creatives) — DONE (2026-05-07): canonical migration `20260507140000_phase5_creatives.sql` deployed via `supabase db push`; PR #6 merged into main (commit `f5aa8ff`); 3 canonical tables (brand_kits, creative_generations, creatives) + 2 organizations columns (plan_type, vault_byok_openrouter_secret_id — Phase 7 BYOK substrate for FR-011) + 1 private storage bucket (`creatives`) live in production; `/creatives/*` + `/brand-kit/*` 503 gates lifted; existing backend services + routes + 5 frontend mocked-shell pages now reach live schema.
9. Phase X broader (MCP, tool governance, DB log-sink fan-out, strategy_tag enum) — REQUIRES EXPLICIT AUTHORIZATION; Phase 4 Part 2 prerequisite now satisfied. Specs: `specs/mcp-orchestration.md`, `specs/mcp-integration.md`, `specs/ai-execution.md`, `specs/009-ai-orchestration/spec.md`.
9. Frontend wiring of remaining mocked surfaces — bound to each owning phase's unlock state (Phase 6 campaigns frontend wiring, Phase 7 frontend integration scope for settings/billing/team)
10. PHASE2_ENVELOPE_FOLLOWUP — migrate `integrations.ts`/`connect.ts`/`metrics.ts` onto canonical Phase 1 `{success, data, error:{message,code}, request_id}` envelope (currently emit legacy bare-array / `{error:'...'}` shapes). Coordinated frontend update to api-client/page consumers required. NOT a Phase 2 blocker; deferred for future Phase-1-cross-cutting patch.
11. PHASE2_OAUTH_ENV_FAILFAST_FOLLOWUP — promote META_APP_ID/SECRET, GOOGLE_ADS_*, SHOPIFY_API_KEY/SECRET, OAUTH_REDIRECT_BASE_URL to startup-fail-fast (matches LIVE_FLAG_DEPENDENCIES pattern). NOT a Phase 2 blocker; runtime errors today are explicit per-route.
12. ✅ PHASE5_ENVELOPE_FOLLOWUP — DONE (2026-05-08, continuation #10): canonicalized `creatives.ts` + `brand-kit.ts` onto Phase 1 ok()/fail() envelope. Backend-only minimal diff (+45/-43 LOC across two files); frontend uncoupled (mocked-shells; zero apiClient references); zero schema/middleware/router-mount changes; HARD LOCK invariants preserved verbatim; Phase 5 closure state preserved.
13. ✅ Phase 6 (Campaigns) Sub-pass A — DONE (2026-05-08, continuations #11 author + #12 deploy): authored migration `20260508120000_phase6_create_campaign_templates.sql` (2 INSERT rows in `actions_library`); deployed via Supabase MCP apply_migration as registry version `20260508175638`; replaced fictional hardcoded UUIDs with natural-key template lookup in `pushCampaign`; added Phase 6 NEW invariant cross-account ownership validation (`CROSS_ACCOUNT_FORBIDDEN` → 403); added `create_campaign` simulated-only `ACTION_HANDLERS` entry. Strategic deviation from queue order #9 operator-authorized. Phase 0–5 invariant verification post-deploy: ALL PASS.
14. ✅ Phase 6 (Campaigns) Sub-pass B — DONE (2026-05-08, continuation #13): wired `app/campaigns/{page, [id]/page, create/page}.tsx` to canonical campaigns API; opportunistically unified `lib/api-client.ts` envelope-unwrap (detection-based, backwards-compat with Phase 2 legacy shapes — folds in PHASE2_ENVELOPE_FOLLOWUP coordinated FE-axis update); added Step 0 Campaign Name input (required by backend POST contract); Save Draft operational; Launch button intentionally unwired until Sub-pass C. Active hardened surface envelope coverage now end-to-end through frontend for campaigns list/detail/create + ai-suggestions + push. Sub-pass C (real-mode Meta/Google CREATE handlers) remains pending operator authorization.
15. 🟡 Phase 7 Sub-pass A0 — IN PROGRESS (2026-05-08, continuation #14): authored canonical migration `20260508130000_phase7_a0_credits_substrate.sql` — additive substrate only. Adds `organizations.credits_balance INTEGER NOT NULL DEFAULT 0 CHECK (>= 0)` + `ai_usage_logs` table (RLS by org_id) + `credits_ledger` table (RLS by org_id) + `deduct_credits` / `refund_credits` / `log_ai_usage` RPCs. Closes Phase 5 ↔ Phase 7 silent runtime drift on POST /creatives/generate (currently fails with "function deduct_credits does not exist" for non-LTD orgs). ZERO backend code changes — call sites in `routes/v1/creatives.ts` and `services/creatives/creative-generator.ts` already match the new RPC contracts. **Deploy step pending** via authorized `supabase db push` / Supabase MCP `apply_migration`. Sub-pass A1 (Stripe webhook + plan upgrade route + BYOK key storage routes + AI usage logging instrumentation at ai/execute + ai-suggestions + decision-generator + creative-generator call sites) DEFERRED pending operator authorization. Sub-pass B (settings/billing/BYOK UI) DEFERRED.
16. ✅ Phase 4/Phase 6 BRIDGE — auto-firing automation_rules on AI decision stream — DONE (2026-05-09, continuations #23 + #24). Resolves the cross-phase dependency previously listed as Phase 4 Part 2 GOVERNANCE-BLOCKED item (auto-firing on AI decision stream). Continuation #23: single-line fire-and-forget post-persist hook in `services/ai/execute-ai-decision.ts` calling `evaluateRulesForAIDecision(input.org_id, decision_id)`; backend-only; ZERO migration. Continuation #24 — Path F (hybrid): additive nullable column migration `20260509130000_phase3_ai_decisions_category.sql` (deployed) + AI Output Contract `category?: string` formalization in `aiValidator.ts` + persistence write to `ai_decisions.category` + two-stage extractCategory shim (column → JSONB fallback) in `automation-engine.ts` + AI prompt emission in both `ai.ts` systemPrompts + operator-visibility bridge `ai_decisions(category)` joined into `GET /automation/runs` SELECT. NO new orchestration framework, NO MCP routing, NO new services, NO frontend pages wired, NO closed-phase reopened structurally. Stream-driven Decision → Action → Result loop now operational and end-to-end auditable. Phase X broader (MCP/tool-governance/log-sink fan-out/strategy_tag enum) remains LOCKED — this bridge is NOT MCP routing.

---

## EXECUTION RULES

- NEVER switch phase until completion condition is met
- ALWAYS apply patches in parallel where they don't conflict (Phase 0 + Phase 1 are parallel-safe)
- DO NOT skip blocker validation
- Frontend MUST NOT break

---

## NEXT ACTION (STRICT)

👉 Phase 5 (AI Creatives) CLOSED (2026-05-07). Phase 6 Sub-passes A + B + C CLOSED (2026-05-08, continuations #11/#12/#13/#20). Phase 7 Sub-passes A0/A0.5/A1a/A1b/A1c/B/D + Stripe webhook hardening CLOSED (2026-05-08 → 2026-05-09, continuations #14 → #22). Phase 4/Phase 6 BRIDGE — auto-firing automation_rules on AI decision stream — RESOLVED (2026-05-09, continuations #23 + #24 Path F). All Phase 0 + Phase 1 + Phase 2 + Phase 3 (linear) + Phase 4 minimal + Phase 4 Part 2 + Phase 5 + Phase 6 Sub-A/B/C + Phase 7 Sub-A0/A0.5/A1a/A1b/A1c/B/D + #23 + #24 closed. Active-surface backend runtime hardening SATURATED. The PATCH QUEUE next major item (#9 — Phase X broader) remains governance-locked and requires EXPLICIT user authorization.

✅ Closed since last NEXT ACTION:
- E) Phase 5 unlock — schema deployed + 3 new tables + 2 organizations columns + 1 storage bucket + `/creatives/*` + `/brand-kit/*` 503 gates lifted ✅
- J) Phase 6 Sub-passes A + B + C — campaign templates seeded; frontend wired to canonical envelope; real-mode Meta + Google Ads `create_campaign` handlers behind LIVE flags + per-org allowlist (continuations #11/#12/#13/#20) ✅
- K) Phase 7 monetization end-to-end — credits substrate + Stripe inbound webhook + outbound Checkout/Portal + per-org Meta Vault token resolver + initial credits JIT grant + Stripe replay-protection + frontend BYOK/plan UI + automation/history wiring (continuations #14 → #22) ✅
- L) Phase 4/Phase 6 BRIDGE — auto-firing automation_rules on AI decision stream — post-persist hook (#23) + Path F AI Output Contract `category` formalization + visibility bridge (#24) ✅

🔒 GOVERNANCE-LOCKED (require explicit authorization):
- F) Phase X broader — MCP routing + tool governance + DB log-sink fan-out + strategy_tag enum. Specs: `specs/mcp-orchestration.md`, `specs/mcp-integration.md`, `specs/ai-execution.md`, `specs/009-ai-orchestration/spec.md`. Prerequisites: Phase 4 Part 2 stable (now satisfied). Note: any future post-persist hook EXTENSION beyond #23's existing single-call hook will require its own minimal-diff cross-phase authorization.
- G) Remaining Phase 6 frontend wiring (4 mocked-shell pages governance-deferred per #22: `app/automation/strategies/page.tsx`, `app/actions/page.tsx`, `app/actions/[id]/page.tsx`, `app/creatives/page.tsx` — all blocked on UX↔backend semantic mismatches per #22 governance documentation). Phase 7 monetization extensions beyond what is already shipped (e.g., invoice/utilization endpoints, payment-method management) — bound to separate authorization.
- H) ✅ Auto-firing of automation_rules on AI decision stream — RESOLVED 2026-05-09 (continuations #23 + #24 Path F). The three previously-tracked options (Phase 3 anomaly unlock OR AI Output Contract result.category extension OR closed-Phase-3 post-persist hook) were ultimately combined: Path 3 hook (#23) + Path-2-equivalent column-level formalization (#24) shipped together. Manual `POST /automation/rules/:id/execute` preserved.
- I) ✅ PHASE5_ENVELOPE_FOLLOWUP — CLOSED continuation #10 (2026-05-08). `creatives.ts` + `brand-kit.ts` canonicalized onto Phase 1 ok()/fail() envelope. Backend-only minimal diff; frontend uncoupled (mocked-shells); zero schema/middleware/router-mount changes; backend tsc 0 + frontend tsc 0.
- M) ✅ PHASE2_ENVELOPE_FOLLOWUP — RESOLVED (continuation #31, 2026-05-12) under ADJACENT CONTINUATION AUTHORITY ("wiring already-existing contracts" + "expanding existing canonical APIs"). Backend route-side canonicalization complete: all 16 sites across `integrations.ts` (5) + `connect.ts` (7) + `metrics.ts` (4) converted from legacy `c.json({error:'...'})` / bare `c.json({...})` shapes onto Phase 1 canonical `ok()`/`fail()` envelope. FE apiClient detection-unwrap (#13) absorbed the change transparently — zero FE consumer behavior change. Added `request_id` correlation to Phase 2 routes (previously absent). DB errors now throw → errorHandler emits sanitized canonical 500 with request_id (matches #4 hardening pattern). Single 204 No Content response in `integrations.ts` DELETE preserved as raw `Response` (canonical envelope is JSON-only; 204 has no body by HTTP spec). All HARD LOCK invariants preserved verbatim; no schema/migration/route-mount changes; backend tsc 0; frontend tsc 0. PATCH QUEUE position #10 closed.
- N) PHASE2_OAUTH_ENV_FAILFAST_FOLLOWUP (PATCH QUEUE #11) — non-blocker; promote OAuth + STRIPE_SECRET_KEY to startup-fail-fast; tracked.
- O) Per-org Meta `*_budget` Vault token migration — `pause_campaign` + CREATE handlers migrated #21 Part B; `*_budget` handlers retain single-tenant `META_TEST_ACCESS_TOKEN`; tracked.
- P) ✅ Auto-fire success log line — RESOLVED (continuation #28, 2026-05-12). Added symmetric success-branch logger to the #23 hook's `.then()` callback in `services/ai/execute-ai-decision.ts`. Logs only when `rulesFired > 0` (silent for the typical no-match case to avoid log flooding). Observability-only; ZERO behavior change; preserves CONSTITUTION §3 "Fail Loudly" pattern by symmetrizing operator visibility across success + failure branches. Operators can now grep `[AI] auto-fire automation rules fired=` for every auto-firing run in the `[AI]` console stream alongside the existing failure-branch `[AI] auto-fire automation rules failed` lines.
- Q) ✅ action-executor.ts pre-dispatch logging-mode label accuracy — RESOLVED (continuations #26 + #27, 2026-05-09). #26 corrected meta.pause_campaign env-gate divergence introduced when #21 Part B made dispatch Vault-aware. #27 widened the label across all 7 live-capable handler dispatch tuples (meta/google × pause_campaign/create_campaign + meta.{increase,decrease}_budget + send_alert_email) by mirroring each ACTION_HANDLERS dispatch conjunction verbatim. Observability-only; ZERO dispatch / handler / Vault / env-precedence change. The separate semantic-gap follow-up for `meta.{increase,decrease,create}_budget` Vault adoption (per-org Vault dispatch shape matching google.pause_campaign) remains tracked as item O.

🟢 Governance-NEUTRAL (allowed without phase unlock):
- SEO baseline only — `app/sitemap.ts`, `app/robots.ts`, `app/manifest.ts`, `app/opengraph-image.tsx`, root `metadata` cleanup. NOT a phase deliverable; production-baseline only. NO marketing strategy / blog / docs / public-funnel work — those are governance-locked.
- Drift cleanup of orphan/zombie surfaces — `app/dashboard/saas/page.tsx` (Stitch template residue, broken types), `app/dashboard/channel/` singular duplicate, hardcoded `/campaigns/1` placeholder in `Sidebar.tsx`. Each is removable with minimal diff and breaks no phase.

🚫 DO NOT:
- Touch Phase 2 without explicit authorization
- Build automation engine before Phase 2 lands
- Reactivate legacy `decisions` table without an explicit decision
- Re-open the closed Phase 4 minimal slice
- Re-open closed Phase 0 / Phase 1 patches
- Re-open the 13 closed backend runtime hardening passes
- Wire mocked frontend surfaces ahead of their owning phase unlock
- Implement BYOK / Stripe / credits / onboarding / public marketing without Phase 7 unlock

---

## DECISION RULE (VERY IMPORTANT)

If:

- Phase incomplete → KEEP WORKING on current minimal scope
- Phase complete → MOVE per priority order above
- Phase blocked → APPLY parallel patches (0 or 1) ONLY

---

🚨 HARD LOCK (UPDATED 2026-05-03 — Phase 4 close)

AI MUST NOT:
- write to DB before validation layer exists ✅ (enforced at type-system + control flow)
- call external APIs without logging ✅ (passive logger emits request/raw on every call)

EXECUTION (Phase 4 minimal — closed) MUST NOT:
- bypass `actions_library` template lookup
- skip required-parameter validation
- write to `decision_history` without server-side org_id
- run automation logic — automation engine is NOT in the closed slice
- exceed `META_INCREASE_BUDGET_MAX_PERCENT` (default 50) on a single increase_budget call
- bypass the partial unique index on `(org_id, execution_id)` when `execution_id` is supplied
- email anyone outside the calling org's admin set; placeholder emails (@placeholder.local, @clerk.placeholder) are filtered

If violated → STOP execution

---

## REAL SYSTEM CAPABILITIES (RUNTIME TRUTH — UPDATED 2026-05-09 — continuation #25 reconciliation pass)

- AI: ✅ WORKING — real OpenRouter, validated, persisted, type-gated. AI Output Contract carries optional top-level `category` field (Path F #24, 2026-05-09); persistence writes to `ai_decisions.category` nullable column.
- Decisions: ✅ persisted in `ai_decisions` (canonical); legacy `decisions` table deprecated
- AI Logging: ✅ console-level structured `[AI]` lines via `aiLogger`; DB sink fan-out pending (broader Phase X). Auto-fire infra-failure log line landed at #23; success-path log line tracked as candidate follow-up.
- Data Source: ✅ READY — Phase 2 closed 2026-05-07; integrations + ad_accounts + campaign_metrics + sync_logs tables live; OAuth + Inngest sync infrastructure operational. Real platform data flows once user OAuths a platform (requires META_APP_ID/SECRET, GOOGLE_ADS_*, SHOPIFY_API_KEY/SECRET in env).
- Auth: ✅ FULLY WORKING — Clerk JWT verification + JIT auto-provisioning of org+user rows in `authMiddleware`. Initial credits JIT grant active per #21 Part C (`INITIAL_ORG_CREDITS_GRANT`, default 100).
- Automation Engine: ✅ ACTIVE — Phase 4 Part 2 closed 2026-05-07; automation_rules + automation_runs tables live; CRUD + manual-execute via `POST /api/v1/automation/rules/:id/execute`. AUTO-FIRING on AI decision stream ACTIVE (continuations #23 post-persist hook + #24 Path F category formalization, 2026-05-09): every persisted ai_decisions row evaluates against enabled org rules; matching rules (trigger_type === ai_decisions.category with two-stage shim fallback to result.category JSONB; confidence ≥ rule.min_confidence_threshold/100) auto-execute via canonical Phase 4 executeAction pipeline; full audit trail in automation_runs + decision_history; `GET /automation/runs` joins `ai_decisions(category)` for operator visibility.
- AI Output Contract Category: ✅ FORMALIZED — additive nullable column on `ai_decisions` + AI prompt emission + two-stage extractCategory shim; Path F #24, 2026-05-09. AI responses without category continue to work (no rule auto-fires; intentional safety property).
- Stripe Monetization: ✅ ACTIVE — inbound webhook with HMAC + timestamp tolerance + event-id dedup (#22); outbound Checkout + Customer Portal (#21); credits substrate + ledger + RPCs (#14/#18); BYOK key storage routes (#19); frontend BYOK + plan UI (#19). Phase 7 monetization end-to-end live.
- Per-org execution rate limit: ✅ ACTIVE — `ACTION_EXECUTION_MAX_PER_MINUTE` (default 60) gates `executeAction` on every non-replay invocation; idempotent replays not counted.
- Real Action Surface (live, behind flags + token + allowlist):
  - `meta.pause_campaign`
  - `meta.decrease_budget`
  - `meta.increase_budget` (with server-side max-percent cap)
  - `send_alert_email` (Resend; org-admin recipients only; placeholder filter)
  - `google.pause_campaign` (Phase 4 Part 2; per-org Vault refresh-token; OAuth refresh; customer_id from ad_accounts; behind GOOGLE_PAUSE_CAMPAIGN_LIVE flag + GOOGLE_LIVE_ORG_ALLOWLIST)
- Backend API: ✅ WORKING (Hono); `POST /api/v1/ai/execute`, `POST /api/v1/actions/:id/execute`, `GET /api/v1/integrations`, `POST /api/v1/integrations/connect/start`, `POST /api/v1/integrations/connect/complete`, `DELETE /api/v1/integrations/:id`, `POST /api/v1/integrations/:id/sync`, `GET /api/v1/integrations/:id/sync-logs`, `GET /api/v1/metrics/summary`, `GET /api/v1/metrics/channels`, `GET/POST/PATCH/DELETE /api/v1/automation/rules`, `POST /api/v1/automation/rules/:id/execute`, `GET /api/v1/automation/runs` all live
- org_id enforcement: ✅ middleware-level + RLS-level on every Phase 3/4 table
- Execution Layer: ✅ CLOSED — actions_library + decision_history; idempotent; impact_snapshot persisted; 7 real-mode handlers behind flags; structured `[exec]` audit logs
- Real Action Surface inventory (consolidated 2026-05-09 #25 — supersedes prior duplicated/stale Phase-4-Part-2-deferred block):
  - `meta.pause_campaign` ✅ (Phase 4 minimal; per-org Vault Meta token resolver since #21 Part B; sandbox fallback)
  - `meta.decrease_budget` ✅ (Phase 4 minimal; still single-tenant `META_TEST_ACCESS_TOKEN`; per-org Vault migration tracked as known follow-up)
  - `meta.increase_budget` ✅ (Phase 4 minimal; server-side `META_INCREASE_BUDGET_MAX_PERCENT` cap; still single-tenant; per-org Vault migration tracked)
  - `send_alert_email` ✅ (Phase 4 minimal; Resend; org-admin recipients only; placeholder filter)
  - `google.pause_campaign` ✅ (Phase 4 Part 2; per-org Vault refresh-token; OAuth refresh; customer_id from ad_accounts; behind `GOOGLE_PAUSE_CAMPAIGN_LIVE` + `GOOGLE_LIVE_ORG_ALLOWLIST`)
  - `meta.create_campaign` ✅ (Phase 6 Sub-pass C #20; per-org Vault Meta token via #21 Part B resolver; status='PAUSED' hard-coded for safety; behind `META_CREATE_CAMPAIGN_LIVE` + `META_LIVE_ORG_ALLOWLIST`)
  - `google.create_campaign` ✅ (Phase 6 Sub-pass C #20; two-step CampaignBudget+Campaign mutate; per-org Vault refresh-token; status='PAUSED' hard-coded; behind `GOOGLE_CREATE_CAMPAIGN_LIVE` + `GOOGLE_LIVE_ORG_ALLOWLIST`)

---

## CURRENT EXECUTION TARGET (STRICT)

All Phase 0 + Phase 1 + Phase 2 + Phase 3 (linear) + Phase 4 minimal + Phase 4 Part 2 + Phase 5 slices CLOSED (2026-05-07). Phase 6 Sub-A/B/C CLOSED (2026-05-08, continuations #11/#12/#13/#20). Phase 7 Sub-A0/A0.5/A1a/A1b/A1c/B/D + Stripe webhook hardening CLOSED (2026-05-08 → 2026-05-09, continuations #14 → #22). Phase 4/Phase 6 BRIDGE — auto-firing automation_rules on AI decision stream — CLOSED (2026-05-09, continuations #23 + #24 Path F). SYSTEM_CONTROL.md governance-metadata reconciliation CLOSED (2026-05-09, continuation #25). action-executor.ts pre-dispatch logging-mode label parity CLOSED (2026-05-09, continuations #26 + #27). Auto-fire success log line item P CLOSED + governance-meta sync (2026-05-12, continuation #28). Phase 3 anomaly engine Inngest-receiver guard added (2026-05-12, continuation #29) as defense-in-depth; continuation #30 (2026-05-12) corrected #29's claim about active 42P01 firing — actual registration array commented-out the receiver, so no production 42P01 was occurring; guard remains valid if receiver is ever re-registered. Plus inngest.ts comment-block phase-classification sync (Phase 2 + Phase 5 marked CLOSED instead of stale "deferred"). PHASE2_ENVELOPE_FOLLOWUP RESOLVED (2026-05-12, continuation #31) — backend canonicalization of integrations.ts/connect.ts/metrics.ts (16 sites) onto Phase 1 canonical envelope under ADJACENT CONTINUATION AUTHORITY; FE apiClient absorbed transparently. Post-#31 comment-only hygiene + HARDENED-BUT-UNUSED governance headers added (2026-05-12, continuation #32) — apiClient.ts stale "PHASE2_ENVELOPE_FOLLOWUP deferral state" comments updated; alerts.ts + decisions.ts received governance-header doc blocks preventing future maintainers from mistakenly canonicalizing 503-gated routes. Closed-loop UX-visibility chain (#24 backend visibility bridge → #33 frontend consumption) — `app/automation/history/page.tsx` now consumes the `ai_decisions(category)` JOIN that #24 added to `GET /automation/runs`; operators see categorical trigger labels (e.g., "ROAS_DROP · Rule abc12345…") alongside rule references in the history feed, completing the operator-visibility chain for the auto-fire loop end-to-end. Confidence display gap CLOSED (2026-05-12, continuation #34) — backend `GET /automation/runs` SELECT extended with `ai_decisions(confidence_score)` mirroring the #24 category JOIN pattern; FE history page consumes confidence with NUMERIC 0-1 → 0-100 percentage conversion; closes #22 mocked-deferred confidence preservation that explicitly required this JOIN. ApiError request_id surfacing CLOSED (2026-05-12, continuation #34) — FE `ApiError` class extended to capture canonical envelope `request_id`; operators can quote request_id when reporting errors to enable backend log pivot via the [req]/[err]/[exec]/[AI] correlator chain. UX-facing requestId display wired in `app/settings/billing/page.tsx` (continuation #35, 2026-05-12) via new `formatErrorMessage(err, fallback)` helper in `lib/api-client.ts` — converts ApiError-with-requestId into "(message) (request abc12345…)" display format; backwards-compat with non-ApiError exceptions; demonstrated in 4 catch sites in billing/page.tsx. **formatErrorMessage SIBLING ROLLOUT (continuation #36, 2026-05-12)** per CONTINUATION BATCHING RULE — adopted across all 7 remaining FE consumer files (12 catch sites total) covering `app/decisions/[id]/page.tsx` (1), `app/dashboard/overview/page.tsx` (2), `app/integrations/page.tsx` (3 — incl. preserving the special-case 409 conflict friendly text), `app/campaigns/[id]/page.tsx` (3), `app/campaigns/page.tsx` (1), `app/campaigns/create/page.tsx` (1), `app/automation/history/page.tsx` (1). 100% of FE consumer error displays now surface ApiError.requestId via uniform helper; operators can quote request_id from any FE error message app-wide. **Detailed Explanation panel WIRED (continuation #37, 2026-05-12)** — `GET /api/v1/automation/runs` SELECT extended with `ai_decisions(reasoning_steps)` mirroring #24 category + #34 confidence_score JOIN pattern; `app/automation/history/page.tsx` AI Decision Insights aside renders real reasoning_steps when active entry has linked ai_decision (mock fallback otherwise). Closes the last substantively-addressable item from the #22 history-page MOCKED-DEFERRED list; closed-loop product promise (Data → Insight → Decision → Action → Result → Learning visibility) now exposed end-to-end in operator UI: category + confidence + reasoning_steps + run status all surfaced. **Action-template visibility + AI-signal inline chips WIRED (continuation #38, 2026-05-12)** per CONTINUATION BATCHING RULE — sibling-batched with #37 on the same runtime surface (history page). `/automation/runs` SELECT extended with `actions_library(name, platform, action_type)` (the NOT NULL FK that was unconsumed); FE collapsed-row "Action Taken" column now shows actual action-template badge (was previously always undefined); expanded section gains an inline chip row showing Action + Category + Confidence signals beside the existing Trigger/Data/Result grid. Operators see the complete closed-loop chain inline on every row: Decision → Category → Confidence → Action → Result. **Post-audit batch WIRED (continuation #39, 2026-05-12)** — production-grade STRICT EXECUTION STATUS AUDIT completed (READ-only pass produced no implementation work — pure classification + status calibration: overall 64%, BE 84%, FE 33%, production readiness 62%, orchestration 68%; identified C1 [AI decision auto-trigger] as existential blocker; closed-loop core operational but operationally manual). Three audit-surfaced implementation-ready continuations executed: (i) `result_data` JSONB structured execution-provenance rendering in the history-page Result card (primitive top-level keys only — `mode`/`stage`/`http_status`/`token_source`/`idempotent_replay`/`original_history_id`; nested `body` filtered defensively; 80-char value truncation; 6-key cap); (ii) `action_type` technical-class label rendered on the action chip in monospace (only when distinct from friendly name); (iii) opportunistic comment-hygiene refresh on `inngest.ts` registration block (`Phase 2 ingestion deferred` → "Phase 2 UNLOCKED (#3); auto-sync registration pending operator decision (#30)") + governance classification comment added above `/decisions/*` + `/alerts/*` 503 gates documenting CANONICAL AI SYSTEM resolution. ZERO behavior change BE-side (comment-only); FE additive read-side enrichment. Active-surface backend runtime hardening SATURATED. SEO baseline allow-list and drift cleanup CLOSED. **Execution Log page WIRED (continuation #40, 2026-05-12)** under ADJACENT CONTINUATION AUTHORITY (operator-facing execution-visibility surface; backend canonical contract pre-existed; mocked-shell swap to real data with zero schema/backend change). `app/actions/logs/page.tsx` (CLAUDE.md routing-map "Execution Logs → decision history") swapped from MOCK_LOGS array to canonical `GET /api/v1/history` consumption — same Phase 1 envelope pattern as #13/#22/#33/#36. Maps `decision_history` rows (decision, action_taken, trigger_condition, result, ai_explanation, confidence_score, ai_decision_id, executed_by, created_at) onto the existing Stitch table+expanded-detail UI. Stitch Platform filter replaced with "Executed By" (All/Manual/Automation) backed by the canonical `?executed_by=` query param already validated server-side against the CHECK enum (history.ts:29,65). Status filter narrowed from {success,failed,running} to {success,failed,skipped} matching the schema CHECK constraint (no "running" — result is terminal per Phase 4 minimal migration line 85). KPI cards rebound: Total Executions = entries.length; Success Rate = derived from loaded rows; Automation vs Manual ratio = derived. Honest "—" placeholders replace fabricated "$X saved" / "X% latency" mock numbers where decision_history doesn't carry the field at list-row level (impact_snapshot is only on `/history/:id` detail route, not pre-loaded). Loading state + error display via formatErrorMessage (continuation #36 pattern). NOT on the four governance-deferred mocked-shell pages (#22 list) — `actions/logs/page.tsx` is a Phase 4 minimal/Part 2 surface, distinct from the `actions/page.tsx` + `actions/[id]/page.tsx` UX↔backend-mismatch governance-deferred pair. Closes another operator-facing execution-visibility gap (priority items #1 + #4 in IMPLEMENTATION PRIORITY SHIFT). Single-file diff: +237/-183 LOC; frontend tsc 0; HARD LOCK invariants preserved verbatim; no schema/migration/middleware/router-mount changes; no canonical-envelope drift. **Automation Status page WIRED (continuation #41, 2026-05-12)** under same ADJACENT CONTINUATION AUTHORITY — sibling pattern to #40 on the operator-cockpit axis. `app/actions/automation/page.tsx` (CLAUDE.md routing-map "Automation Status → decision center") swapped from `INITIAL_AUTOMATIONS` + `LIVE_EVENTS` mock arrays to canonical Phase 4 Part 2 endpoints: parallel fetch of `GET /api/v1/automation/rules` (rules CRUD list with `actions_library(platform, name)` JOIN already returned by automation.ts:57) and `GET /api/v1/automation/runs?limit=10` (recent runs with full JOIN chain ai_decisions(category, confidence_score, reasoning_steps) + automation_rules(name) + actions_library(name, platform, action_type) per #24/#34/#37/#38 layering). Rule cards render: trigger_type chip (ROAS_DROP/SPEND_SPIKE/CONVERSION_DROP/SCALING_OPPORTUNITY) + platform chip (from actions_library JOIN) + min_confidence_threshold + run_count + relative last_fired_at. **Per-rule enable/disable toggle WIRED via `PATCH /api/v1/automation/rules/:id` with `{enabled: !current}` body** — operator can now arm/disarm individual rules from the cockpit (priority items #1 + #3 + #5 — operator-facing workflow completion + orchestration usability + approval workflow maturity). Toggle uses optimistic-update pattern with rollback on PATCH failure (preserves dormant-by-default safety property: backend rejection snaps UI back to authoritative state). Live Activity feed renders recent runs with status dot + action name + rule name + category — full closed-loop signal chain visible at a glance. KPI cards rebound: Total Rules (rules.length), Recent Success Rate (derived from runs.length), Last Fire (max last_fired_at across rules, relative). Fabricated "$142.8k Revenue Impact" / "-18.2% CPA" / "1.2m Exec Freq" mock numbers replaced with honest derived rule/run stats. Removed UI surfaces with no backend mapping: Sensitivity Slider (not persisted server-side — would require speculative settings schema), `Pause All` / `Resume All` bulk buttons (disabled with explanatory title — bulk-mutation safety: explicit per-rule toggles prevent accidental disarming of the entire auto-fire stream). "Risk & Safety" card relabeled to reflect actual Phase 4 Part 2 guarantees (Per-rule Confidence Gate, Idempotent Execution, Per-org Rate Limit 60/min) instead of fabricated "Stop-loss / Rollback / Platform Alerts". NOT on the four governance-deferred mocked-shell pages (#22 list). Single-file diff: +281/-159 LOC across `app/actions/automation/page.tsx`; frontend tsc 0; HARD LOCK invariants preserved verbatim; no schema/migration/middleware/router-mount changes; no canonical-envelope drift; org-isolation preserved (PATCH passes through authMiddleware → orgId scope → `.eq('org_id', orgId)` in automation.ts:257). **Execution Log lazy detail panel WIRED (continuation #42, 2026-05-12)** — adjacent enrichment to #40 surfacing `data_used` + `impact_snapshot` JSONB fields via lazy-load of `GET /api/v1/history/:id` (the LIST endpoint intentionally omits these for performance per history.ts:85; DETAIL returns full row via `.select('*')` per history.ts:124). Operator visibility for the two CLAUDE.md §9 "Decision History — Special Note" core memory-table fields that LIST hides: `data_used` (data snapshot at decision time) and `impact_snapshot` (before/after state per Phase 4 Phases.md deliverable). One-shot fetch on first row expand; in-memory cache keyed by row id; loading + per-row error display via formatErrorMessage. Defensive primitive-entries renderer mirrors continuation #39's `result_data` filter pattern — only string/number/boolean top-level keys surfaced; arrays + nested objects elided; values truncated to 100 chars; 8-key cap per panel. trace_id rendered separately when present (Phase 0 [req]/[err]/[exec]/[AI] correlator pivot). Single-file diff in `app/actions/logs/page.tsx`; frontend tsc 0; HARD LOCK invariants preserved; no schema/backend/migration/middleware change; org-isolation preserved (auth middleware enforces orgId on `/history/:id` per history.ts:117,126). **Manual rule fire WIRED (continuation #43, 2026-05-12)** — adjacent to #41 on the same surface (`app/actions/automation/page.tsx`). Added "Run Now" button on each rule card wired to canonical `POST /api/v1/automation/rules/:id/execute` (the Phase 4 Part 2 manual-execution path per automation.ts:295 header). Empty body (per automation.ts:309 — empty body is a valid manual fire without ai_decision_id linkage). Goes through canonical executeAction pipeline: idempotency, parameter validation, per-org rate limit (60/min default), audit-row insertion in `decision_history` + `automation_runs`; real-mode handlers gated by LIVE flags + org allowlist (default OFF → simulated execution). Disabled rules: button is disabled with tooltip ("Enable rule to allow manual fire") — backend would 422 RULE_DISABLED anyway; client-side gate avoids the round-trip. On 202 success: success banner shows `rules_fired` count + run_ids; runs feed is best-effort refreshed so the new audit row appears in Live Activity; rule card `run_count` + `last_fired_at` optimistically bumped. Per-rule `firingId` state isolates loading UI so cross-rule operations don't block. Error display via formatErrorMessage (continuation #36 pattern; preserves request_id surfacing on 429 RATE_LIMITED / 404 / 500). Completes the operator-cockpit triad on the Automation Status page (priority #1 — operator-facing workflow completion): rule discovery (#41 GET) + rule arm/disarm (#41 PATCH) + manual fire (#43 POST). Single-file diff in `app/actions/automation/page.tsx`; frontend tsc 0; HARD LOCK invariants preserved; no schema/backend/migration change; org-isolation preserved (orgId scoped at executeRule per automation-engine.ts entry). **Integrations sync_logs visibility WIRED (continuation #44, 2026-05-12)** — `app/integrations/page.tsx` gained per-card expandable "Recent Syncs" panel lazy-loaded from canonical `GET /api/v1/integrations/:id/sync-logs?limit=10` (integrations.ts:112-145, returns sync_logs rows mapped to camelCase: id/startedAt/completedAt/status/recordsWritten/errorMessage). Closes a Phase 2 operator-cockpit gap (priority items #1 + #4 — operator-facing workflow completion + execution visibility): operators can now see per-integration sync run history (started_at, status, records_written) without leaving the Integrations page. Lazy-fetch on first expand; in-memory cache keyed by integration id; loading + per-card error display via formatErrorMessage; max-height-48 scrollable list capped at 10 rows. Status dot color reflects sync state: emerald for success/completed, amber-pulse for in_progress/pending, red for failed. Mirrors the lazy-load + cache + filter-defensive patterns from #42 (history detail) and #43 (rule fire). Single-file diff in `app/integrations/page.tsx`; frontend tsc 0; HARD LOCK invariants preserved; no schema/backend/migration change; org-isolation preserved (sync-logs endpoint pre-checks integration belongs to org per integrations.ts:120-126 before fetching sync_logs). **Cockpit cross-link + server-side rule filter WIRED (continuation #45, 2026-05-12)** — connects two already-wired operator cockpit surfaces with a single deep-link + server-side filter chain. Backend `?rule_id=<UUID>` filter on `/automation/runs` was already canonical (automation.ts:392 — UUID-validated) but not consumed; FE history page now reads `?rule_id=` from URL on mount, validates against `UUID_LIKE_RE` client-side (avoids 400 round-trip), and passes through to the runs LIST query as a server-side filter. Visible "Filtered: showing runs for rule abc12345…" chip with "Clear filter" button (history.replaceState to drop the URL param without router re-mount). `app/actions/automation/page.tsx` rule cards gained a "View Runs" link (Next.js `<Link>` to `/automation/history?rule_id=${rule.id}`) beside the existing #43 Run Now button. Operators can now navigate cockpit chain: rule discovery (#41) → rule arm/fire (#41/#43) → drill into rule-filtered run history (#45) — closes the operator-cockpit coherence loop (priority #1 + #8 — operator-facing workflow completion + discoverability/navigation coherence). Two-file diff: `app/automation/history/page.tsx` (state + URL param read + filter chip render + useEffect dep on ruleFilter) + `app/actions/automation/page.tsx` (Link import + "View Runs" button); frontend tsc 0; HARD LOCK invariants preserved; no schema/backend/migration change; org-isolation preserved (backend `.eq('org_id', orgId)` on automation.ts:437 unchanged). **Dashboard channels time-filter functional fix WIRED (continuation #46, 2026-05-12)** — `app/dashboard/channels/page.tsx` had a Stitch UX bug: "Last 30 Days / Quarterly / Year-to-Date" filter buttons changed styling only; the backend query hard-coded `defaultDateRange()` to a 30-day window regardless. Operators clicking "Quarterly" saw no data change. Fix: replaced `defaultDateRange()` with `dateRangeFor(filter)` that maps each filter to a concrete `{from,to}` ISO pair (30/90/YTD) and added `activeFilter` to the useEffect deps so refetch fires on filter change. Cancellation guard added to avoid stale-state writes on rapid filter clicks. Reset `loading` + `noData` at fetch start so visible empty-state doesn't briefly flash between switches. Operator-facing workflow fix (priority #1) — closes the silent UX-vs-data mismatch. Backend `/api/v1/metrics/channels` already accepts canonical `?from=&to=` params (metrics.ts) — no backend change required. Single-file diff in `app/dashboard/channels/page.tsx`; frontend tsc 0; HARD LOCK invariants preserved; no schema/backend/migration change. **Cockpit refresh + rule-name chip polish WIRED (continuation #47, 2026-05-12)** — three small polish items on the wired cockpit surfaces. (a) `app/automation/history/page.tsx` filter chip from #45 now surfaces the rule's friendly name (`entries[0]?.decision` — the joined `automation_rules.name` mapped in mapRunToEntry) instead of just the UUID-prefix when results are loaded; falls back to UUID prefix on empty. (b) Added explicit "Refresh" header button on `/automation/history` (RefreshCw icon, spinning while refreshing) that re-polls `/automation/runs` without a full route remount; useEffect runs the initial load, button calls the extracted `fetchRuns()` callback. Replaced fabricated "Export Log" mock button with a disabled-placeholder variant (export pipeline not in any current phase). (c) Symmetric "Refresh" button on `app/actions/automation/page.tsx` header that re-polls both rules + runs in parallel; same callback pattern, same icon + spinner. Closes the explicit-refresh gap on the two cockpit surfaces — operators no longer need to navigate-away/back to see new auto-fire entries or rule state churn. Priority items #1 + #3 + #4 + #10 (operator-facing workflow completion + orchestration usability + execution visibility + internal production polish). Two-file diff: `app/automation/history/page.tsx` (+refresh + rule name chip) + `app/actions/automation/page.tsx` (+refresh button); frontend tsc 0; HARD LOCK invariants preserved; no schema/backend/migration change. **Cockpit navigation + refresh extension WIRED (continuation #48, 2026-05-12)** — two small polish items completing the cockpit-coherence pass: (a) `app/actions/automation/page.tsx` Live Activity feed entries are now clickable Next.js `<Link>` wrappers — runs with `automation_rule_id` deep-link to `/automation/history?rule_id=${id}` (consumes #45 server-side filter), manual runs (no rule_id) link to unfiltered history. Hover surface added so clickability is visually obvious. Priority #8 — discoverability/navigation coherence. (b) `app/actions/logs/page.tsx` (Phase 4 execution log) gained a header "Refresh" button mirroring the #47 cockpit pattern — re-polls `/api/v1/history?limit=50` via extracted `fetchEntries` callback; spinning RefreshCw icon while refreshing. Replaced still-mocked "Export CSV" primary button with disabled-placeholder variant matching the #47 history-page treatment. Two-file diff; frontend tsc 0; HARD LOCK invariants preserved; no schema/backend/migration change; no canonical-envelope drift; org-isolation preserved (both endpoints scoped through authMiddleware → orgId). **Quick Stats derived metrics WIRED (continuation #49, 2026-05-12)** — `app/automation/history/page.tsx` Quick Stats aside was the last remaining hardcoded-mock block on the wired-history surface (line 18-23 page-header MOCKED-DEFERRED list). Resolved by deriving real metrics from `entries` (no new endpoint required — pure FE math on already-fetched data): "Total Runs" = entries.length; "Success Rate" = derived percentage with "—" placeholder when empty. Trend bar chart replaced hardcoded `[40,55,35,70,60,85,75,90,65,80,95,88]` with real per-day buckets across the last 12 days, computed in one pass over entries (raw `executedAt` ISO added to HistoryEntry shape since the existing `timestamp` field is locale-formatted and not parsable). Heights normalized to busiest bucket; `title` attribute exposes exact daily count on hover. Empty-state safe (maxBucket clamped to 1). Page-header MOCKED-DEFERRED list updated to mark Quick Stats + trend bar chart as RESOLVED via #49; only the Growth Suggestion card remains mocked (waiting on per-run recommendation endpoint that isn't in any current phase — appropriately deferred). Operator-honesty + derived-metric pattern matches #40/#41 KPI replacements. Single-file diff in `app/automation/history/page.tsx`; frontend tsc 0; HARD LOCK invariants preserved; no schema/backend/migration change; no canonical-envelope drift. **Topbar honesty pass WIRED (continuation #50, 2026-05-12)** — small operator-honesty cleanup on the shared `components/dashboard/Topbar.tsx`. (a) Removed the always-animating fake "unread notification" pulse dot on the Bell button — it implied a persistent unread state that never resolved (no notifications backend exists in any current phase). Button preserved as visual placeholder with "Notifications pending" tooltip + disabled state matching the disabled-placeholder pattern from #47/#48 (Export Log / Export CSV). (b) Fixed duplicate `'use client';` directive (line 1 + line 3 — pre-existing harmless typo). Operator-honesty pattern across cockpit: surfaces either real data or honest placeholders, never fabricated state. Priority items #1 + #10 (operator-facing workflow + internal production polish). Single-file diff in `components/dashboard/Topbar.tsx`; affects every page that mounts the dashboard layout but only visually (no behavior change beyond removing the dishonest pulse). **Continuation #51 (2026-05-12)** extends #50 with two more Topbar honesty items on the same component: (c) LayoutGrid "workspace switcher" button — never had a click handler, now disabled with "Workspace switcher pending" tooltip matching the Bell disabled-placeholder pattern. (d) Top-of-page search input — placeholder text was the original Stitch "Search Precision Matrix..." copy with no backend search endpoint; now disabled with honest "Search — pending" placeholder. Same single-file diff; same honesty pattern across the dashboard layout (real data OR honest disabled-placeholder, never fabricated affordance). Frontend tsc 0; HARD LOCK invariants preserved; no schema/backend/migration change. **Sidebar identity + brand alignment WIRED (continuation #52, 2026-05-12)** — same operator-honesty pattern extended into `components/dashboard/Sidebar.tsx`: (a) Bottom user pill swapped from hardcoded Stitch mock ("Alex Sterling / Director") to real Clerk identity via `useUser()` + `useOrganization()` — `user.fullName` / firstName / primaryEmailAddress fallback chain for the name; computed initials from firstName[0]+lastName[0] (email[0] fallback); organization.name as the subtitle replacing the fabricated "Director" role. Pill made inert (cursor-pointer removed; group hover removed) — Topbar's Clerk `<UserButton>` (AuthSection) remains the canonical interactive account surface; sidebar pill is decorative identity. (b) Brand label realigned from Stitch template residue ("Precision / Curator") to the actual product name per CLAUDE.md §1 ("GrowthHub — AI-powered Growth Operating System"); two-line treatment becomes "GrowthHub / Growth OS". Matches the `app/layout.tsx` root metadata refresh from the 2026-05-07 SEO baseline continuation. Loading state guard: when `useUser` hasn't resolved yet, displayName falls back to "…" and initials to "—" (no flash of incorrect data). Hits priority #1 + #8 + #10 (operator-facing workflow + discoverability/navigation coherence + production polish). Single-file diff in `components/dashboard/Sidebar.tsx`; frontend tsc 0; HARD LOCK invariants preserved; no schema/backend/migration change. **Dashboard discoverability fix WIRED (continuation #53, 2026-05-12)** — two coordinated changes that fix a navigation dead-end on the Dashboard surface. (a) Sidebar's `DASHBOARD_CHILDREN` was missing the canonical Overview entry — the dashboard landing page lives at `/dashboard/overview` per CLAUDE.md §5 routing map (also the Clerk `AFTER_SIGN_IN_URL` per §10 env vars), but operators clicking through Channels/Creative/Attribution/etc. had no sidebar path back to Overview. Added Overview at index 0 (matches routing map order). (b) Created `app/dashboard/page.tsx` server-side redirect to `/dashboard/overview` — previously bare `/dashboard` returned 404 because no root page existed under the directory; mirrors the existing `app/automation/page.tsx` redirect pattern. Both fixes hit priority #8 (discoverability/navigation coherence) and #1 (operator-facing workflow completion — Overview is the first thing operators see after sign-in and they need to navigate back to it). Two-file diff: `components/dashboard/Sidebar.tsx` (+Overview NavItem) + new `app/dashboard/page.tsx` (server-side redirect); frontend tsc 0; HARD LOCK invariants preserved; no schema/backend/migration change. **Sidebar Decision Center direct-link WIRED (continuation #54, 2026-05-12)** — `components/dashboard/Sidebar.tsx` AUTOMATION_CHILDREN had "Decision Center" pointing to `/automation` which then redirected to `/dashboard/automation/decision-center` via `app/automation/page.tsx`. The pathname-based active-state detection in NavGroup (line 142: `child.href === pathname`) never matched after the redirect because the post-redirect pathname (`/dashboard/automation/decision-center`) didn't equal the sidebar's stored href (`/automation`) — operators saw the page render but the sidebar entry never showed "active" highlight. Fixed by linking directly to the canonical destination, eliminating one redirect hop AND restoring active-state coherence. Priority #8 (discoverability/navigation coherence). Single-file diff in `components/dashboard/Sidebar.tsx`; frontend tsc 0; HARD LOCK invariants preserved; no schema/backend/migration change. **Campaigns "Apply Optimization" honesty pass WIRED (continuation #55, 2026-05-12)** — `app/campaigns/page.tsx` had a fake "Apply Optimization" button in the MOCKED-DEFERRED right-sidebar Recommendation card that used `setTimeout(1200)` to simulate a successful apply with no backend call — operators saw a fake "Applied!" success state for a budget-shift that never happened. Replaced with disabled-placeholder ("Bulk budget-shift pipeline pending") matching the established cockpit-wide honesty pattern from #47/#48/#50/#51. Orphaned state + handler also deleted (`applying` + `applied` useState + `handleApply` setTimeout simulator) per the CLAUDE.md "delete completely if certain unused" instruction. Single-file diff in `app/campaigns/page.tsx`; frontend tsc 0; HARD LOCK preserved; no schema/backend/migration change. **Campaigns Bulk Actions + Quick Actions honesty pass WIRED (continuation #56, 2026-05-12)** — extension of #55 covering the two remaining fake-action surfaces in `app/campaigns/page.tsx`: (a) Bulk Actions Bar (Pause / Activate / Duplicate / Increase Budget buttons that fired when campaigns were selected) had NO onClick handlers — clicking them did literally nothing. Replaced with disabled-placeholder treatment ("Bulk action pipeline pending" tooltip). The X clear-selection button kept its real handler (pure client-side state op). (b) Per-row Quick Actions in the expanded row (+10% Budget / +20% Budget / Pause / Duplicate) used `handleQuickAction` setTimeout simulator that briefly toggled the button to "Done!" — same fake-success pattern as #55. Replaced with disabled-placeholders ("Recommendations action pipeline pending"). Orphaned state + handler also deleted (`quickActions` useState + `handleQuickAction` setTimeout + `qa` local in row map) per CLAUDE.md "delete completely if certain unused". Both surfaces were already on the page's MOCKED-DEFERRED list (lines 242 + 423 — no bulk endpoint + anomaly-DEPRECATED recommendations layer). Single-file diff in `app/campaigns/page.tsx`; frontend tsc 0; HARD LOCK preserved; no schema/backend/migration change. **Campaigns detail fake-action honesty pass WIRED (continuation #57, 2026-05-12)** — extension of #55+#56 covering the two remaining fake-action surfaces in `app/campaigns/[id]/page.tsx`: (a) "Execute Action" buttons in the Direct Execution Layer section (4 mock action cards) used `handleExecute` setTimeout simulator that flashed "Executed!" with no real backend call (MOCKED-DEFERRED line 387 — recommendations layer not in any current phase). Replaced with disabled-placeholder. (b) Right-sidebar "APPLY ALL" link used `handleApplyAll` setTimeout simulator (MOCKED-DEFERRED line 522 — anomaly + recommendations + risk_scores). Replaced with disabled-placeholder. Orphaned state + handlers also deleted (`executing`/`executed` + `handleExecute` + `applyingAll`/`appliedAll` + `handleApplyAll`) per CLAUDE.md "delete completely if certain unused". The two REAL backend calls on this page (`handleAudit` → POST /:id/ai-suggestions and `handlePush` → POST /:id/push) are UNTOUCHED — already wired in prior Phase 6 continuations. Operator-honesty pattern now extends through every visible affordance on both campaigns list (#55/#56) AND campaigns detail (#57). Single-file diff in `app/campaigns/[id]/page.tsx`; frontend tsc 0; HARD LOCK preserved; no schema/backend/migration change. **Campaigns create fake-action honesty pass WIRED (continuation #58, 2026-05-12)** — completes the campaigns honesty pass with `app/campaigns/create/page.tsx`: (a) Footer "Launch Campaign" button used `handleLaunch` setTimeout fake simulator that flashed "Launched!" with no real push. Even though Phase 6 Sub-pass C closed the real-mode CREATE handlers (continuation #20), the proper Launch workflow is Save Draft → routed to `/campaigns/[id]` detail page → handlePush (POST /:id/push). A duplicate Launch affordance on the create page misled operators. Replaced with disabled-placeholder + tooltip pointing to the proper flow. (b) Sidebar "Apply AI Boost" button used `handleApplyBoost` setTimeout simulator that flashed "Applied!" — preview-estimation pipeline isn't in any current phase. Replaced with disabled-placeholder. Orphaned state + handlers deleted (`launching`/`launched` + `handleLaunch` + `applyingBoost`/`boostApplied` + `handleApplyBoost`) per CLAUDE.md "delete completely if certain unused". Page-header comment block updated to reflect post-Sub-pass-C state and the #58 honesty rationale. REAL backend call on this page (`handleSaveDraft` → POST /api/v1/campaigns) is UNTOUCHED. **Campaigns surface honesty pass now complete across all three routes**: list (#55/#56), detail (#57), create (#58). Single-file diff in `app/campaigns/create/page.tsx`; frontend tsc 0; HARD LOCK preserved; no schema/backend/migration change. **apiClient `error.code` honored WIRED (continuation #59, 2026-05-12)** — `lib/api-client.ts` had a documented-intent-vs-implementation gap: the backend `deferredPhase` 503 gate (`backend/src/routes/v1/index.ts:55`) explicitly states "frontend api-client uses presence of `error.code` to decide whether the server's message is intentionally caller-facing and should bypass the generic friendlyMessage mask", but the actual apiClient never checked the code field. Result: a 503 with `code: 'DEFERRED'` collapsed to "Server error — try again in a few moments" at the UI, hiding the actionable "Phase X disabled per SYSTEM_CONTROL.md" copy. Same problem for explicit `fail()` codes: INVALID_FILTER / INVALID_TYPE / MISSING_PARAMETER / RATE_LIMITED / NOT_FOUND / RULE_DISABLED / SYNC_IN_PROGRESS / CROSS_ACCOUNT_FORBIDDEN — all operator-actionable but masked by `friendlyMessage`. Fix: when `error.code` is present in the canonical envelope, pass the raw canonical message verbatim to the ApiError. 401 retains friendlyMessage treatment (canonical 401 messages tend to be technical like "missing bearer token" vs the user-friendly "Your session expired — please sign in again" fallback). `isCanonicalEnvelope` type guard extended to include the optional `code` field. Closes the long-tracked intent-vs-implementation gap and propagates actionable error copy to every consumer of formatErrorMessage (#36) across the FE — operator clarity for every actionable backend failure mode, not just 4xx-and-friendly. Single-file diff in `lib/api-client.ts`; frontend tsc 0; HARD LOCK invariants preserved; no schema/backend/migration change. **Integrations 409 special-case removed WIRED (continuation #60, 2026-05-12)** — `app/integrations/page.tsx` had a special-case branch in the sync error handler (added at #36 alongside the formatErrorMessage rollout) that hardcoded the 409 SYNC_IN_PROGRESS friendly text. With the #59 apiClient code-honor fix, formatErrorMessage now surfaces the canonical `'A sync is already in progress for this integration'` message (integrations.ts:100) directly — the FE-side workaround is redundant. Consolidated onto the uniform formatErrorMessage path that every other FE catch site uses (no remaining `err instanceof ApiError && err.status === ...` shape-discriminator branches in the wired surfaces). Cleanup-only; behavior preserved with a slightly more verbose canonical wording. Single-file diff in `app/integrations/page.tsx`; frontend tsc 0; HARD LOCK invariants preserved; no schema/backend/migration change. **Decisions detail loadError surface WIRED (continuation #61, 2026-05-12)** — `app/decisions/[id]/page.tsx` catch block collapsed every error case into a single misleading "Decision not found" message: `try { ... } catch { setNotFound(true) }` — swallowed 503 DEFERRED, 401 auth, 404 NOT_FOUND, and infrastructure failures equally. Operators landing on the page during the current Phase-3-deferred stage saw the wrong message. With #59 honoring `error.code`, the canonical envelope's actionable copy is now available; updated the catch to capture `formatErrorMessage(err)` into a new `loadError` state and display it in the fallback section (replacing the hardcoded "Decision not found" text with `loadError ?? "Decision not found."`). Now operators see: 503 DEFERRED → "/api/v1/decisions is intentionally disabled (Phase 3 anomaly engine ...) per SYSTEM_CONTROL.md"; 401 → "Your session expired — please sign in again"; 404 → canonical not-found; real network failure → "Connection failed". The genuine-not-found fallback string is preserved for the edge case where decision is null without an explicit error. Forward-compat: when Phase 3 anomaly engine unlocks and the route is reactivated, the page automatically transitions from "deferred" copy to real not-found / real data without further changes. Single-file diff in `app/decisions/[id]/page.tsx`; frontend tsc 0; HARD LOCK invariants preserved; no schema/backend/migration change. **Remaining swallowed-error catches surfaced WIRED (continuation #62, 2026-05-12)** — sibling continuation to #61 covering the two remaining error-swallowing catches in wired pages: (a) `app/dashboard/channels/page.tsx` collapsed every load error onto `setNoData(true)`, so operators saw "No channel data available — connect a platform" CTA even for auth/network/503/etc. failures. Added separate `loadError` state; renders a red-tone error panel above the "No data" empty-state, gated to only show when load succeeded with zero rows. (b) `app/integrations/page.tsx` `handleDisconnect` catch hardcoded "Failed to disconnect. Please try again." — same masking. Replaced with `formatErrorMessage`. Both surfaces now propagate the canonical `error.code`-discriminated message via #59 code-honor → operator clarity for every actionable backend failure mode on these wired surfaces. Two-file diff (`app/dashboard/channels/page.tsx` + `app/integrations/page.tsx`); frontend tsc 0; HARD LOCK invariants preserved; no schema/backend/migration change. **Error-handling chain (#59 → #60 → #61 → #62) now complete**: backend canonical codes flow verbatim through apiClient → formatErrorMessage → every FE catch site → operator-visible UI. No remaining swallow-as-generic patterns on the wired surfaces. **Campaigns detail post-push refresh WIRED (continuation #63, 2026-05-12)** — `app/campaigns/[id]/page.tsx` left the campaign status badge stale after a successful push: backend POST /:id/push transitions the row server-side (e.g. draft → active), but the FE only set `pushed=true` without refetching. Extracted the load logic into a reusable `refreshCampaign` callback (useEffect retains cancellation guard for unmount races); handlePush now fires-and-forgets a refresh after success. Best-effort: failure leaves existing campaign state in place rather than wiping it (the original push response is the authoritative signal; refresh failure shouldn't undo the success UI). Single-file diff in `app/campaigns/[id]/page.tsx`; frontend tsc 0; HARD LOCK invariants preserved; no schema/backend/migration change. Priority #1 + #4 (operator-facing workflow completion + execution visibility). **Integrations post-sync history refresh WIRED (continuation #64, 2026-05-12)** — adjacent to #44 + #63 on the same execution-visibility axis. The #44 Recent Syncs lazy panel cached the sync_logs response on first expand; subsequent clicks of "Sync Now" with the panel still open didn't refresh the panel even though a new `in_progress` sync_log row was inserted server-side. Operators had to manually toggle the panel closed/open to see the new entry. Fix: extended `fetchSyncHistory` with an optional `force` parameter that bypasses the cache-hit early return (default behavior preserved for the first-load case). `handleSync` now fires a force-refresh after successful sync-queue when `historyOpen.has(id)` — gated to avoid wasted fetches when the panel is closed. Single-file diff in `app/integrations/page.tsx`; frontend tsc 0; HARD LOCK invariants preserved; no schema/backend/migration change. Closes the last post-mutation refresh gap on the wired surfaces. **Campaigns list cockpit consistency WIRED (continuation #65, 2026-05-12)** — `app/campaigns/page.tsx` filter bar had two dead-state UI elements that violated the operator-honesty pattern: (a) Objective `<select>` with no onChange handler — campaigns table has no `objective` column per the create-page header comment ("Sections that remain UI-state only (deferred): Objective selector → no `campaigns.objective` column in spec"); (b) "Last 7d" Calendar button with no onClick — GET /campaigns has no date-range filter. Both disabled with explanatory tooltips matching the cockpit-wide honesty pattern from #47/#48/#50/#51/#55-58. Plus: added the missing header Refresh button (cockpit consistency with #47/#48 automation history + execution log + automation status). Extracted `fetchCampaigns` callback so the button can re-fire without useEffect dep churn; `showSpinner` opt-in keeps the existing filter-change refetch UX (uses the original loading state) while letting the explicit refresh button track its own spinner state. Single-file diff in `app/campaigns/page.tsx`; frontend tsc 0; HARD LOCK invariants preserved; no schema/backend/migration change. Priority #1 + #8 + #10 (operator-facing workflow + navigation coherence + production polish). **Cockpit refresh button coverage now uniform** across all wired LIST surfaces: campaigns (#65), execution log (#48), automation history (#47), automation status (#47), integrations (filter-change-driven; explicit not needed since no shared filter state). **Dashboard refresh button coverage WIRED (continuation #66, 2026-05-12)** — extended the cockpit Refresh pattern to the two dashboard landing pages. (a) `app/dashboard/overview/page.tsx` gained a small refresh strip at the top of the page that re-fires both `loadMetrics` + `loadCampaigns` callbacks. Placed OUTSIDE the dismissible AI Summary insight card so operators retain the affordance after dismissing the insight panel. (b) `app/dashboard/channels/page.tsx` gained a Refresh button beside the time-filter pill group. Extracted `refreshChannels` callback that tracks its own `refreshing` state separately from the initial-load `loading` flag — refresh doesn't flip the page back to loading skeleton; operators see prior data while refresh is in-flight. Both follow the established #47/#48/#65 RefreshCw + animate-spin + disabled-while-refreshing pattern. Two-file diff (`app/dashboard/overview/page.tsx` + `app/dashboard/channels/page.tsx`); frontend tsc 0; HARD LOCK invariants preserved; no schema/backend/migration change. **Cockpit refresh coverage now COMPLETE across every wired top-level surface**: dashboard overview (#66), dashboard channels (#66), campaigns list (#65), execution log (#48), automation history (#47), automation status (#47), integrations (filter+state-driven). **Settings billing + Integrations explicit refresh WIRED (continuations #67 + #68, 2026-05-12)** — closed the last two cockpit surfaces lacking explicit header refresh affordance. (#67) `app/settings/billing/page.tsx` header reshaped from single column to flex-justify-between layout; Refresh button on the right re-fires the existing `load()` callback (already invoked by BYOK save/delete flows). Uses the same loading flag the page already tracks — no new state added. (#68) `app/integrations/page.tsx` header gained an explicit Refresh button — pre-fix `fetchIntegrations` only auto-fired after sync queue (via the 3s setTimeout in handleSync), so operators wanting to manually re-poll connection state had to nudge a sync to force the refetch. Header reshaped to flex-justify-between to fit the button. Both follow the established RefreshCw + animate-spin + disabled-while-loading pattern. Two-file diff (`app/settings/billing/page.tsx` + `app/integrations/page.tsx`); frontend tsc 0; HARD LOCK invariants preserved; no schema/backend/migration change. **Cockpit refresh affordance now uniform across EVERY wired top-level operator surface**: dashboard overview (#66) + dashboard channels (#66) + campaigns list (#65) + campaigns detail (post-push #63) + execution log (#48) + automation history (#47) + automation status (#47) + integrations (#68) + settings billing (#67). Zero-mismatch operator-cockpit consistency achieved. **Campaign detail status badge honesty WIRED (continuation #69, 2026-05-12)** — `app/campaigns/[id]/page.tsx` header status badge was hardcoded `bg-emerald-100 text-emerald-700` regardless of actual `campaign.status` value. Paused, draft, completed, and archived campaigns all rendered with the same "active green" styling, misleading operators about live state at a glance. Replaced the hardcoded class with a status-discriminating `statusBadgeClass` (active→emerald, paused→amber, draft→primary, others→muted), mirroring the STATUS_STYLES pattern already established in `app/campaigns/page.tsx` (#13/#55). Single-file diff in `app/campaigns/[id]/page.tsx`; frontend tsc 0; HARD LOCK invariants preserved; no schema/backend/migration change. Priority #1 + #10 (operator-facing workflow + production polish). **Campaigns Platform filter backend-alignment WIRED (continuation #70, 2026-05-12)** — `app/campaigns/page.tsx` Platform filter dropdown listed "All / Meta / Google / TikTok" but the FE if-chain (useEffect line 124-128) had no TikTok→backend-param branch, AND the backend `VALID_PLATFORMS = new Set(['meta', 'google'])` per campaigns.ts:26 explicitly rejects 'tiktok' with INVALID_FILTER. Net effect: selecting TikTok silently fell through to no platform filter sent — operators saw ALL campaigns rendered, misinterpreting it as "TikTok only" results. Fixed by removing TikTok from the dropdown options AND narrowing the `PlatformFilter` type to `"All" | "Meta" | "Google"` so the type system prevents future drift. Matches the campaigns/create page comment ("Select Meta or Google as a platform (TikTok / Snapchat support is coming soon)"). The mocked-deferred ALLOCATION sidebar chart still labels TikTok as a placeholder slice — left untouched since that's purely decorative MOCKED-DEFERRED content. Single-file diff in `app/campaigns/page.tsx`; frontend tsc 0; HARD LOCK invariants preserved; no schema/backend/migration change. **Campaigns create platform "Soon" badge WIRED (continuation #71, 2026-05-12)** — sibling to #70 on the same backend-alignment axis. `app/campaigns/create/page.tsx` Step 2 Platform Selection grid surfaced Meta/Google/TikTok/Snapchat as equally-valid checkboxes; only after Save Draft did operators discover via friendly-error that TikTok/Snapchat aren't backend-supported yet. Added a "Soon" amber badge next to TikTok/Snapchat labels to surface the constraint upfront. Checkboxes remain toggleable (forward-compat preserved — when those platforms unlock, the badge gets removed and the existing supportedPlatform check logic just expands). Single-file diff in `app/campaigns/create/page.tsx`; frontend tsc 0; HARD LOCK invariants preserved; no schema/backend/migration change. Priority #1 + #10 (operator-facing workflow + production polish). **Campaigns create Save Draft proactive validation WIRED (continuation #72, 2026-05-12)** — sibling to #70/#71 on the backend-alignment axis. `app/campaigns/create/page.tsx` footer Save Draft button was always clickable; operators with empty campaign name OR only TikTok/Snapchat selected saw the canonical error message only AFTER clicking. Added a proactive client-side predicate (`hasName && hasSupportedPlatform`) that disables the button upfront with explanatory tooltip ("Enter a campaign name first" / "Select Meta or Google as a platform" / "Save campaign as draft"). Server-side validation in `handleSaveDraft` (line 107-124) remains the authoritative gate — defense-in-depth never trusts the client. Single-file diff in `app/campaigns/create/page.tsx`; frontend tsc 0; HARD LOCK invariants preserved; no schema/backend/migration change. **Backend `total` count surfacing WIRED (continuation #73, 2026-05-12)** — both `app/automation/history/page.tsx` Quick Stats and `app/actions/logs/page.tsx` Total Executions KPI were showing `entries.length` (capped at the limit=50 fetch page) as if it were the org's full count. For organizations with 100+ runs/decisions, this misrepresented the system memory — operators saw "Total Runs: 50" when the actual count was higher. Both API responses already carry `total: number` (count: 'exact' from PostgREST per automation.ts:434 + history.ts:85); FE pages were just dropping it. Fix: capture `data.total` in a new `totalRunsBackend` / `totalBackend` state; render with honest "(showing 50)" suffix when total > loaded. Two-file diff (`app/automation/history/page.tsx` + `app/actions/logs/page.tsx`); frontend tsc 0; HARD LOCK invariants preserved; no schema/backend/migration change. Priority #4 + #7 (execution visibility + confidence/risk visibility — operators see the true system memory size). **Campaigns list backend `total` indicator WIRED (continuation #74, 2026-05-12)** — sibling to #73 extending the truthful-count pattern to `app/campaigns/page.tsx`. Pre-fix the campaigns list rendered only the loaded page (default limit=50 per campaigns.ts:55) with no indication that more campaigns existed beyond the page. For orgs with 50+ campaigns the list was a silent subset. Captured `data.total` in a new `totalBackend` state and rendered a "Showing N of M campaigns" line above the bulk actions bar when `totalBackend > campaigns.length`. The line only shows when more rows exist (avoids cluttering small lists). Single-file diff in `app/campaigns/page.tsx`; frontend tsc 0; HARD LOCK invariants preserved; no schema/backend/migration change. **Backend `total` count truthfulness now uniform across LIST surfaces**: automation history (#73), execution log (#73), campaigns list (#74). The remaining wired LISTs (automation rules on automation-status page; integrations) intentionally don't paginate at typical org scale — rules typically <20, integrations capped at 3 platforms. **Campaigns detail trend chart "Revenue" tab removed WIRED (continuation #75, 2026-05-12)** — `app/campaigns/[id]/page.tsx` Trend Analysis chart offered Spend / Revenue / ROAS toggles, but backend `trend_14d` returns only `date / spend / roas` per `services/campaigns/campaigns.ts:277-281` (vals.revenue is computed internally but dropped from the emitted shape). Pre-fix the FE's `revenueH` was assigned the same value as `spendH` (line 258 of the page) — operators clicking the Revenue tab saw spend data displayed under "Revenue" label, a quiet honesty break. Removed the Revenue option from the tab list, narrowed `TrendMetric` type union to `"spend" | "roas"`, dropped `revenueH` from `trendBars` shape, simplified `barKey` discriminator. When backend extends `trend_14d` to include revenue, just re-add `"revenue"` to the type + tab list and add `revenueH: Math.round((d.revenue / maxRevenue) * 100)` to the mapper. Single-file diff in `app/campaigns/[id]/page.tsx`; frontend tsc 0; HARD LOCK invariants preserved; no schema/backend/migration change. Priority #1 + #10 (operator-facing workflow + production polish). **Channel Correlation chart honesty marker WIRED (continuation #76, 2026-05-12)** — `app/dashboard/channels/page.tsx` Channel Correlation chart presented `CHART_COLUMNS` hardcoded mock data as "Daily performance fluctuations by acquisition source." But backend `/metrics/channels` returns aggregate snapshot per platform (current window), not daily time-series — no `/metrics/channels/timeseries` endpoint exists. Added a "Sample" amber badge next to the title + rewrote the subtitle to honestly state "Daily time-series view pending — see real current-window ROAS comparisons in the channel cards above" (pointing operators at the real-data section directly above). Reduced opacity to 50% to visually de-emphasize and removed hover/cursor-pointer affordance (mocked bars aren't actionable). Same honest-placeholder pattern as #50/#51/#55-58/#71. Single-file diff in `app/dashboard/channels/page.tsx`; frontend tsc 0; HARD LOCK invariants preserved; no schema/backend/migration change. **Dashboard overview Revenue vs Spend chart honesty WIRED (continuation #77, 2026-05-12)** — `app/dashboard/overview/page.tsx` chart had two honesty issues. (a) Title said "Revenue vs Spend **Trend**" implying time-series, but the chart is per-campaign comparison (no time-series endpoint exists for this view). Renamed to "Revenue vs Spend by Campaign". (b) Empty-state fell back to fake Mon-Sun hardcoded bars that looked plausibly real to operators; replaced with an honest empty-state ("No campaign data yet. Create a campaign →" link to /campaigns/create) when `campaigns.length === 0`. Real-data path preserved (per-campaign revenue/spend, normalized to max). Campaign label slice widened from 3 to 4 chars to reduce the "looks like day names" misread on real data. Single-file diff in `app/dashboard/overview/page.tsx`; frontend tsc 0; HARD LOCK invariants preserved; no schema/backend/migration change. Priority #1 + #10. **Campaigns detail Active Ad Sets sample marker WIRED (continuation #78, 2026-05-12)** — `app/campaigns/[id]/page.tsx` Active Ad Sets section rendered hardcoded `AD_SETS` mock data ("USA_Broad_SummerVibe", "UK_Interests_Fashion_Lover") without any honesty indicator. Section is documented MOCKED-DEFERRED at line 491 (adsets table not deployed in any current phase); same pattern as #76 applied: added "Sample" amber badge next to the section heading + subtitle ("Ad set tracking pending — schema not yet deployed") pointing operators to the deferred state explicitly. Reduced tbody opacity to 60% to visually de-emphasize the mocked rows. Single-file diff in `app/campaigns/[id]/page.tsx`; frontend tsc 0; HARD LOCK invariants preserved; no schema/backend/migration change. Priority #1 + #10. **Campaigns detail Direct Execution + AI Insight sample markers WIRED (continuation #79, 2026-05-12)** — extends the #76/#78 Sample-marker pattern to two more MOCKED-DEFERRED blocks in `app/campaigns/[id]/page.tsx`. (a) "Direct Execution Layer" section header — already had disabled buttons (#57), now has "Sample" badge + "Recommendations layer pending" subtitle on the header bar; grid wrapper at opacity-60. (b) Right-sidebar "AI Strategic Insight" card displayed hardcoded copy ("12% drop in 48h", "+24% CPA") that looked like real anomaly findings — operators viewing the detail page could misread these as live alerts. Added "Sample" badge to header + explicit "Illustrative — anomaly engine pending Phase 3 unlock" footer line; entire right sidebar wrapped at opacity-70. Pattern now consistent: every MOCKED-DEFERRED block on wired pages carries visible Sample marker + clear "pending" subtitle + opacity de-emphasis. Single-file diff in `app/campaigns/[id]/page.tsx`; frontend tsc 0; HARD LOCK invariants preserved; no schema/backend/migration change. **Campaigns list expanded-row + right-sidebar sample markers WIRED (continuation #80, 2026-05-12)** — final extension of the #76/#78/#79 honesty pattern, now covering the remaining MOCKED-DEFERRED blocks in `app/campaigns/page.tsx`. (a) Per-row expanded sub-blocks (Top Ad Sets / Top Creatives / AI Insight) showed hardcoded mocks ("USA_Broad_SummerVibe" ad sets, creative gradient placeholders, "ROAS decreased 12% in 24h" fake alert) wrapped at full opacity. Added a top-strip "Sample" banner inside the expanded row reading "Expanded-row content is illustrative — ad sets table, creatives cross-join, and AI insight pending future phases" + opacity-60 wrapper on the three sub-block grid. (b) Right-sidebar Account Snapshot KPIs ("$74,209.50" / "$284,192.10" / "3.83x +4.2%") + Recommendation card + Platform Allocation chart were hardcoded fabricated org metrics. Added "Sample" header strip on the sidebar wrapper + opacity-70 to visually distinguish from the real-data table. Single-file diff in `app/campaigns/page.tsx`; frontend tsc 0; HARD LOCK invariants preserved; no schema/backend/migration change. **Operator-honesty Sample-marker coverage now COMPLETE across every MOCKED-DEFERRED block on every wired page**: dashboard channels correlation chart (#76), campaigns/[id] Active Ad Sets (#78), campaigns/[id] Direct Execution + AI Strategic Insight (#79), campaigns list expanded rows + right sidebar (#80). No mocked data block on the wired cockpit is presented as authoritative anymore. **Settings billing Sample markers WIRED (continuation #81, 2026-05-12)** — extends the #76/#78/#79/#80 honesty pattern to the three remaining MOCKED-DEFERRED blocks on `app/settings/billing/page.tsx`. (a) "System Utilization" card displayed fabricated "$320 / $500 / +34% vs last month" with no monthly-spend aggregation endpoint — Sample badge + opacity-70. (b) "Payment Method" credit-card visualization showed hardcoded "Alexander Wright VISA 4242" data; "Change" button had no Stripe payment-method API to call — Sample badge + opacity-70 + Change button disabled with "Payment-method management pending" tooltip. (c) "Billing History" table rendered fabricated invoice rows with no invoices endpoint; Filter icon button had no handler — Sample badge + opacity-70 + Filter button disabled. Single-file diff in `app/settings/billing/page.tsx`; frontend tsc 0; HARD LOCK invariants preserved; no schema/backend/migration change. **Operator-honesty Sample-marker coverage now COMPREHENSIVE across ALL wired pages**: dashboard channels (#76), campaigns/[id] (#78/#79), campaigns list (#80), settings billing (#81). Every MOCKED-DEFERRED block on every wired page carries visible Sample marker + clear "pending" subtitle + opacity de-emphasis + disabled-state on any associated fake-action buttons. **Settings billing residual Sample markers WIRED (continuation #82, 2026-05-12)** — extends #81 to cover the last three remaining MOCKED-DEFERRED blocks in `app/settings/billing/page.tsx`. (a) "Plan Features" hardcoded list ("Unlimited Executions / Priority API Access / Dedicated Support") had no plan-catalogue endpoint backing — inline Sample marker added above the feature grid + opacity-70 + hover removed from feature pills. (b) "View Details" button beside Upgrade Plan had no onClick — disabled with "Plan details pending" tooltip. (c) "Annual Billing Banner" gradient card with "Switch & Save" CTA fabricated annual-plan availability — Sample badge on the title + opacity-70 + Switch & Save button disabled with "Annual plan pending" tooltip + subtitle copy rewritten to honestly say "Annual plan pending — monthly plans only via current Stripe checkout." Single-file diff in `app/settings/billing/page.tsx`; frontend tsc 0; HARD LOCK invariants preserved; no schema/backend/migration change. **Operator-honesty Sample-marker coverage is now FULLY SATURATED**: every fabricated-content block on every wired page carries the badge + opacity de-emphasis + associated button disablement. Nothing on the wired cockpit can be misread as live data. **Campaigns create Creative Selection sample marker WIRED (continuation #83, 2026-05-12)** — final residual MOCKED-DEFERRED block on the wired campaigns flow. `app/campaigns/create/page.tsx` Step 5 "Creative Selection" displayed 3 hardcoded gradient placeholder tiles with copy-only labels ("Summer Flash Sale", "Brand Awareness Reel", "New Arrivals Showcase"); operators could select one but `selectedCreative` is never included in the handleSaveDraft `targeting` payload (cross-Phase-5 surface deferred per the page header comment). Added Sample badge to the step header + opacity-70 wrapper on the tile grid + disabled the non-functional "Generate New AI Creative" CTA with explanatory tooltip ("Creative generation from campaign-create flow pending — use /creatives generator directly"). Tile selection itself remains interactive for visual feedback but doesn't claim to persist. Single-file diff in `app/campaigns/create/page.tsx`; frontend tsc 0; HARD LOCK invariants preserved; no schema/backend/migration change. **Dashboard overview status dot colors aligned WIRED (continuation #84, 2026-05-12)** — `app/dashboard/overview/page.tsx` STATUS_DOT map had paused/draft both rendering as the same muted grey, hiding the distinction between operator-halted (paused) and not-yet-pushed (draft). Aligned with the discriminating scheme established in campaigns/[id] status badge (#69): active→emerald-pulse, paused→amber, draft→primary, completed→blue (added), archived→muted. Single-file diff in `app/dashboard/overview/page.tsx`; frontend tsc 0; HARD LOCK invariants preserved; no schema/backend/migration change. Priority #1 + #10. **Campaigns list STATUS_STYLES alignment WIRED (continuation #85, 2026-05-12)** — completes the cross-page status-color alignment started at #69 + #84. `app/campaigns/page.tsx` STATUS_STYLES used a divergent palette (Active=green, Learning=yellow, Paused=muted) that didn't match the detail-page badge (emerald/amber/primary). Operators switching between list ↔ detail saw the same campaign change colors. Aligned to the canonical scheme: Active→emerald, Learning(=draft)→primary, Paused→amber. **Status visualization is now fully consistent across all three campaign-rendering surfaces**: dashboard overview campaigns table (#84), campaigns list (#85), campaigns detail header (#69). Single-file diff in `app/campaigns/page.tsx`; frontend tsc 0; HARD LOCK invariants preserved; no schema/backend/migration change. **Automation root redirect retargeted WIRED (continuation #86, 2026-05-12)** — `app/automation/page.tsx` redirected `/automation` to `/dashboard/automation/decision-center`, a mocked-shell page that expects a non-existent `/api/v1/automation/decision-center` endpoint. Operators typing the URL directly landed on a mocked dashboard misrepresenting the system. Retargeted redirect to `/automation/history` (real wired surface backed by Phase 4 Part 2 `GET /api/v1/automation/runs`). The Sidebar's "Decision Center" child link (#54) still points directly at the Stitch decision-center page for operators who want to see the mocked visualization layout — both paths now have honest destinations. Single-file diff in `app/automation/page.tsx`; frontend tsc 0; HARD LOCK invariants preserved; no schema/backend/migration change. Priority #1 + #8. **Campaigns detail Export + Duplicate buttons disabled WIRED (continuation #87, 2026-05-12)** — `app/campaigns/[id]/page.tsx` topbar Export + Duplicate buttons had no onClick handlers (no campaign-export endpoint; no duplicate endpoint). Replaced with disabled-placeholder treatment matching the cockpit-wide honesty pattern from #47/#48/#50/#51/#55-58. Single-file diff in `app/campaigns/[id]/page.tsx`; frontend tsc 0; HARD LOCK invariants preserved; no schema/backend/migration change. Priority #1 + #10. **Dashboard channels "Filter by Status" button disabled WIRED (continuation #88, 2026-05-12)** — `app/dashboard/channels/page.tsx` Data Breakdown header had a "Filter by Status" button with no onClick handler; per-channel status filtering isn't a supported backend operation (status is derived from connection state per-platform, not per-row filterable). Replaced with disabled-placeholder + "Per-channel status filter pending" tooltip. Single-file diff in `app/dashboard/channels/page.tsx`; frontend tsc 0; HARD LOCK invariants preserved; no schema/backend/migration change. **Automation history Growth Suggestion sample marker WIRED (continuation #89, 2026-05-12)** — `app/automation/history/page.tsx` AI Decision Insights aside contained a "Growth Suggestion" card explicitly listed as remaining MOCKED-DEFERRED in the page header comment (no per-run recommendation endpoint; AI Output Contract is reasoning, not prescriptive next-action). The card displayed fabricated suggestion text ("Lower threshold to 2.5 to capture higher volume during the current seasonal upswing") with a fully clickable "Apply Adjustment" button having no onClick handler. Added Sample badge next to the "Growth Suggestion" label + opacity-70 wrapper + disabled the Apply Adjustment button with "Recommendation apply pipeline pending" tooltip. Single-file diff in `app/automation/history/page.tsx`; frontend tsc 0; HARD LOCK invariants preserved; no schema/backend/migration change. **Dashboard overview data-freshness indicator WIRED (continuation #90, 2026-05-12)** — operators clicking Refresh (#66) had no visual signal of how stale the displayed metrics were. Added `lastUpdatedAt` state set after successful `loadMetrics`; renders as "Updated Xm ago" text beside the Refresh button. A 30s setInterval re-ticks `nowTick` so the relative-time display refreshes while the page stays open (e.g. "just now" → "1m ago" → "5m ago"). `relUpdated()` follows the cockpit-wide pattern from `app/actions/automation/page.tsx`/`automation/history` (just now / Nm ago / Nh ago / Nd ago). Single-file diff in `app/dashboard/overview/page.tsx`; frontend tsc 0; HARD LOCK invariants preserved; no schema/backend/migration change. Priority #4 + #10 (execution visibility + production polish). **Automation history data-freshness indicator WIRED (continuation #91, 2026-05-12)** — extension of #90 to `app/automation/history/page.tsx`, the CLAUDE.md §9 "system memory" surface where freshness signal is especially important. Operators reviewing past run audit history need to know if displayed entries are current or stale. Same pattern: `lastUpdatedAt` state set after both initial-load useEffect AND refresh `fetchRuns` callback; 30s setInterval re-ticks `nowTick` for live relative-time display; "Updated X ago" renders beside the Refresh button in the page header. Identical `relUpdated()` formatting (just now / Nm / Nh / Nd ago) to #90 — copy-paste rather than shared hook since hook-extraction is more invasive than this minimal-diff per-page application. Future extension to remaining 7 surfaces (execution log / automation status / campaigns list / integrations / settings billing / dashboard channels) can apply the same pattern as needed; for now, the two most-data-volatile surfaces (dashboard overview metrics + automation history runs) have the indicator. Single-file diff in `app/automation/history/page.tsx`; frontend tsc 0; HARD LOCK invariants preserved; no schema/backend/migration change. **Automation status data-freshness indicator WIRED (continuation #92, 2026-05-12)** — extension of #90/#91 to `app/actions/automation/page.tsx`, the third volatility-sensitive cockpit surface. Auto-fires on the AI decision stream can happen between explicit operator refreshes; the "Updated X ago" signal lets operators judge whether the visible rule state + recent-runs feed is current. Same inline pattern: `lastUpdatedAt` set in both initial-load useEffect AND refreshAll() callback; 30s setInterval re-ticks; renders beside Refresh button. Single-file diff in `app/actions/automation/page.tsx`; frontend tsc 0; HARD LOCK invariants preserved; no schema/backend/migration change. **Data-freshness indicator now covers the three most-volatile cockpit surfaces**: dashboard overview metrics (#90), automation history audit log (#91), automation status live rule state (#92). **Execution log data-freshness indicator WIRED (continuation #93, 2026-05-12)** — extension of #90/#91/#92 to `app/actions/logs/page.tsx`, the decision_history append-only audit feed. Every execution adds rows to the table server-side; freshness signal helps operators judge whether the visible page reflects recent activity or a stale snapshot. Same inline pattern: `lastUpdatedAt` set in both initial-load useEffect AND fetchEntries() callback; 30s setInterval re-ticks `nowTick`; "Updated X ago" renders beside Refresh button. Single-file diff in `app/actions/logs/page.tsx`; frontend tsc 0; HARD LOCK invariants preserved; no schema/backend/migration change. **Data-freshness indicator now covers all four high-volatility cockpit surfaces**: dashboard overview metrics (#90), automation history audit log (#91), automation status live rule state (#92), execution log decision_history feed (#93). **Integrations data-freshness indicator WIRED (continuation #94, 2026-05-12)** — extension of the #90/#91/#92/#93 pattern to `app/integrations/page.tsx`, the fifth volatility-sensitive cockpit surface. Sync state changes asynchronously when Inngest background jobs complete (per-integration `lastSyncedAt` + `status` fields are updated from the worker without an FE event); freshness signal helps operators judge whether a recently-queued sync has reflected yet, especially after the implicit 3s setTimeout refetch in handleSync. Same inline pattern: `lastUpdatedAt` set in the `fetchIntegrations` useCallback after successful fetch; 30s setInterval re-ticks; "Updated X ago" renders beside the Refresh button. Single-file diff in `app/integrations/page.tsx`; frontend tsc 0; HARD LOCK invariants preserved; no schema/backend/migration change. **Five wired surfaces now carry the data-freshness indicator**: dashboard overview, automation history, automation status, execution log, integrations. **AUTH MIDDLEWARE CATCH-SCOPE ISOLATION WIRED (continuation #98, 2026-05-12)** — runtime safety hardening per operator priority shift away from cosmetic polish. `backend/src/middleware/auth.ts` had a single `try { ... } catch (err) → return 401 UNAUTHORIZED` wrapping BOTH `verifyToken` (which legitimately throws on bad/expired/wrong-key JWTs) AND the entire JIT auto-provision flow (org-upsert + initial-grant RPC + user-lookup + user-insert/update). Supabase-js calls normally use `{data, error}` (handled explicitly via `if (error) return fail(...)`) — but low-level fetch/DNS/connection-pool failures, unexpected `.then` callback exceptions, and any future programming error inside the try block DO throw and were silently rebranded as "Missing or invalid authentication token". Misclassified failure modes pre-fix: Supabase DNS blip during JIT org-upsert → operator chases 401 retries instead of the real DB outage; programming error in `payload.o.x.y` access → 401 hides the real exception from Sentry/errorHandler; grant_credits RPC throwing synchronously inside `.then` rejection handler → 401 instead of sanitized 500 + request_id correlation. Fix: narrowed the try-catch to `verifyToken` ONLY (`let payload: ClerkSessionPayload; try { payload = ... } catch { return 401 }`). The JIT logic runs OUTSIDE the catch — any throw now bubbles to Hono's `onError` → errorHandler → sanitized 500 with request_id (CONSTITUTION §3 "Fail Loudly" + Phase 0 correlator-chain invariant). The supabase `{data, error}` `return fail(...)` branches are preserved verbatim — they remain the primary failure path for expected DB errors (RLS denials, unique-violation races, schema drift); the catch-scope split only protects against unexpected throws. Single-file diff in `backend/src/middleware/auth.ts`; backend tsc 0 (only pre-existing env-missing-module errors remain — same as session start); HARD LOCK invariants preserved; canonical envelope unchanged; org_id enforcement unchanged; no schema/migration. Priority items addressed: #1 "auth middleware catch-scope isolation" of the safety-enforcement override. **APPROVAL-PATH AUTO-FIRE GATE WIRED (continuation #99, 2026-05-12)** — runtime safety hardening per PRIORITY SAFETY ENFORCEMENT OVERRIDE item #2 (authoritative approval-policy enforcement) AND item #3 (approval/auto-fire execution coordination). The AUTHORITATIVE GOVERNANCE REQUIREMENT states: "Spend-increasing or launch-capable actions MUST NOT auto-fire without approval-path enforcement." Pre-fix: `services/execution/automation-engine.ts:evaluateRulesForAIDecision` (the auto-fire entry point per #23 post-persist hook) called `fireRule` → `executeAction` directly for EVERY matched rule, including those pointing at `meta.increase_budget` (spend-increasing) or `meta.create_campaign` / `google.create_campaign` (launch-capable). Auto-fired escalations could materially impact account spend without any operator gate. Fix: added centralized `SPEND_INCREASING_OR_LAUNCH_ACTION_TYPES` policy constant (set of 3 action_types — single source of truth per the override's "small centralized execution-policy enforcement over distributed conditional logic" rule); extended the rules SELECT in `evaluateRulesForAIDecision` with `actions_library(action_type)` PostgREST nested-select (matches the #38 pattern; non-null FK guarantees join row presence); added gate inside the rule-match loop that SKIPS any rule whose joined action_type is in the restricted set BEFORE inserting automation_runs and BEFORE calling executeAction. Skip logs to stdout as `[automation] auto_fire_blocked org_id=… rule_id=… action_type=… ai_decision_id=… reason=approval_required` — operators can grep this for any blocked auto-fire. **Manual fire path via `executeRule` (POST /automation/rules/:id/execute) is INTENTIONALLY unaffected** — operator triggering is the implicit approval. Architecture impact: ZERO new schema; ZERO new tables; ZERO migration; preserves canonical contracts (automation_runs shape unchanged, executeAction signature unchanged, rules CRUD unchanged); preserves single-writer (executeAction remains sole writer to decision_history); preserves org_id isolation; preserves dormant-by-default safety (rules pointing at restricted templates now harder, not easier, to fire). Future migration: when an `actions_library.requires_approval` column lands, this constant migrates to a runtime DB check; the gate position stays identical (only source of truth changes). Single-file diff in `backend/src/services/execution/automation-engine.ts`; backend tsc 0 (pre-existing env-missing-module errors only); HARD LOCK invariants preserved verbatim; no schema/migration. Priority items addressed: #2 + #3 of the safety-enforcement override. **AUTO-FIRE DISPATCH DEDUPE GATE WIRED (continuation #100, 2026-05-12)** — runtime safety hardening per PRIORITY SAFETY ENFORCEMENT OVERRIDE item #4 (approval dispatch idempotency/dedupe). Pre-fix: if `evaluateRulesForAIDecision` was called twice for the same `aiDecisionId` (Inngest retry of the post-persist hook, concurrent webhook delivery, manual re-trigger for testing), each call would INSERT a fresh `automation_runs` row and dispatch a fresh `executeAction` call — duplicating the side effect on the underlying ad platform. The existing `decision_history (org_id, execution_id)` partial unique index protects decision_history duplicates IF the caller supplies an idempotency key, but the auto-fire path doesn't synthesize one. Fix: application-level dedupe at the same gate position as #99. Before fireRule dispatch, query `automation_runs WHERE org_id=… AND automation_rule_id=… AND ai_decision_id=…` via `.maybeSingle()`. If a row exists (any status — pending/success/failed), skip with audit log `[automation] auto_fire_dedupe_skip org_id=… rule_id=… ai_decision_id=… existing_run_id=… existing_status=… reason=already_fired`. Why application-level vs schema unique constraint: `automation_runs.ai_decision_id` is NULLABLE (manual fires via executeRule pass it optionally); PostgreSQL unique constraints treat NULLs as distinct, so a `(automation_rule_id, ai_decision_id)` unique would NOT enforce uniqueness when NULL — would require a partial unique index, which is schema scope outside this minimal-diff. Single-writer architecture (only `evaluateRulesForAIDecision` + `executeRule` write automation_runs, both inside this module) makes application-level check sufficient. **Manual fire path via `executeRule` is INTENTIONALLY unaffected** — operator-triggered re-execution is the implicit re-approval. Architecture impact: ZERO new schema; ZERO new tables; ZERO migration; preserves canonical contracts; preserves single-writer (automation_runs inserts still only happen in fireRule); preserves org_id isolation. Single-file diff in `backend/src/services/execution/automation-engine.ts`; backend tsc 0 (pre-existing env-missing-module errors only); HARD LOCK invariants preserved. **All four PRIORITY SAFETY ENFORCEMENT OVERRIDE items now runtime-enforced**: #1 auth catch-scope isolation (#98), #2 + #3 approval-policy enforcement (#99), #4 dispatch dedupe (#100). AI_OPERATING_MODEL.md approval semantics are now centralized invariants in `services/execution/automation-engine.ts` rather than operator-config conventions. **AUTO-FIRE DEDUPE BACKFILL ROBUSTNESS WIRED (continuation #101, 2026-05-12)** — defensive refinement of the #100 dedupe check. Pre-#100 the database may contain duplicate `automation_runs` rows for the same `(automation_rule_id, ai_decision_id)` pair (from any retry-induced double-write that occurred before the gate landed). The original #100 implementation used `.maybeSingle()`, which throws PGRST116 when 2+ rows are returned — converting the dedupe check itself into a 500 error precisely when pre-existing duplicates exist (the case it's designed to detect). Fix: switched to `.order('executed_at', desc).limit(1)` array select + `.length > 0` check. Still answers the correct question ("has this rule+decision pair already fired?") and returns the most-recent existing run id for the audit-log message, even when pre-existing duplicates exist. Backfill-safe: dedupe gate works correctly from day-1 of deploy regardless of prior history. Single-file diff in `backend/src/services/execution/automation-engine.ts`; backend tsc 0; HARD LOCK invariants preserved; no schema/migration. **APPROVAL-REQUIRED OPERATOR VISIBILITY WIRED (continuation #102, 2026-05-12)** — surfaces the #99 auto-fire-gate classification to operators via the rule list. Without this, operators viewing /actions/automation could not tell which rules will auto-fire vs which require manual trigger; rules pointing at restricted action_types would silently not fire on the AI-decision stream and operators would only learn this by grepping stdout. Per priority #4 (execution visibility) + #5 (approval workflow maturity). Three-file additive diff: (a) `backend/src/services/execution/automation-engine.ts` — exported `actionRequiresApproval(actionType): boolean` helper consuming the existing SPEND_INCREASING_OR_LAUNCH_ACTION_TYPES set; refactored the inline #99 gate to use the helper (single source of truth). (b) `backend/src/routes/v1/automation.ts` — extended GET /rules SELECT with `actions_library.action_type` and computed `requires_approval` per-row server-side from the central helper before returning. Manual fire route POST /rules/:id/execute is INTENTIONALLY unaffected (operator triggering = implicit approval). (c) `app/actions/automation/page.tsx` — ApiRule type extended with optional `requires_approval` boolean; rule card renders an amber "Approval" badge with AlertTriangle icon + tooltip "Spend-increasing or launch-capable — auto-fire is blocked; operator must trigger manually via Run Now" when the flag is true. **NO FE mirror of the action-type set** — operator-facing display reads the server-computed boolean directly, preventing FE/BE policy drift. Three-file diff (`automation-engine.ts` + `automation.ts` + `actions/automation/page.tsx`); backend tsc 0; frontend tsc 0; HARD LOCK invariants preserved; no schema/migration; preserves canonical envelope (extending the data block in the rules response is additive — existing FE consumers ignore the new field). Closes the operator-visibility chain on the safety-enforcement pass: gate logic (#99/#100/#101 runtime) + operator awareness (#102 UI). **TRIGGER-SOURCE AUDIT DISCRIMINATOR WIRED (continuation #103, 2026-05-12)** — closes an audit-trail gap exposed by the safety pass. The Phase 4 minimal migration `decision_history.executed_by` CHECK constraint allows only `('manual','automation')` values, so BOTH auto-fires (from `evaluateRulesForAIDecision` via the post-persist hook) AND operator-triggered manual rule fires (from `executeRule` via POST /automation/rules/:id/execute) recorded as `executed_by='automation'` — the audit row could not distinguish auto-fire vs operator-approved manual fire. Critical when reviewing accountability for approval-required actions: operators manually firing `meta.increase_budget` rules (post-#99 the only path) need a clear audit signal that THIS particular execution was operator-approved, not auto-fired. Fix: added `AutomationTriggerSource = 'auto_fire' | 'manual_rule_fire'` parameter to `fireRule` (default `'auto_fire'`); merged into `result_data` JSONB after the executeAction call (preserves all keys executeAction wrote — mode/stage/http_status/token_source/idempotent_replay/etc. — and adds `trigger_source`). Both call sites pass explicit values: `evaluateRulesForAIDecision` → `'auto_fire'`, `executeRule` → `'manual_rule_fire'`. Failure path also preserves trigger_source so failed fires retain the distinction. No schema change required — JSONB result_data is freely-shaped. **Operator visibility through existing UI**: the automation history page already renders primitive top-level keys from result_data (per #39); `trigger_source` string passes the existing renderer's filter automatically, so operators see the new field in the Result card without any FE diff. Single-file diff in `backend/src/services/execution/automation-engine.ts`; backend tsc 0; HARD LOCK invariants preserved verbatim (CHECK constraint untouched; canonical envelope untouched; single-writer preserved); no schema/migration. Closes the safety pass audit chain: gate (#99) + dedupe (#100/#101) + operator awareness (#102 UI badge) + audit-row distinction (#103 trigger_source). **INTEGRATIONS SYNC IN-PROGRESS DEDUPE HARDENING WIRED (continuation #104, 2026-05-12)** — runtime safety hardening on `backend/src/routes/v1/integrations.ts` POST /:id/sync endpoint. Pre-fix the in-progress dedupe lookup had two safety gaps: (a) `error` field was discarded — a DB lookup failure (network blip, RLS denial, schema drift, connection-pool exhaustion) silently returned `data=null` and the code treated it as "no in-progress sync" → would ALLOW a duplicate Inngest sync.requested event to queue. This is a FALSE NEGATIVE on the dedupe path — the most critical case to fail-loud (the lookup IS the only safety check preventing duplicate sync work; a discarded error here disables the safety entirely under DB failure). (b) `.maybeSingle()` errors PGRST116 if 2+ rows exist — pre-existing duplicate in_progress rows from prior bugs would convert the dedupe check itself into a 500 error. Fix: capture error + throw → errorHandler emits sanitized 500 with request_id (CONSTITUTION §3 "Fail Loudly"); switched to `.limit(1)` array select for backfill robustness (mirrors the #101 fix pattern on automation_runs dedupe). Single-file diff in `backend/src/routes/v1/integrations.ts`; backend tsc 0; HARD LOCK invariants preserved; canonical envelope unchanged; no schema/migration. **Safety pass + auxiliary hardening summary (#98 → #104, 7 backend changes)**: auth catch-scope isolation (#98), centralized approval gate (#99), dispatch dedupe with backfill robustness (#100/#101), operator UI badge from centralized policy (#102), audit-row trigger_source distinction (#103), integrations sync dedupe hardening (#104). All preserve canonical contracts, single-writer model, org_id isolation, and require zero new schema/migration. **Campaigns list data-freshness indicator WIRED (continuation #95, 2026-05-12)** — extension of #90/#91/#92/#93/#94 to `app/campaigns/page.tsx`, the sixth volatility-sensitive cockpit surface. Campaign state changes via push operations, status transitions, and potential AI-driven updates; freshness signal lets operators judge whether the list reflects recent activity. Same inline pattern: `lastUpdatedAt` set in both initial-load useEffect AND `fetchCampaigns()` callback; 30s setInterval re-ticks; "Updated X ago" renders beside Refresh button. Single-file diff in `app/campaigns/page.tsx`; frontend tsc 0; HARD LOCK invariants preserved; no schema/backend/migration change. **Six wired surfaces now carry the data-freshness indicator**: dashboard overview, automation history, automation status, execution log, integrations, campaigns list. **Dashboard channels data-freshness indicator WIRED (continuation #96, 2026-05-12)** — extension of #90-#95 to `app/dashboard/channels/page.tsx`, the seventh volatility-sensitive cockpit surface. Channel metrics aggregate over `campaign_metrics` rows which are inserted/updated by Inngest sync jobs; freshness signal helps operators judge whether the displayed ROAS/spend/revenue reflects recent ingestion. Same inline pattern: `lastUpdatedAt` set in both initial-load useEffect AND `refreshChannels()` callback; 30s setInterval re-ticks; "Updated X ago" renders beside the Refresh button. Single-file diff in `app/dashboard/channels/page.tsx`; frontend tsc 0; HARD LOCK invariants preserved; no schema/backend/migration change. **Seven wired surfaces now carry the data-freshness indicator** — coverage now spans every wired LIST/dashboard surface in the cockpit. **Settings/billing refresh WIRED (continuation #67, 2026-05-12)** — `app/settings/billing/page.tsx` extended with the same Refresh button affordance. Re-fires the existing `load()` callback that BYOK save/delete already invoke. Placed at the page header level (flex layout between page title and refresh button). Single-file diff in `app/settings/billing/page.tsx`; frontend tsc 0; HARD LOCK invariants preserved; no schema/backend/migration change. **Cockpit refresh coverage absolute**: dashboard overview + channels + campaigns list + execution log + automation history + automation status + settings/billing — every wired top-level operator surface has a uniform RefreshCw + animate-spin + disabled-while-refreshing button.

Active execution target (within current authorization):

NONE — every operator-authorized phase / sub-pass / bridge is closed within its authorized scope. Remaining items in the PATCH QUEUE require explicit phase unlock:

- Phase X broader unlock — needs explicit authorization. Prerequisites NOW satisfied: Phase 4 Part 2 stable + Phase 5 closed + Phase 4/6 bridge resolved. Spec authority: `specs/mcp-orchestration.md`, `specs/mcp-integration.md`, `specs/ai-execution.md`, `specs/009-ai-orchestration/spec.md`.
- Remaining 4 Phase 6 mocked-shell pages governance-deferred per #22 (UX↔backend semantic mismatches): `app/automation/strategies/page.tsx`, `app/actions/page.tsx`, `app/actions/[id]/page.tsx`, `app/creatives/page.tsx`. Each requires either a preset-templates / AI-recommendations backend layer (not in any current phase) or page redesign (forbidden by "no redesign").
- Phase 7 monetization extensions beyond what is shipped (invoice/utilization/payment-method endpoints) — bound to separate authorization.
- ✅ PHASE2_ENVELOPE_FOLLOWUP — RESOLVED (continuation #31, 2026-05-12). Backend-side canonicalization complete across integrations.ts/connect.ts/metrics.ts (16 sites); FE apiClient absorbs transparently.
- PHASE2_OAUTH_ENV_FAILFAST_FOLLOWUP / STRIPE_SECRET_KEY startup-fail-fast — non-blocker hardening tasks; tracked.
- Per-org Meta `*_budget` Vault token migration — tracked.
- (RESOLVED) Auto-fire success log line (#28) + action-executor.ts pre-dispatch logging-mode label parity (#26 + #27) — both observability follow-ups CLOSED; see NEXT ACTION items P + Q. No further tracked observability follow-ups on these surfaces.

Holding pattern (default):

- Maintain governance lock until next authorization arrives
- Preserve all closed-slice invariants verbatim (including Phase 5 + Phase 6 Sub-A/B/C + Phase 7 Sub-A0/A0.5/A1a/A1b/A1c/B/D + Phase 4/6 bridge #23 + #24 Path F closure state)
- Reject any work that crosses into Phase X broader without explicit authorization
- Reject any work that wires the 4 governance-deferred Phase 6 mocked-shell pages without spec amendment or operator authorization
- Preserve remaining deferred-phase 503 gates: `/decisions/*`, `/alerts/*` (each tied to Phase 3 anomaly DEPRECATED+DEFERRED state)

DO NOT:

- modify legacy `decisions` table without explicit re-architecture decision
- re-open Phase 4 minimal slice
- re-open closed Phase 0 / Phase 1 patches
- re-open the 13 closed backend runtime hardening passes
- re-open Phase 4 Part 2 / Phase 5 / Phase 6 Sub-A/B/C / Phase 7 Sub-A0/A0.5/A1a/A1b/A1c/B/D / continuations #23 / #24 — all closed
- wire the 4 governance-deferred Phase 6 mocked-shell pages ahead of authorization
- extend the auto-fire post-persist hook (#23) beyond its single-call shape without explicit cross-phase authorization
- expand AI Output Contract (`AIResponse`) beyond Path F's optional `category` field without explicit authorization
- implement Phase X broader (MCP routing / tool governance / log-sink fan-out / strategy_tag enum) without explicit authorization

CURRENT GOAL:

Hold governance lock. Phase foundation is hardened; closed-loop Decision → Action → Result product promise (CLAUDE.md §1) is now operational AND verifiable end-to-end via `automation_rules` + AI Output Contract `category` emission + executeAction canonical pipeline + `automation_runs.ai_decisions(category)` join. The system is ready for Phase X broader unlock when explicitly authorized; tracked-followup work (#10/#11/M/N/O/P/Q items above) is the only governance-neutral candidate set without further authorization.

IF goal unclear → STOP (no guessing)

---

## PHASE 4 (MINIMAL SLICE) — DELIVERABLE CHECKLIST (CLOSED)

- [x] actions_library (system reference, seeded, RLS authenticated-read)
- [x] decision_history (org-scoped, RLS, FK to organizations + ai_decisions, trace_id)
- [x] execution_id idempotency (partial unique index `(org_id, execution_id)`)
- [x] impact_snapshot persistence (JSONB after-state per audit row)
- [x] executed_by (`manual` | `automation`) discriminator
- [x] executeAction service — validates, logs, dispatches, persists
- [x] POST /api/v1/actions/:id/execute route — full body shape (params + idempotency key + ai_decision_id + trace_id)
- [x] 4 real-mode handlers (Meta pause, Meta decrease_budget, Meta increase_budget, Resend send_alert_email)
- [x] Structured `[exec]` lifecycle logging (start / api_call / api_response / end / error)
- [x] Server-side max-percent cap on increase_budget (`META_INCREASE_BUDGET_MAX_PERCENT`, default 50)
- [x] Type-check 0 errors
- [x] Constraint enforcement verified live (CHECK on result enum, FK to organizations, partial unique index, CHECK on executed_by)

Out of scope (Phase 4 Part 2):
- [ ] automation_rules / automation_runs
- [ ] automation engine code
- [ ] google.pause_campaign real-mode (Phase 2 prerequisite)
- [ ] per-org credential storage (Phase 2 prerequisite)
- [ ] per-org rate limiting (cross-cutting; future)

---

## SAFE EXECUTION ORDER (REFERENCE)

1. Validate AI output (NO DB WRITE) — `validateAIResponse`
2. If valid → allow persistence — type-gated in `persistAIDecision`
3. Log AI interaction — `logAIInteraction` (passive)
4. Apply confidence rules — `deriveStatus`
5. Execute action (when triggered) — `executeAction`:
   a. idempotency pre-check on `(org_id, execution_id)` — return cached row on hit
   b. fetch template from `actions_library` (no execution if missing)
   c. validate required params per `parameter_schema` (no execution if missing)
   d. resolve optional `ai_decisions` linkage (org-scoped lookup)
   e. emit `[exec] phase=exec.start`
   f. dispatch handler (real or simulated based on flags + token + allowlist)
   g. emit `[exec] phase=exec.end`
   h. INSERT `decision_history` row with `executed_by`, `result`, `ai_decision_id`, `trace_id`, `execution_id`, `impact_snapshot`
   i. on `23505` race: SELECT existing row, return idempotent reply

NEVER:
- save before validation
- skip logging
- bypass confidence logic
- bypass actions_library lookup
- skip parameter validation
- write decision_history without server-side org_id
- exceed META_INCREASE_BUDGET_MAX_PERCENT on a single call
- email outside the calling org's admin set
- store a placeholder email recipient in any external send

---

## DATABASE STATE

- Supabase project: CONNECTED
- Migration directory (canonical): `/supabase/migrations`
- Live migrations:
  - `20260428090320_remote_schema.sql`
  - `20260428091421_remote_schema.sql`
  - `20260429090032_remote_schema.sql`
  - `20260502000001_ai_persistence.sql` (Phase 3 — ai_decisions, ai_logs)
  - `20260503100000_phase3_close_campaigns_schema.sql` (Phase 3 close — campaigns RLS + org_id)
  - `20260503130000_phase4_minimal_execution_layer.sql` (Phase 4 — actions_library, decision_history)
  - `20260503140000_phase4_decision_history_idempotency.sql` (Phase 4 — execution_id + partial unique index)
  - `20260503150000_phase4_decision_history_impact_snapshot.sql` (Phase 4 — impact_snapshot column)
  - `20260503170252_remote_schema.sql` (remote sync — drift fix)
  - `20260507120000_phase2_data_ingestion.sql` (Phase 2 — integrations, ad_accounts, campaign_metrics partitioned, sync_logs; deployed 2026-05-07; 4 tables verified live)
  - `20260507130000_phase4_part2_automation.sql` (Phase 4 Part 2 — automation_rules, automation_runs, decision_history extension columns; deployed 2026-05-07; PR #5 merged commit `df2f243`; `/automation/*` gate lifted)
  - `20260507140000_phase5_creatives.sql` (Phase 5 — brand_kits + creative_generations + creatives tables; organizations.plan_type + vault_byok_openrouter_secret_id columns as Phase 7 substrate for FR-011 BYOK gate; private `creatives` storage bucket; deployed 2026-05-07; PR #6 merged commit `f5aa8ff`; `/creatives/*` + `/brand-kit/*` gates lifted)
  - `20260508120000_phase6_create_campaign_templates.sql` (Phase 6 Sub-pass A — additive seed of `meta.create_campaign` + `google.create_campaign` rows in `actions_library`; idempotent via existing UNIQUE; **authored 2026-05-08 continuation #11; DEPLOYED 2026-05-08 continuation #12 via Supabase MCP apply_migration**; live registry version: `20260508175638_phase6_create_campaign_templates` — server-side timestamp; local-vs-registry timestamp drift tracked as MIGRATION_TIMESTAMP_RECONCILIATION follow-up; closes `pushCampaign` silent 404 bug from prior fictional UUID coupling)
  - `20260508130000_phase7_a0_credits_substrate.sql` (Phase 7 Sub-pass A0 — additive credits substrate: `organizations.credits_balance INTEGER NOT NULL DEFAULT 0 CHECK (>= 0)` column + `ai_usage_logs` table (RLS by org_id, 3 indexes) + `credits_ledger` table (RLS by org_id, 2 indexes) + `deduct_credits(p_org_id TEXT, p_amount INTEGER) RETURNS INTEGER` RPC + `refund_credits(p_org_id TEXT, p_amount INTEGER) RETURNS INTEGER` RPC + `log_ai_usage(...)RETURNS UUID` RPC; **authored 2026-05-08 continuation #14; deploy via Supabase MCP `apply_migration` PENDING**; closes Phase 5 ↔ Phase 7 silent runtime drift where `creatives.ts:POST /generate` calls non-existent RPCs)
- Schema: ALIGNED with code (Phase 0 + Phase 1 + Phase 2 + Phase 3 + Phase 4 minimal + Phase 4 Part 2 + Phase 5 — all fully closed; Phase 6 Sub-pass A live; Phase 7 Sub-pass A0 substrate authored, deploy pending)
- Legacy: `/db/_archive_migrations/` — ARCHIVE ONLY, never referenced

RULE:

Claude MUST read:
- the canonical migration file for the table being touched
- before ANY DB usage, confirm the table schema matches the code's `from(...).select(...)` shape

---

## LAST UPDATE

2026-05-03 — Phase 4 (minimal slice) CLOSED. CLOSING_AUDIT_PHASE4.md generated. Phase 4 Part 2 explicitly scoped as DEFERRED with documented unlock conditions. PATCH QUEUE updated; Phase 0 + Phase 1 are now the active focus (parallel-safe). HARD LOCK refreshed to incorporate Phase 4 invariants (max-percent cap, idempotency unique index, recipient placeholder filter). REAL SYSTEM CAPABILITIES enumerates the live action surface and the deferred surface explicitly.

2026-05-07 — Phase 0 + Phase 1 foundation patches CLOSED via runtime evidence (verified across 13 backend runtime hardening passes). Phase 0: tracingMiddleware + requestLoggerMiddleware + correlator chain across `[req]/[err]/[exec]/[AI]/[auth]/[clerk-webhook]`. Phase 1: canonical envelope (`backend/src/utils/response.ts`) + audit columns on active write paths (auth.ts JIT, createCampaign, updateCampaign). Phase 1 metadata-JSONB on `decisions/creatives/automation_runs` reclassified as governance-bound to those phases' unlocks (NOT a Phase 1 blocker). 13 cumulative backend hardening passes (UUID/body/path/LIST validation parity, PGRST116 discriminator parity, request_id triple-sink, ai-suggestions silent-write fix, pushCampaign discriminator, auth.ts canonical normalization, AI empty-prompt guard, body-limit cap, smoke-flag gate, LIVE-flag dependency fail-fast, deferred-router 503 gating) added to PATCH QUEUE position #5 as DONE. SYSTEM STATUS line corrected: Frontend re-classified from "COMPLETED" to "SHELLED" per runtime evidence (5 pages wired / 34 mocked; mocked-shell state is INTENTIONAL per Phases.md phase scope, NOT drift). NEXT ACTION + CURRENT EXECUTION TARGET updated to reflect saturation: no in-scope active execution target remains; allowed governance-neutral moves are SEO baseline (allow-list) and orphan drift cleanup; all other paths require explicit phase unlock authorization. HARD LOCK preserved verbatim; REAL SYSTEM CAPABILITIES preserved verbatim; PHASE 4 CHECKLIST preserved verbatim; SAFE EXECUTION ORDER preserved verbatim; DATABASE STATE preserved verbatim; TOOLING / EXECUTION CONSTRAINT preserved verbatim. NO code, schema, contract, or migration changes this turn.

2026-05-07 (continuation) — Governance-neutral allow-list executed end-to-end. (1) Drift cleanup: removed three orphan/zombie surfaces — `app/dashboard/saas/page.tsx` (Stitch template residue, broken types, no Sidebar/routing-map reference), `app/dashboard/channel/page.tsx` (singular duplicate of `/dashboard/channels`), `lib/data/mock-data.ts` (Stitch demo dataset that only fed the two orphan pages); removed hardcoded `/campaigns/1` placeholder Sidebar entry and now-unused `BarChart2` import (`components/dashboard/Sidebar.tsx`). (2) SEO baseline created (allow-list only, per SEO SPECIAL RULE; no marketing/blog/docs/funnel work): `app/sitemap.ts` (root only, authenticated SaaS surface intentionally minimal), `app/robots.ts` (public-allow root + auth pages, disallow all authenticated /dashboard, /campaigns, /decisions, /actions, /automation, /creatives, /integrations, /settings, /api), `app/manifest.ts` (PWA manifest using CLAUDE.md §8 design tokens), `app/opengraph-image.tsx` (1200×630 brand image via next/og), `app/layout.tsx` root metadata refreshed (Stitch placeholder copy "Precision Curator Dashboard" → "GrowthHub — AI-powered Growth Operating System"; added openGraph + twitter + robots fields + metadataBase). Frontend tsc → 0 errors (saas/page.tsx pre-existing error eliminated). Backend tsc → 0 errors. Phase locks preserved: NO deferred phase opened, NO schema/contract/migration changes, NO mocked-shell pages wired, NO frontend-backend bridge created beyond static metadata files. Architecture invariants intact: single-writer backend, org_id enforcement, deferred-router 503 gating, canonical envelope, request_id correlator chain, all 13 prior closed hardening passes. Governance-neutral scope is now exhausted; further moves require explicit Phase 2 / Phase 4 Part 2 / Phase X / Phase 5 / Phase 6 frontend / Phase 7 unlock authorization.

2026-05-07 (continuation #2) — Phase 2 unlock prep AUTHORIZED and STARTED. Discovered runtime drift: Phase 2 backend code (connect.ts, integrations.ts, metrics.ts, vault.ts, oauth-state.ts, jobs/inngest.ts, services/sync/{meta,google,shopify,index}.ts) + frontend callback (`app/api/integrations/callback/[platform]/`) all in place; specs/002-data-ingestion/tasks.md marks T001–T027 complete. BUT canonical migration was missing from `/supabase/migrations/` — only existed in forbidden `/db/_archive_migrations/` directory (per CLAUDE.md MIGRATION SOURCE OF TRUTH rule, archive is never referenced for execution). Executed schema-prep step ONLY (not deploy or gate-lift): authored `supabase/migrations/20260507120000_phase2_data_ingestion.sql` from `specs/002-data-ingestion/data-model.md` authority, with one runtime-evidenced amendment — added `campaign_metrics.integration_id UUID REFERENCES integrations(id)` to match runtime upsert payloads in services/sync/{meta,google,shopify}.ts (per "runtime evidence overrides documentation" governance rule). Migration creates 4 tables: integrations, ad_accounts, campaign_metrics (PARTITIONED BY date with 8 quarterly partitions + default), sync_logs; all with RLS + org_id-scoped policies + indexes. Phase 2 status: DEFERRED → PARTIAL. DATABASE STATE updated to list authored-but-not-yet-deployed migration. PATCH QUEUE position #6 reclassified IN PROGRESS. NEXT ACTION + CURRENT EXECUTION TARGET reflect new state. NO 503 gates lifted — gates remain in place on `/integrations/*` and `/metrics/*` until `supabase db push` deploy is verified. NO backend code touched; tsc still 0 errors. Phase locks preserved: Phase 4 minimal slice (actions_library, decision_history) UNTOUCHED; Phase 3 (ai_decisions, ai_logs, campaigns) UNTOUCHED; Phase 4 Part 2 STILL DEFERRED behind Phase 2 deploy + per-org token plumbing. Architecture invariants intact: single-writer backend, org_id enforcement, deferred-router gating preserved on all currently-deferred routers, canonical envelope, request_id correlator chain, all 13 prior closed hardening passes preserved.

2026-05-07 (continuation #3) — Phase 2 unlock COMPLETED. Operator confirmed: PR merged into `main`; local main synced; `supabase db push` completed successfully; 4 canonical Phase 2 tables (integrations, ad_accounts, campaign_metrics, sync_logs) verified live in production Supabase. Executed final unlock step: lifted 503 gates on `/integrations/*` and `/metrics/*` in `backend/src/routes/v1/index.ts` (removed two `v1.use(...)` deferredPhase lines; preserved gates on `/decisions/*`, `/alerts/*`, `/automation/*`, `/creatives/*`, `/brand-kit/*` per their respective unowned phases). Wired frontend now reaches live backend: `app/integrations/page.tsx` (GET /api/v1/integrations), `app/integrations/connect/page.tsx` (POST connect/start), `app/api/integrations/callback/[platform]/route.ts` (POST connect/complete), `app/dashboard/overview/page.tsx` (GET /api/v1/metrics/summary), `app/dashboard/channels/page.tsx` (GET /api/v1/metrics/channels). Phase 2 status: PARTIAL → CLOSED. CURRENT PHASE updated. SYSTEM STATUS Integrations line corrected: NOT CONNECTED → READY. REAL SYSTEM CAPABILITIES Data Source line corrected: STATIC → READY. PATCH QUEUE position #6 closed; positions #10 and #11 added for non-blocker follow-ups (PHASE2_ENVELOPE_FOLLOWUP for canonical-envelope migration of legacy Phase 2 routes, PHASE2_OAUTH_ENV_FAILFAST_FOLLOWUP for startup-fail-fast on OAuth env). Backend tsc → 0 errors. Phase locks preserved: Phase 4 minimal slice UNTOUCHED, Phase 3 UNTOUCHED, Phase 4 Part 2 STILL DEFERRED (now its prerequisites are satisfied; awaits explicit unlock authorization), all other deferred routers still 503-gated. Architecture invariants intact: single-writer backend, org_id enforcement, canonical envelope on active hardened surface (auth/ai/actions/history/campaigns), request_id correlator chain, all 13 prior closed backend hardening passes. Known remaining envelope inconsistency on Phase 2 routes (legacy bare-array + `{error:'...'}` shapes) explicitly tracked as non-blocker follow-up; the wired frontend was already coupled to the legacy shapes, so canonicalizing the envelope requires coordinated frontend update beyond Phase 2 scope.

2026-05-12 (continuation #39) — `result_data` JSONB execution-provenance render + `action_type` technical-label render + Inngest registration-block comment-hygiene refresh. Post-audit continuation batched under CONTINUATION BATCHING RULE + IMPLEMENTATION EXECUTION MANDATE. **Background:** the operator-requested READ-only audit (which produced no implementation work — pure classification + status calibration) surfaced three implementation-ready candidates: (a) `result_data` JSONB rendering — runtime-verified (BE returns it at automation.ts:414 since Phase 4 Part 2; FE was dropping it); (b) `action_type` already mapped post-#38 but unrendered; (c) stale `Phase 2/5 deferred` comments in inngest.ts registration block. (a) + (b) execute as implementation continuations (highest priority); (c) bundles opportunistically as comment-hygiene per EXECUTION PRIORITY OVERRIDE bundling rule. Other audit candidates explicitly REJECTED with evidence per IMPLEMENTATION EXECUTION MANDATE STOP-condition reasoning: `decision_history` nested-through JOIN (semantic overlap with reasoning_steps requires operator design call); Growth Suggestion card (requires new contract surface); STRIPE_SECRET_KEY + OAuth env fail-fast (SYSTEM_CONTROL.md item N explicit "operator decision"); uncommenting Inngest functions (SYSTEM_CONTROL.md item from #30 explicit "operator decision"). **Code changes (3 files):** (1) `app/automation/history/page.tsx` — (i) added `resultData: Record<string, unknown> | null` field to `HistoryEntry`; (ii) mapped `run.result_data ?? null` in `mapRunToEntry` (shape-agnostic passthrough); (iii) rendered in the Result card via an IIFE filter: `Object.entries(resultData).filter(primitive-only).slice(0, 6)` → renders as a `<dl>` of `key: value` pairs below `resultDetail`; values stringified + truncated to 80 chars; nested objects (e.g., upstream-API `body` from action-executor live-mode runs) intentionally filtered out to avoid leaking large/unstructured payloads to operator UI; only renders for `result === "Success"` (failed runs already surface `error_message` via `resultDetail`); (iv) added `action_type` rendering on the action chip in monospace as a tertiary label, only shown when distinct from the friendly `name` (avoid redundancy when name === action_type); (v) updated file-top WIRED comment block to enumerate #39 wiring. (2) `backend/src/jobs/inngest.ts` — refreshed the 4 commented-out function entries in `functions = [...]` registration array to reflect ACCURATE current state: `dailySyncAll` / `syncIntegration` previously labeled "Phase 2 ingestion deferred" → corrected to "Phase 2 UNLOCKED (#3); auto-sync registration pending operator decision (SYSTEM_CONTROL.md #30)"; `generateCreative` previously labeled "Phase 5 creatives deferred" → corrected to "Phase 5 UNLOCKED (#3); Inngest registration paused pending operator decision on Inngest-driven creative generation path"; `generateDecisions` kept as accurately DEFERRED per CANONICAL AI SYSTEM resolution (legacy `decisions` table). NO function uncommenting (operator decision boundary). (3) `backend/src/routes/v1/index.ts` — added a governance comment block above the `/decisions/*` + `/alerts/*` 503 gates explicitly classifying them as DEFERRED per CANONICAL AI SYSTEM resolution (not "missing"), referencing the canonical `ai_decisions` flow as the operative replacement. **UX delta (FE):** PRIOR — Result card showed only `resultDetail` ("Action Executed" / generic for success; `error_message` for failed). POST-#39 — successful runs additionally surface up to 6 primitive-typed top-level keys from `result_data` (e.g., `mode: live`, `stage: transport`, `http_status: 200`, `token_source: vault`, `idempotent_replay: true`, `original_history_id: <uuid>`) below the resultDetail line as a small key:value list. Full execution provenance visible inline per operator audit need; nested upstream-API response bodies remain opaque (defensive). Action chip now shows up to three signal tokens: friendly name · platform · monospace action_type (the technical class label). **Architectural delta (BE):** ZERO. The two BE files touched (inngest.ts + routes/v1/index.ts) are comment-only governance-hygiene refreshes — no handler change, no registration change, no gate change. **Tracing pass conducted BEFORE selecting work** per CONTINUOUS EXECUTION POLICY mandatory tracing AND EXECUTION PRIORITY OVERRIDE (post-audit context): enumerated all unconsumed backend-returned fields on `/automation/runs` (result_data identified as the sole remaining unconsumed field after #38; action_type was mapped post-#38 but never rendered); verified action-executor write shapes (15 distinct result_data shapes; common primitive keys: mode/stage/http_status/token_source/idempotent_replay/original_history_id; nested keys: body/token expansion); selected primitive-only filter as the defensive render strategy. Verified all other audit candidates against IMPLEMENTATION EXECUTION MANDATE STOP conditions; documented rejections in this entry. **Verification:** backend `tsc --noEmit` → 0 errors. Frontend `tsc --noEmit` → 0 errors. **Contamination analysis:** ZERO migration; ZERO route mounted/unmounted; ZERO 503 gate change; ZERO Inngest registration change (NO function uncommented — comment refresh only); ZERO new phase unlocked; ZERO deferred surface activated; ZERO new abstraction; ZERO new dependency; ZERO behavior change for the auto-fire chain itself (purely additive read-side enrichment, same as #37/#38). PII / secret-leak surface analysis: `result_data` primitive-only filter excludes `body` (potentially-large upstream response); `token_source` is the metadata label `"vault"` / `"env"` (NOT the token itself — confirmed via action-executor.ts:677); `http_status` is integer; `idempotent_replay` is boolean; `original_history_id` is a UUID with no PII. Safe to surface to operator UI. **Preserved verbatim:** all HARD LOCK invariants; CANONICAL AI SYSTEM resolution; ApiError public API; formatErrorMessage helper (#35); all closed-slice classifications Phase 0/1/2/3-linear/4-min/4-Part2/5/6 Sub-A/B/C/7 Sub-A0-D/#22→#38; Phase X broader LOCK; 4 Phase 6 deferred mocked-shells (not touched); `/decisions/*` + `/alerts/*` 503 gates (governance documented at-site, gates UNCHANGED); single-writer backend; org-isolation; canonical envelope; request_id correlator chain; all 49 backend handler signatures. **Files changed:** `app/automation/history/page.tsx` (+~50 LOC across resultData field + render IIFE + action_type chip + comment block), `backend/src/jobs/inngest.ts` (+4 commented-line replacements with accurate rationale), `backend/src/routes/v1/index.ts` (+10 LOC governance-classification comment above the gates). Plus `specs/SYSTEM_CONTROL.md` governance metadata. **Operator deploy prerequisites:** frontend redeploy only (BE changes are comment-only; no PostgREST hot-reload needed; no behavior change). **Remaining tracked-followups in CURRENT EXECUTION TARGET:** items N (STRIPE_SECRET_KEY + Phase 2 OAuth startup-fail-fast — operator decision); O (Meta `*_budget` Vault dispatch — operator decision); Phase 2/Phase 5 Inngest registration (#30 — explicit operator decision); operator-side 28 spec files uncommitted; `database-migrations.md` canonical-contract conflict; Phase X broader / Phase 8 / 4 deferred Phase 6 mocked-shells (locked phase unlock required); audit-surfaced FE coverage gap (24 mocked-shell pages, ~10 of which have live BE — `/automation`, `/actions`, `/creatives/*` families — substantial multi-page wiring work pending operator sequencing). **Adjacent natural-continuation candidates surfaced (CORRECTED post-#39 governance re-verification)** — the original #39 close listed candidates (a) "wire /automation/page.tsx Decision Center" and (b) "wire /actions/page.tsx + /actions/[id]/page.tsx" as "MEDIUM-EFFORT IMPLEMENTATION operator-decision-free". This was **INCORRECT**: per SYSTEM_CONTROL.md line 364 explicit prohibition ("🚫 DO NOT: Wire mocked frontend surfaces ahead of their owning phase unlock"), line 10 SYSTEM STATUS classification ("Mocked: 33 pages governance-bound … INTENTIONAL, not drift"), and line 344 explicit enumeration of `app/actions/page.tsx` + `app/actions/[id]/page.tsx` among the 4 Phase 6 deferred shells, BOTH candidates are GOVERNANCE-DEFERRED and require explicit phase unlock authorization. Per FRONTEND SHELL RULE these pages are IMPLEMENTED BUT SHELLED, not missing or drift. Per IMPLEMENTATION EXECUTION MANDATE STOP condition "operator decision boundary is encountered" — both candidates require operator phase-unlock decision, NOT autonomous wiring. Remaining ACCURATE candidates: (c) `decision_history` nested-through-ai_decisions JOIN — operator design call required (semantic overlap with reasoning_steps); (d) AI decision auto-trigger — operator decision (audit C1). **No autonomous implementation continuation currently available within governance boundaries.** Active-surface backend runtime hardening SATURATED (line 334). Remaining work per line 342-352: all items GOVERNANCE-LOCKED (F/G) or OPERATOR-DECISION-PENDING (N/O) or CLOSED. Governance-NEUTRAL allowances per line 353-355 (SEO baseline, drift cleanup) already SATURATED. Per CONTINUATION BATCHING RULE legitimate STOP condition: "HARD LOCK or operator decision boundary is encountered." Next implementation continuation requires explicit operator authorization on one of: (1) Phase X broader unlock (item F); (2) Phase 6 frontend wiring unlock for the 4 deferred shells (item G); (3) Phase 7 monetization extensions beyond shipped scope; (4) AI decision auto-trigger design + activation (audit C1); (5) Phase 2/Phase 5 Inngest function registration activation (item from #30).

2026-05-12 (continuation #38) — Action-template visibility + AI-signal inline chips WIRED in `app/automation/history/page.tsx`. Sibling-batched continuation per CONTINUATION BATCHING RULE — completed within the same execution pass as #37 was the structurally correct call, but #37 was scoped to the AI Decision Insights aside alone; #38 covers the per-row expanded section and the collapsed-row action badge. Operator-authorized under IMPLEMENTATION EXECUTION MANDATE + ADJACENT CONTINUATION AUTHORITY ("extending existing dashboards/services/routes" + "connecting already-existing backend/frontend surfaces"). **Background:** post-#37 tracing pass enumerated remaining UI/backend visibility gaps in `automation_runs` → history-page consumption: (i) `action_template_id` was returned but no JOIN existed → FE showed only rule name (trigger logic), never the action template that was actually dispatched; (ii) `category` + `confidence` from #24/#34 were rendered ONLY in the collapsed-row trigger string (category) and the aside-panel confidence bar — the expanded per-entry section ignored both signals. Both gaps are sibling-class to #37 (same surface, same FK pattern, minimal-diff). **Code changes (2 files):** (1) `backend/src/routes/v1/automation.ts` — extended SELECT string from `'…ai_decisions(category, confidence_score, reasoning_steps)'` to `'…ai_decisions(category, confidence_score, reasoning_steps), actions_library(name, platform, action_type)'`; added governance comment block referencing #38 lineage and noting the `actions_library` FK is NOT NULL per Phase 4 Part 2 migration so the nested-select is always populated on legitimate runs. (2) `app/automation/history/page.tsx` — (a) extended `ApiAutomationRun` with optional `actions_library?: { name, platform, action_type } | null` block (defensive optionality only guards PostgREST detection-failure / RLS-denial); (b) added `actionName`, `actionPlatform`, `actionType` fields to `HistoryEntry`; (c) mapped `run.actions_library?.{name,platform,action_type} ?? null` in `mapRunToEntry`; (d) populated previously-unused `actionTag` field from `actionName` so the collapsed-row "Action Taken" column now displays the actual action template name as a small badge (was previously always undefined → never rendered); (e) added a chip row above the existing 3-card grid in the expanded section showing three conditional chips: Action (Sparkles icon, primary tint; "{name} · {platform}"), Category (Lightbulb icon, neutral tint; "{category}"), Confidence (CheckCircle2 icon, emerald tint; "{confidence}% confidence"). Each chip only renders when its data is present; whole chip row hidden when none are present (manual runs / pre-AI-contract rows degrade gracefully). (f) updated the file-top WIRED comment block to record #38 wiring of action template + AI-signal chips. **UX delta:** PRIOR — operator viewing the expanded history-row section saw only Trigger Condition / Data Used / Result; the actual action template executed was invisible everywhere on the page; category and confidence appeared only in the aside or compressed into the collapsed trigger string. POST-#38 — collapsed row "Action Taken" column shows the action template name as a small badge under the actionTaken text; expanded section shows a chip row with three inline signal chips (Action template + platform, Category, Confidence percentage) followed by the existing Trigger/Data/Result grid. Operators see the full closed-loop chain at a glance: Decision (rule name) → Category (AI signal class) → Confidence (AI certainty) → Action (template dispatched) → Result (success/failure). **Tracing pass conducted BEFORE selecting work** per CONTINUOUS EXECUTION POLICY + EXECUTION PRIORITY OVERRIDE: enumerated unused backend-returned fields on `/automation/runs` (action_template_id without name JOIN; result_data structured JSONB; category+confidence already mapped but not rendered in expanded section); verified `actions_library` FK is NOT NULL and the nested-select pattern matches #24/#34/#37 verbatim (no new abstraction); verified `decision_history` data_used wiring would require nested-through-ai_decisions JOIN with semantic overlap with reasoning_steps (#37) — deferred as needing more design thought, NOT batched; verified `result_data` JSONB-of-any-shape requires defensive rendering — deferred for separate continuation. Selected scope: actions_library JOIN + inline chip row (clear sibling to #37). **Verification:** backend `tsc --noEmit` → 0 errors. Frontend `tsc --noEmit` → 0 errors. **Contamination analysis:** ZERO migration; ZERO route mounted/unmounted; ZERO 503 gate change; ZERO Inngest registration change; ZERO new phase unlocked; ZERO deferred surface activated; ZERO new abstraction; ZERO new dependency; ZERO behavior change for the auto-fire chain itself (purely additive read-side enrichment, same as #37). PostgREST relationship detection on `automation_runs.action_template_id → actions_library.id` is standard FK-based — no foreign-key hint needed. **Preserved verbatim:** all HARD LOCK invariants; CANONICAL AI SYSTEM resolution (legacy `decisions` table still DEPRECATED+DEFERRED; `decision_history` separately tracked and intentionally NOT touched this pass — its nested-through JOIN remains a future candidate pending design decision on ai_explanation vs reasoning_steps redundancy); ApiError public API; formatErrorMessage helper (#35); all closed-slice classifications Phase 0/1/2/3-linear/4-min/4-Part2/5/6 Sub-A/B/C/7 Sub-A0-D/#22→#37; Phase X broader LOCK; 4 Phase 6 deferred mocked-shells (not touched); `/decisions/*` + `/alerts/*` 503 gates; single-writer backend; org-isolation; canonical envelope; request_id correlator chain. **Files changed:** `backend/src/routes/v1/automation.ts` (+11 LOC governance comment / -2/+2 SELECT extension), `app/automation/history/page.tsx` (~+57/-3 across type extension + mapping + chip-row render + comment update). Plus `specs/SYSTEM_CONTROL.md` governance metadata. **Operator deploy prerequisites:** backend redeploy (PostgREST SELECT scope change; hot-reloads automatically) + frontend redeploy (consumer rendering). No DB migration required. **Remaining tracked-followups in CURRENT EXECUTION TARGET:** items N (STRIPE_SECRET_KEY + Phase 2 OAuth startup-fail-fast — operator decision); O (Meta `*_budget` Vault dispatch — operator decision); Phase 2/Phase 5 Inngest registration (#30); operator-side 28 spec files uncommitted; `database-migrations.md` canonical-contract conflict; Phase X broader / Phase 8 / 4 deferred Phase 6 mocked-shells (locked phase unlock required). **Adjacent natural-continuation candidates surfaced** (for next-turn execution, in priority order): (a) `result_data` JSONB rendering in expanded section's Result card — currently shows only error_message OR generic "Action Executed"; `result_data` carries structured details like `{mode: 'live', http_status, body, token_source}` for live-mode runs and `{idempotent_replay: true, original_run_id}` for replays. Requires defensive `<pre>{JSON.stringify(...)}</pre>` rendering OR a small typed renderer for the common `{mode, stage, http_status}` shape. Operator-visible value: full execution provenance. (b) `decision_history` nested-through-ai_decisions JOIN for `data_used` + `ai_explanation` — requires decision on whether ai_explanation (legacy single-field) should override / coexist with / be hidden under the reasoning_steps render from #37; semantic overlap warrants explicit design call. (c) Growth Suggestion card in AI Decision Insights aside — still requires new contract surface (no per-run recommendation endpoint). NOT recommended without operator decision.

2026-05-12 (continuation #37) — Detailed Explanation panel WIRED via `ai_decisions(reasoning_steps)` JOIN — closes the last `app/automation/history/page.tsx` MOCKED-DEFERRED panel that was substantively addressable. Operator-authorized under IMPLEMENTATION EXECUTION MANDATE + ADJACENT CONTINUATION AUTHORITY ("completing partially-implemented execution chains" + "wiring already-existing contracts"). **Background:** at #22 the Detailed Explanation panel was preserved as mocked-deferred with reason "no per-run AI explanation endpoint; CONSTITUTION §AI Output Contract is per-decision, not per-run". That deferral premise was already resolved by #24 (added `ai_decisions(category)` JOIN to `GET /automation/runs`) + #34 (extended same JOIN with `confidence_score`). The JOIN infrastructure was present; `reasoning_steps` data was just not being consumed. AI Output Contract validator (`backend/src/utils/aiValidator.ts`) guarantees `reasoning_steps: Array<{step, insight}>` with min length 1 on every persisted ai_decisions row — so when the JOIN returns a non-null `ai_decisions` block, `reasoning_steps` is non-null AND non-empty. **Pattern continuity:** same single-token extension as #24/#34 — PostgREST nested-select on existing FK `automation_runs.ai_decision_id`; no schema change; no migration; no index change. **Code changes (2 files):** (1) `backend/src/routes/v1/automation.ts` — extended SELECT string from `'…ai_decisions(category, confidence_score)'` to `'…ai_decisions(category, confidence_score, reasoning_steps)'`; added governance comment block referencing #24/#34/#37 lineage. (2) `app/automation/history/page.tsx` — (a) extended `ApiAutomationRun.ai_decisions` type with `reasoning_steps: Array<{step, insight}> | null`; (b) added `reasoningSteps: Array<{step, insight}> | null` field to `HistoryEntry`; (c) mapped `run.ai_decisions?.reasoning_steps ?? null` in `mapRunToEntry`; (d) widened `activeEntry` type from `Pick<HistoryEntry, "confidence">` to `Pick<HistoryEntry, "confidence" | "reasoningSteps">` (empty-state stub now carries `reasoningSteps: null`); (e) replaced hardcoded "ROAS threshold 3.5/3.8/72h" italic mock in the Detailed Explanation panel with a conditional render: ordered list of `{step}: {insight}` items when `activeEntry.reasoningSteps` is non-empty, else falls back to the original ROAS mock copy. (f) updated MOCKED-DEFERRED comment block at top of file: removed "Detailed Explanation panel" + "Confidence Score per entry" (the latter was already wired post-#34 but the comment was stale); kept Growth Suggestion card + Quick Stats aside + Trend bar chart as the genuinely remaining mocks; added a WIRED section explicitly documenting #34/#37 wiring. **UX delta:** prior to #37, every operator viewing the AI Decision Insights aside saw the same hardcoded ROAS threshold paragraph regardless of which entry was expanded; post-#37, when the active entry has a linked ai_decision (auto-fire chain populated `ai_decision_id`), the panel renders the actual numbered reasoning chain that the AI produced for that decision (e.g., "1. Signal detection: ROAS dropped below 2.0 threshold over 48h. 2. Cause attribution: creative fatigue on top 3 ads. 3. Action selection: pause underperforming ads to protect spend efficiency."). Manual runs (no `ai_decision_id`) and pre-AI-contract rows continue to show the mock fallback. Closes another item from the #22 mocked-deferred list using the data infrastructure that already exists. **Closed-loop product promise (CLAUDE.md §1) now exposed end-to-end:** Data (Phase 2 sync) → Insight (Phase 3 AI decision validated through aiValidator.ts with reasoning_steps) → Decision (canonical ai_decisions row with category + confidence + reasoning_steps) → Action (Phase 6 auto-fire chain via execute-ai-decision.ts → evaluateRulesForAIDecision → executeAction) → Result (automation_runs row joined back) → Learning visibility (history page renders all 4 surfaces: category + confidence + reasoning_steps + run status). **Tracing pass conducted BEFORE selecting work** per CONTINUOUS EXECUTION POLICY: enumerated remaining MOCKED-DEFERRED items in history page; identified Detailed Explanation as the one whose deferral premise had been resolved by prior continuations; verified `reasoning_steps` JSONB is persisted on every `ai_decisions` row (aiValidator min-length-1 invariant); verified pattern matches #24/#34 (no new abstraction). **Verification:** backend `tsc --noEmit` → 0 errors. Frontend `tsc --noEmit` → 0 errors. **Contamination analysis:** ZERO migration; ZERO route mounted/unmounted; ZERO 503 gate change; ZERO Inngest registration change; ZERO new phase unlocked; ZERO deferred surface activated; ZERO new abstraction; ZERO new dependency; ZERO contract change (AI Output Contract was already producing reasoning_steps; this just consumes it). ZERO behavior change for the auto-fire chain itself (purely additive read-side enrichment). **Preserved verbatim:** all HARD LOCK invariants; CANONICAL AI SYSTEM resolution (legacy `decisions` table still DEPRECATED+DEFERRED); ApiError public API; formatErrorMessage helper (#35); all closed-slice classifications Phase 0/1/2/3-linear/4-min/4-Part2/5/6 Sub-A/B/C/7 Sub-A0-D/#22→#36; Phase X broader LOCK; 4 Phase 6 deferred mocked-shells (not touched); `/decisions/*` + `/alerts/*` 503 gates; single-writer backend; org-isolation; canonical envelope; request_id correlator chain. **Files changed:** `backend/src/routes/v1/automation.ts` (+9 LOC governance comment / -2/+2 SELECT extension), `app/automation/history/page.tsx` (~+38/-15 across type extension + mapping + render). Plus `specs/SYSTEM_CONTROL.md` governance metadata. **Operator deploy prerequisites:** backend redeploy (PostgREST SELECT scope change) + frontend redeploy (consumer rendering). PostgREST hot-reloads automatically; no DB migration required. **Remaining tracked-followups in CURRENT EXECUTION TARGET:** items N (STRIPE_SECRET_KEY + Phase 2 OAuth startup-fail-fast — operator decision); O (Meta `*_budget` Vault dispatch — operator decision); Phase 2/Phase 5 Inngest registration (#30); operator-side 28 spec files uncommitted; `database-migrations.md` canonical-contract conflict; Phase X broader / Phase 8 / 4 deferred Phase 6 mocked-shells (locked phase unlock required). **Adjacent natural-continuation candidates surfaced** (for next-turn execution): (a) GET `/api/v1/history/:id` consumer wiring for expanded history panel — backend returns full `decision_history` row with `data_used` + `ai_explanation`; would enrich the per-entry expanded section (currently shows only trigger / dataUsed-stub / resultDetail); (b) enrich the expanded per-entry section with category badge + confidence pill mirroring the aside panel (currently those signals only appear in the collapsed-row trigger string and the aside); (c) Growth Suggestion card in AI Decision Insights aside remains mocked (no per-run recommendation endpoint; AI Output Contract is reasoning, not prescriptive next-action) — converting this would require new contract surface OR mapping reasoning_steps[last].insight as a heuristic suggestion (low-confidence; not recommended without operator decision).

2026-05-12 (continuation #36) — formatErrorMessage SIBLING ROLLOUT across all remaining FE consumer files per CONTINUATION BATCHING RULE ("Stopping after only one safe continuation while adjacent sibling continuations remain implementation-ready is considered premature exhaustion"). **Sibling identification**: #35 added `formatErrorMessage` helper + applied in `app/settings/billing/page.tsx` (4 sites) but explicitly surfaced 12 remaining sibling sites across 7 FE consumer files as candidates for next-turn execution. Per the new rule those siblings should have been batched with #35 — corrected this turn. **Files updated (7 files, 12 catch sites)**: (1) `app/decisions/[id]/page.tsx` — 1 catch site; replaced `err instanceof ApiError ? err.message : (err as Error).message ?? "Execution failed"` pattern with `formatErrorMessage(err, "Execution failed")`; (2) `app/dashboard/overview/page.tsx` — 2 catch sites (metrics summary + campaigns list); preserved the 404→empty-campaigns special-case fallthrough at the second site; (3) `app/integrations/page.tsx` — 3 catch sites (load + connect-start + sync-trigger); preserved the special-case 409 conflict friendly text at the sync-trigger site (non-409 path now formatted via helper); skipped a 4th `catch {}` no-binding site at handleDisconnect (no err captured; not enrichable); (4) `app/campaigns/[id]/page.tsx` — 3 catch sites (load + ai-suggestions + push); (5) `app/campaigns/page.tsx` — 1 catch site (list load); (6) `app/campaigns/create/page.tsx` — 1 catch site (draft save); (7) `app/automation/history/page.tsx` — 1 catch site (history load). Each conversion: import update (+ `formatErrorMessage`) + per-site replacement of `const e = err as ApiError | Error; setError(e.message || "fallback")` (or equivalent narrowing pattern) with `setError(formatErrorMessage(err, "fallback"))`. **Backwards-compatibility**: every site preserves its specific fallback message string (helper accepts `fallback` parameter for unknown-type case); ApiError 404→empty-state special-case at dashboard preserved; ApiError 409→friendly-text special-case at integrations preserved; non-ApiError exceptions still formatted correctly. **Universal coverage delta**: PRIOR — only billing page (post-#35) surfaced ApiError.requestId in user-visible error display; other consumers showed `e.message` raw. POST-#36 — 100% of FE consumer error displays uniformly surface ApiError.requestId via `formatErrorMessage`, enabling operator-side log pivot via `[req]/[err]/[exec]/[AI]` correlator chain from ANY error in the app. **Tracing pass conducted BEFORE selecting work** per CONTINUOUS EXECUTION POLICY mandatory tracing AND CONTINUATION BATCHING RULE sibling identification: enumerated 12 catch sites across 7 files; verified all qualify as siblings (same runtime surface = FE error display; same architectural scope = catch + display; same contract = formatErrorMessage helper from #35; no operator decision; minimal-diff individually + collectively). **Verification**: frontend `tsc --noEmit` → 0 errors. Backend not touched. **Contamination analysis**: ZERO backend code change; ZERO migration; ZERO route mounted/unmounted; ZERO 503 gate change; ZERO Inngest registration change; ZERO new phase unlocked; ZERO deferred surface activated; ZERO new abstraction (helper from #35); ZERO new dependency; ZERO behavior change for the success-path (only error-display text format enriched). **Preserved verbatim**: all HARD LOCK invariants; ApiError public API; CANONICAL AI SYSTEM resolution; all closed-slice classifications Phase 0/1/2/3-linear/4-min/4-Part2/5/6 Sub-A/B/C/7 Sub-A0-D/#22→#35; Phase X broader LOCK; 4 Phase 6 deferred mocked-shells (not touched); `/decisions/*` + `/alerts/*` 503 gates; single-writer backend; org-isolation; canonical envelope; request_id correlator chain (now surfaced uniformly to UI across all FE consumer error displays). **Files changed**: `app/decisions/[id]/page.tsx` (+2/-2), `app/dashboard/overview/page.tsx` (+4/-3), `app/integrations/page.tsx` (+8/-7), `app/campaigns/[id]/page.tsx` (+6/-9), `app/campaigns/page.tsx` (+2/-2), `app/campaigns/create/page.tsx` (+2/-2), `app/automation/history/page.tsx` (+2/-2). Plus `specs/SYSTEM_CONTROL.md` governance metadata. **Operator deploy prerequisites**: frontend redeploy only. **Remaining tracked-followups in CURRENT EXECUTION TARGET**: items N (STRIPE_SECRET_KEY + Phase 2 OAuth startup-fail-fast — operator decision); O (Meta `*_budget` Vault dispatch — operator decision); Phase 2/Phase 5 Inngest registration (newly surfaced #30); operator-side 28 spec files uncommitted; `database-migrations.md` canonical-contract conflict; Phase X broader / Phase 8 / 4 deferred Phase 6 mocked-shells (locked phase unlock required). **Adjacent natural-continuation candidates surfaced** (for next-turn execution): (a) GET `/api/v1/history/:id` consumer wiring for expanded history panel — currently no FE consumer; backend returns full decision_history row with `data_used` + `ai_explanation`; would substantively enrich operator audit view; (b) enrich expanded history panel detail with category + confidence beyond trigger string (currently only in collapsed trigger).

2026-05-12 (continuation #35) — FE UX-display wiring of #34's ApiError.requestId surfacing. Operator-authorized under IMPLEMENTATION EXECUTION MANDATE ("Implementation-ready continuations SHOULD be executed immediately whenever safely possible") + ADJACENT CONTINUATION AUTHORITY ("connecting already-existing backend/frontend surfaces"). **Background:** #34 extended `ApiError` to capture `request_id` from canonical envelope; the field is consumer-accessible via `err.requestId` but no FE consumer was surfacing it in displayed error text. Surfaced at #34 close as a candidate continuation; executed this turn per the mandate. **Approach selection:** mass-updating all 13 catch sites across 6 FE files would qualify as broad refactor (violates "no broad refactor" rule) AND multiple sites had divergent narrowing patterns (`as ApiError | Error` vs `instanceof Error` vs `instanceof ApiError ? : `). Chosen: add reusable `formatErrorMessage(err, fallback)` helper in `lib/api-client.ts` + apply in cohesive-scope (1 file, 4 catch sites in `app/settings/billing/page.tsx`). Other FE consumers can opt-in at their natural-continuation pace; no urgent mass-update needed since the architectural enablement (#34 ApiError.requestId field) is universal. **Code changes (2 files):** (1) `lib/api-client.ts` — added `formatErrorMessage(err: unknown, fallback?: string): string` export. Returns "(message) (request abc12345…)" for ApiError-with-requestId; "(message)" for ApiError-without; `err.message || fallback` for generic Error; fallback for unknown. requestId truncated to 8 chars for display readability; full requestId remains accessible via `(err as ApiError).requestId` for support / log-pivot use cases. (2) `app/settings/billing/page.tsx` — imported helper; replaced 4 catch-block patterns (`const e = err as ApiError | Error; setError(e.message || "...")`) with single-line `setError(formatErrorMessage(err, "..."))`. Cohesive scope (single page; all 4 catch blocks in the same component); minimal-diff per site (-3/+2 LOC each); semantic equivalence preserved (fallback message + message-source-priority unchanged). **Backwards-compatibility:** ApiError constructor signature preserved (request_id parameter remains optional with `= null` default); existing consumers reading `err.message` directly continue to work; new helper is opt-in. **UX delta (billing page only this pass):** prior to #35, billing-page errors displayed only the friendly message text (e.g., "Server error — try again in a few moments"); post-#35, errors now display with the backend request_id suffix when available (e.g., "Server error — try again in a few moments (request abc12345…)"). Operators reporting billing-page errors can quote the request_id directly from the displayed text, enabling backend [req] log lookup. Other FE consumers remain unchanged this pass (no behavior change there). **Tracing pass conducted BEFORE selecting work** per CONTINUOUS EXECUTION POLICY mandatory tracing: enumerated 13 catch sites across 6 FE files; verified mass-update would constitute broad refactor; selected cohesive single-file scope as the minimal-diff implementation continuation. **Verification:** frontend `tsc --noEmit` → 0 errors. Backend not touched. **Contamination analysis:** ZERO backend code change; ZERO migration; ZERO route mounted/unmounted; ZERO 503 gate change; ZERO Inngest registration change; ZERO new phase unlocked; ZERO deferred surface activated; ZERO speculative infrastructure (helper has 4 immediate uses; not unused-export). **Preserved verbatim:** all HARD LOCK invariants; ApiError public API (constructor signature backwards-compat); CANONICAL AI SYSTEM resolution; all closed-slice classifications Phase 0/1/2/3-linear/4-min/4-Part2/5/6 Sub-A/B/C/7 Sub-A0-D/#22→#34; Phase X broader LOCK; 4 Phase 6 deferred mocked-shells; `/decisions/*` + `/alerts/*` 503 gates; single-writer backend; org-isolation; canonical envelope; request_id correlator chain (now extended one more layer to user-facing UI via formatErrorMessage). **Files changed:** `lib/api-client.ts` (+30 LOC for helper function block), `app/settings/billing/page.tsx` (4 catch sites, -12/+8 LOC + 1 import update). Plus `specs/SYSTEM_CONTROL.md` governance metadata. **Operator deploy prerequisites:** frontend redeploy only. **Remaining tracked-followups in CURRENT EXECUTION TARGET:** items N (STRIPE_SECRET_KEY + Phase 2 OAuth startup-fail-fast — operator decision); O (Meta `*_budget` Vault dispatch — operator decision); Phase 2/Phase 5 Inngest registration (newly surfaced #30); operator-side 28 spec files uncommitted; `database-migrations.md` canonical-contract conflict; Phase X broader / Phase 8 / 4 deferred Phase 6 mocked-shells (locked phase unlock required). **Adjacent natural-continuation candidates surfaced** (for next-turn execution per IMPLEMENTATION EXECUTION MANDATE): (a) adopt `formatErrorMessage` in remaining 5 FE consumer files (~9 catch sites) — natural rollout; (b) extend GET /api/v1/history/:id consumer for the wired history page expansion section (currently no consumer; backend returns full row with data_used + ai_explanation); (c) enrich expanded history panel with category + confidence beyond the trigger string (currently only in collapsed view).

2026-05-12 (continuation #34) — Batched implementation continuation per EXECUTION PRIORITY OVERRIDE ("MUST prioritize implementation continuation over governance-comment cleanup"). Two parallel chains completed: (A) **Confidence display gap CLOSURE** — closes the #22-explicitly-tagged mocked-deferred confidence preservation. Backend: `routes/v1/automation.ts:399` SELECT extended from `'ai_decisions(category)'` to `'ai_decisions(category, confidence_score)'` (single-token additive change; same pattern as #24 visibility bridge). FE: `app/automation/history/page.tsx` extended `ApiAutomationRun.ai_decisions` interface shape to include `confidence_score: number | null`; `mapRunToEntry()` now extracts `run.ai_decisions?.confidence_score` and converts NUMERIC 0-1 → 0-100 percentage (preserves Path 3 confidence_score schema convention); removed the stale "Confidence not yet on automation_runs JOIN" comment that the #22 deferral noted. (B) **ApiError request_id surfacing** — closes the FE observability gap surfaced at #33. `lib/api-client.ts`: extended `ApiError` constructor with optional `requestId: string | null = null` field; updated `isCanonicalEnvelope` type guard to include `request_id`; error path now captures `body.request_id` from canonical envelope and threads it to `ApiError`. Defense-in-depth: requestId is null for legacy/non-canonical responses + connection-failure path. Operators can now `console.log(err.requestId)` in any catch block to obtain the backend [req]/[err]/[exec]/[AI] correlator. **Both chains qualify under ADJACENT CONTINUATION AUTHORITY**: (a) "expanding existing canonical APIs" (backend SELECT extension), (b) "wiring already-existing contracts" (FE consuming what backend returns), (c) "connecting already-existing backend/frontend surfaces" (request_id was always in canonical envelope; just not surfaced to ApiError). **No NEW mocked-deferred surface wired** — both consume data the backend already returns OR extend an already-thrown error class. **Tracing pass conducted BEFORE selecting work** per CONTINUOUS EXECUTION POLICY mandatory tracing: verified backend SELECT returns vs FE consumes mismatch (confidence_score); verified ApiError class vs error-path body access mismatch (request_id). **Files changed (3 files):** `backend/src/routes/v1/automation.ts` (+8/-4 — SELECT token + governance comment), `app/automation/history/page.tsx` (+11/-8 — interface + mapper + comment refresh), `lib/api-client.ts` (+10/-3 — ApiError constructor + envelope type guard + error-path capture). Plus `specs/SYSTEM_CONTROL.md` governance metadata. **Verification:** backend `tsc --noEmit` → 0 errors; frontend `tsc --noEmit` → 0 errors. **Contamination analysis:** ZERO migration; ZERO route mounted/unmounted; ZERO 503 gate change; ZERO Inngest registration change; ZERO new phase unlocked; ZERO deferred surface activated; ZERO new abstraction; ZERO new dependency. **Preserved verbatim:** all HARD LOCK invariants; CANONICAL AI SYSTEM resolution; all closed-slice classifications Phase 0/1/2/3-linear/4-min/4-Part2/5/6 Sub-A/B/C/7 Sub-A0-D/#22→#33; Phase X broader LOCK; 4 Phase 6 deferred mocked-shells (this turn's history page is wired Phase 6 surface from #22 — NOT one of the 4 deferred); legacy `decisions` table DEPRECATED+DEFERRED state; single-writer backend; org-isolation; canonical envelope; request_id correlator chain (now extended one layer further to FE ApiError consumers). **Closed-loop UX delta:** PRIOR — operators saw category in history feed (post-#33) but confidence was hardcoded 0 per #22 mocked-deferred; FE errors threw with status+message but no request_id correlation to backend logs. POST-#34 — confidence displayed alongside category as auto-fired-rule trigger signal (e.g., "ROAS_DROP · Rule abc12345… · Confidence 94"); operators can quote `err.requestId` when reporting errors to support, enabling backend log pivot. **Remaining tracked-followups in CURRENT EXECUTION TARGET:** items N (STRIPE_SECRET_KEY + Phase 2 OAuth startup-fail-fast — operator decision); O (Meta `*_budget` Vault dispatch — operator decision); Phase 2/Phase 5 Inngest registration (newly surfaced #30); operator-side 28 spec files uncommitted; `database-migrations.md` canonical-contract conflict; Phase X broader / Phase 8 / 4 deferred Phase 6 mocked-shells (locked phase unlock required).

2026-05-12 (continuation #33) — FE category consumption of #24 Path F visibility bridge (`app/automation/history/page.tsx`). Operator-authorized under ADJACENT CONTINUATION AUTHORITY ("completing partially-implemented execution chains" + "wiring already-existing contracts" + "connecting already-existing backend/frontend surfaces") + CONTINUOUS EXECUTION POLICY's explicit guidance to "prefer implementation continuation over governance-only maintenance whenever runtime-safe adjacent implementation exists". **Runtime evidence (partially-implemented chain):** #24 (2026-05-09 Path F) added `ai_decisions(category)` to `GET /api/v1/automation/runs` response via PostgREST nested-select on the existing FK `automation_runs.ai_decision_id → ai_decisions(id)`. The data has been returned from backend since #24 deploy. However, `app/automation/history/page.tsx` `ApiAutomationRun` interface (wired at #22) did NOT include the `ai_decisions` join field — backend ahead of frontend. The page-internal comment at line 125 even acknowledged the gap: "until ai_decisions JOIN lands in a future pass" — STALE post-#24 (the JOIN already landed; the FE just didn't consume it). **FE-side chain completion this pass (1 file, +18/-8):** (1) extended `ApiAutomationRun` interface with `ai_decisions?: { category: string | null } | null` field (matches backend JOIN shape exactly); (2) added `category: string | null` field to `HistoryEntry` interface for FE-internal pass-through; (3) extended `mapRunToEntry()` to extract `run.ai_decisions?.category ?? null` and prepend it to the trigger display when both category AND a rule-based run are present (format: "ROAS_DROP · Rule abc12345…"; falls back to original "Rule abc12345…" / "Manual run" formats when category is null or run is manual — preserves all existing display semantics); (4) updated stale page-internal comments to reflect the chain completion (category JOIN consumed; confidence_score JOIN remains a separate hardening pass per #22 mocked-deferred classification). **Closed-loop UX-visibility delta:** PRIOR state — operators could see auto-fire activity via backend JSON inspection of `GET /automation/runs` but the history page UI showed only generic "Rule abc12345…" labels; categorical signal that fired the rule was invisible at the UI layer. POST-#33 state — operators see the AI-decision-derived categorical trigger (ROAS_DROP / SPEND_SPIKE / CONVERSION_DROP / SCALING_OPPORTUNITY / etc.) directly in the history feed, completing the auto-fire chain end-to-end as a single coherent operator-facing UX (AI decision → category emission → rule match → auto-fire → audit row → category visible in UI). **No NEW mocked-deferred surface wired** — this consumes data the backend already returns; the FE surface (history page entries) was already wired at #22 to render automation_run rows; this pass extends the rendering with one additional field. Confidence_score JOIN remains a separate hardening pass per #22 + #24 governance documentation. **Truthful UX preservation:** category display is conditional — non-null category from non-manual run shows the categorical prefix; manual runs OR null-category runs preserve the original "Rule abc12345…" / "Manual run" labels. NO fake category values; NO speculative confidence; NO redesign of preserved mocked-deferred sections (AI Decision Insights aside, Quick Stats aside, Detailed Explanation panel, Trend bar chart — all #22 preservations intact). **Verification:** frontend `tsc --noEmit` → 0 errors. Backend not touched. **Contamination analysis:** ZERO backend code change (consumer-only on backend data that's been returned since #24); ZERO migration; ZERO route mounted/unmounted; ZERO 503 gate change; ZERO Inngest registration change; ZERO new phase unlocked; ZERO deferred surface activated; ZERO new abstraction. **Preserved verbatim:** all HARD LOCK invariants; closed-slice invariants Phase 0/1/2/3-linear/4-min/4-Part2/5/6 Sub-A/B/C/7 Sub-A0-D/#22→#32; Phase X broader LOCK; 4 Phase 6 deferred mocked-shells (this page is wired Phase 6 surface from #22 — NOT one of the 4 deferred); legacy `decisions` table DEPRECATED+DEFERRED state; single-writer backend; org-isolation; canonical envelope; request_id correlator chain. **Files changed:** `app/automation/history/page.tsx` (+18/-8 — interface extension + mapper update + 2 comment refreshes), `specs/SYSTEM_CONTROL.md` (governance metadata). **Operator deploy prerequisites:** frontend redeploy only. **Remaining tracked-followups in CURRENT EXECUTION TARGET:** items N (STRIPE_SECRET_KEY + Phase 2 OAuth startup-fail-fast — operator decision); O (Meta `*_budget` Vault dispatch — operator decision); Phase 2/Phase 5 Inngest registration (newly surfaced #30); operator-side 28 spec files uncommitted; `database-migrations.md` canonical-contract conflict; Phase X broader / Phase 8 / 4 deferred Phase 6 mocked-shells (locked phase unlock required). **Possible adjacent natural-continuation candidates surfaced this turn** (for next operator decision): (a) extend `GET /automation/runs` backend SELECT to also include `ai_decisions(confidence_score)` — same pattern as #24 — would close the confidence display gap acknowledged at #22; (b) wire `request_id` to FE error display for operator debugging — apiClient receives it but doesn't surface it to consumers.

2026-05-12 (continuation #32) — Post-#31 documentation hygiene + HARDENED-BUT-UNUSED governance headers. Operator-authorized under continuous-execution + SAFE GOVERNANCE FIXES rules ("improve runtime/spec consistency" + "improve single-source-of-truth enforcement" + "remove ambiguity" + "preserve existing architecture"). **Discovered after #31:** `lib/api-client.ts` comment block at lines 23-28 + lines 76-79 described "Phase 2 legacy shapes (...) preserves PHASE2_ENVELOPE_FOLLOWUP deferral state" — STALE post-#31 (item M is now RESOLVED; Phase 2 routes emit canonical envelope). ALSO discovered: `alerts.ts` + `decisions.ts` route handlers (both 503-gated; classified HARDENED BUT UNUSED per implementation classification model) had NO governance comment explaining why their legacy `c.json({error:'...'})` shapes are preserved verbatim. Future maintainers could mistakenly "canonicalize" these routes for envelope consistency, touching unreachable code AND creating confusion about whether the routers are active. **Files changed (comment-only):** (1) `lib/api-client.ts` — refreshed both comment blocks to reflect 12-router canonical-envelope coverage post-#31, noted legacy-shape detection retained as defense-in-depth; (2) `backend/src/routes/v1/alerts.ts` — added 30-line governance header block classifying as HARDENED BUT UNUSED + cross-referencing the 503 gate at routes/v1/index.ts:91 + documenting the canonical AI SYSTEM resolution + listing the 4-step unlock procedure should Phase 3 anomaly engine ever be re-authorized; (3) `backend/src/routes/v1/decisions.ts` — same governance header pattern + cross-referencing both routes/v1/index.ts:90 503 gate AND continuation #29 defense-in-depth guard at jobs/inngest.ts (generateDecisions early-return); 6-step unlock procedure documented; (4) `specs/SYSTEM_CONTROL.md` — CURRENT EXECUTION TARGET closure-enum extended with continuation #32 + this log entry added. **Verification:** backend `tsc --noEmit` → 0 errors; frontend `tsc --noEmit` → 0 errors. **Contamination analysis:** ZERO code-behavior change (pure comment + governance-header additions); ZERO migration; ZERO route mounted/unmounted; ZERO 503 gate change; ZERO Inngest registration change; ZERO new phase unlocked; ZERO deferred surface activated; ZERO new abstraction; ZERO new dependency. **Preserved verbatim:** all HARD LOCK invariants; ACTION_HANDLERS dispatch; resolveMetaAccessToken; auto-fire hook (#23); Path F category (#24); all closed-slice invariants Phase 0/1/2/3-linear/4-min/4-Part2/5/6 Sub-A/B/C/7 Sub-A0-D/#22→#31; Phase X broader LOCK; 4 Phase 6 deferred mocked-shells; legacy `decisions` table DEPRECATED+DEFERRED state; single-writer backend; org-isolation; canonical envelope coverage; request_id correlator chain. **Files changed:** `lib/api-client.ts` (comments only ~10 lines refreshed), `backend/src/routes/v1/alerts.ts` (+30 lines doc-comment), `backend/src/routes/v1/decisions.ts` (+45 lines doc-comment), `specs/SYSTEM_CONTROL.md` (governance metadata). **Active hardened surface envelope coverage:** 12 routers (unchanged from #31); HARDENED BUT UNUSED inventory now explicitly documented = 2 routers (alerts, decisions) preserved per CANONICAL AI SYSTEM resolution. **Operator deploy prerequisites:** ZERO — pure documentation; no runtime change; no rebuild required. **Remaining tracked-followups in CURRENT EXECUTION TARGET:** items N (STRIPE_SECRET_KEY + Phase 2 OAuth startup-fail-fast — operator decision), O (Meta `*_budget` Vault dispatch — operator decision); Phase 2 / Phase 5 Inngest registration decisions (newly surfaced at #30); operator-side 28 spec files uncommitted; `database-migrations.md` canonical-contract conflict; Phase X broader / Phase 8 / 4 deferred Phase 6 mocked-shells (all locked-phase unlock required).

2026-05-12 (continuation #31) — PHASE2_ENVELOPE_FOLLOWUP item M RESOLUTION pass under ADJACENT CONTINUATION AUTHORITY ("wiring already-existing contracts" + "expanding existing canonical APIs" — both explicit allowed examples). **Runtime evidence pre-conversion:** Phase 2 routes (`integrations.ts` / `connect.ts` / `metrics.ts`) closed since #3 (2026-05-07) but emitted legacy envelope shapes inconsistent with the Phase 1 canonical envelope (`backend/src/utils/response.ts:ok/fail`): bare `c.json({...})` for success, `c.json({error:'...',message:'...'},status)` for failure, with no `request_id` correlation. FE apiClient detection-unwrap (#13) was specifically designed to absorb BOTH shapes — verified via `lib/api-client.ts:31-80` which gates on the `success` key. FE consumers (`app/integrations/page.tsx`, `app/dashboard/{overview,channels}/page.tsx`, `app/api/integrations/callback/[platform]/route.ts`) get identical post-unwrap results regardless of backend shape. **Conversion executed (3 files, 16 sites):** (1) `metrics.ts` — 4 sites (2 endpoints × success+400 paths; DB errors now throw → errorHandler per #4 hardening pattern); +24/-9. (2) `integrations.ts` — 5 sites (GET / DELETE / POST sync / GET sync-logs); DELETE `204 No Content` preserved as raw `Response` (canonical envelope is JSON-only; 204 has no body by HTTP spec); +20/-13. (3) `connect.ts` — 7 sites (POST start with platform/shop validation + ALREADY_CONNECTED check → 409 instead of legacy 400; POST complete with state validation + OAuth exchange + Vault storage + upsert paths; OAuth-exchange error now 502 with `OAUTH_EXCHANGE_FAILED` code instead of 500); +13/-11. **Added on every endpoint:** `request_id` field via existing `getRequestId(c)` helper; structured `code` field on every `fail()` for operator triage (`INVALID_QUERY` / `NOT_FOUND` / `NOT_CONNECTED` / `SYNC_IN_PROGRESS` / `INVALID_PLATFORM` / `MISSING_PARAMETER` / `ALREADY_CONNECTED` / `INVALID_OAUTH_STATE` / `OAUTH_EXCHANGE_FAILED` / `VAULT_STORE_FAILED` / `INTEGRATION_UPSERT_FAILED`). **Variables type extended:** added `requestId: string` to all three routers' `Variables` type so `ok()`/`fail()` can read `c.get('requestId')` — matches the established pattern from history.ts / campaigns.ts. **No status-code semantic changes beyond two improvements:** ALREADY_CONNECTED 400 → 409 (correct semantic for conflict); OAUTH_EXCHANGE_FAILED 500 → 502 (correct semantic for upstream-provider failure). All other status codes preserved verbatim. **Verification:** backend `tsc --noEmit` → 0 errors; frontend `tsc --noEmit` → 0 errors; grep verification confirms ZERO remaining legacy `c.json({error:` shapes in Phase 2 routes. **Preserved verbatim:** all HARD LOCK invariants; ACTION_HANDLERS dispatch; resolveMetaAccessToken; auto-fire hook (#23); Path F category (#24); inngest.ts function registration state; closed-slice invariants across Phase 0/1/2/3-linear/4-min/4-Part2/5/6 Sub-A/B/C/7 Sub-A0-D/#22/#23/#24/#25/#26/#27/#28/#29/#30; Phase X broader LOCK; 4 Phase 6 deferred mocked-shells; `/decisions/*` + `/alerts/*` 503 gates; legacy `decisions` table DEPRECATED+DEFERRED state; single-writer backend; org-isolation; canonical envelope coverage now EXTENDED to Phase 2 (matches Phase 4 minimal + Phase 4 Part 2 + Phase 6 + Phase 7 surface envelope-coverage); request_id correlator chain extended to Phase 2 routes. **Contamination analysis:** ZERO migration; ZERO route mounted/unmounted; ZERO 503 gate change; ZERO new phase unlocked; ZERO deferred surface activated; ZERO new abstraction; ZERO new dependency; ZERO new env var; ZERO new feature flag; ZERO Inngest registration change. **Files changed:** `backend/src/routes/v1/metrics.ts` (+24/-9), `backend/src/routes/v1/integrations.ts` (+20/-13), `backend/src/routes/v1/connect.ts` (+13/-11), `specs/SYSTEM_CONTROL.md` (governance metadata). **Active hardened surface envelope coverage delta:** 9 routers → 12 routers (3 Phase 2 routes now canonicalized; total = auth / integrations / connect / metrics / actions / history / automation / brand-kit / creatives / campaigns / ai / billing — all on canonical envelope). **Operator deploy prerequisites:** backend redeploy only; NO migration, NO env changes, NO frontend redeploy required. **Remaining tracked-followups in CURRENT EXECUTION TARGET:** items N (STRIPE_SECRET_KEY + Phase 2 OAuth startup-fail-fast — semantic startup behavior change; operator decision), O (Meta `*_budget` Vault dispatch semantic gap — explicitly out-of-scope per #27 operator instruction). Newly-surfaced decisions from #30 still pending: Phase 2 / Phase 5 Inngest function registration re-enable decisions. **Operator-side spec authoring still in flight** (28 spec files +14,829 lines uncommitted); these items continue to remain blocked on operator commit.

2026-05-12 (continuation #30) — Self-correction of #29's mischaracterized runtime claim + inngest.ts phase-classification sync. Per SAFE GOVERNANCE FIXES rule ("improve single-source-of-truth enforcement" + "remove ambiguity") + RUNTIME EVIDENCE OVERRIDES ASSUMPTIONS rule. **Discovered after #29:** the `functions = [...]` registration array at `backend/src/jobs/inngest.ts:323-329` has `dailySyncAll`, `syncIntegration`, `generateDecisions`, `generateCreative` ALL commented out — only `aiHello` is registered. So the `generateDecisions` Inngest receiver was NEVER invoked in production; #29's claim that "every production Phase 2 sync silently triggered a 42P01" was based on an incorrect assumption that the receiver was registered. Runtime evidence reveals no 42P01 was occurring because the function never executed. **#29's early-return guard is still architecturally valid** as defense-in-depth (activates if/when the receiver is ever re-registered by operator decision), but the SYSTEM_CONTROL.md entry mis-described the runtime state and must be corrected to preserve governance-doc accuracy. **Doc corrections this turn (2 files):** (1) `specs/SYSTEM_CONTROL.md` CURRENT EXECUTION TARGET closure-enum line updated to clarify #29's defense-in-depth nature (Phase 3 anomaly Inngest-receiver guard added as DiD; no actual production 42P01 was silenced because the receiver was not registered); (2) `backend/src/jobs/inngest.ts` comment block at lines 305-322 — updated stale Phase 2 (still labeled "deferred" despite CLOSED at #3) + Phase 5 (still labeled "deferred" despite CLOSED at #7) classifications to current SYSTEM_CONTROL.md state; clarified that `dailySyncAll` + `syncIntegration` registration remains paused pending operator decision (not because Phase 2 is deferred); same for `generateCreative` (Phase 5 CLOSED but Inngest path paused); cross-referenced #29 for `generateDecisions`; clarified `aiHello` smoke-test flag gate. **Verification:** backend `tsc --noEmit` → 0 errors. **Contamination analysis:** ZERO code-behavior change (comment-only edit on inngest.ts + governance-meta edit on SYSTEM_CONTROL.md); ZERO migration; ZERO route mounted/unmounted; ZERO function registered/unregistered; ZERO 503 gate change; ZERO new phase unlocked; ZERO deferred surface activated. **Preserved verbatim:** all HARD LOCK invariants; CANONICAL AI SYSTEM resolution; all closed-slice classifications; Phase X broader LOCK; 4 Phase 6 deferred mocked-shells; `/decisions/*` + `/alerts/*` 503 gates; single-writer backend; org-isolation; canonical envelope; request_id correlator chain; #29 defense-in-depth guard preserved verbatim. **Files changed:** `backend/src/jobs/inngest.ts` (comment block ~20 lines updated), `specs/SYSTEM_CONTROL.md` (governance metadata). **Governance lesson recorded:** when authoring continuation entries, runtime evidence about Inngest function REGISTRATION (the `functions = [...]` export array) must be verified separately from function DEFINITION presence (the `inngest.createFunction(...)` call). A defined function that isn't in the registration array never executes regardless of trigger events emitted upstream. Future continuations involving Inngest functions should `grep -E "export const functions = \[" jobs/inngest.ts` before claiming a function is "active". **Remaining tracked-followups in CURRENT EXECUTION TARGET:** items M (PHASE2_ENVELOPE_FOLLOWUP), N (STRIPE_SECRET_KEY + Phase 2 OAuth startup-fail-fast), O (Meta `*_budget` Vault dispatch semantic gap). All require operator authorization. **Newly-surfaced operator-decision items** (NOT my decisions; flagged for operator awareness): (a) Phase 2 (CLOSED) Inngest-driven sync path is registered-paused — Phase 2 routes emit events but no receiver handles them; operator should decide whether to re-register `syncIntegration` + `dailySyncAll` or formally adopt the synchronous request path as the canonical Phase 2 sync surface; (b) Phase 5 (CLOSED) generateCreative Inngest registration similarly paused — operator should decide same question for creative generation; (c) operator-side spec authoring still in flight (28 spec files +14,829 lines uncommitted) — these items remain blocked on operator commit.

2026-05-12 (continuation #29) — ORPHAN/ZOMBIE ISOLATION pass for Phase 3 anomaly engine Inngest-trigger chain. Operator-authorized under continuous-execution policy + SAFE GOVERNANCE FIXES rule ("remove orphan/zombie systems" + "improve single-source-of-truth enforcement" + "improve execution determinism"). **Runtime evidence before fix:** `jobs/inngest.ts:114` (Phase 2 closed `syncIntegration` function) emits `intelligence/decisions.requested` event after every successful Phase 2 sync. Receiver `generateDecisions` Inngest function (jobs/inngest.ts:145) called `dispatchIntelligence(orgId, 'sync_complete')` → `services/intelligence/index.ts:13` → `INSERT INTO decision_runs (...)`. **`decision_runs` table does NOT exist in canonical migrations** (verified empty `grep -E "CREATE TABLE.*decision_runs" /supabase/migrations/*.sql`; documented in Phase 4 Part 2 spec-vs-runtime adaptation log). Result before fix: every production Phase 2 sync silently triggered a 42P01 (relation does not exist) Inngest job failure — operator dashboard noise, no data corruption, latent execution-determinism violation. The full chain (decision-generator.ts writes to legacy `decisions` table per CANONICAL AI SYSTEM resolution = DEPRECATED+DEFERRED) was governance-correctly classified as ORPHAN/ZOMBIE in audit #28 + audit-of-#28; the wiring had remained live. **Code (1 file, +50/-0):** `backend/src/jobs/inngest.ts` — added early-return guard at top of `generateDecisions` Inngest function body. Guard: (a) emits structured `[inngest]` log line so operators see explicit deferral rather than 42P01 stack traces; (b) returns `{skipped: true, reason: 'phase3_anomaly_deprecated'}`; (c) preserves the dispatchIntelligence call below the early-return as UNREACHABLE source so spec conformance + Phase 3 anomaly engine revival remain possible without re-authoring. **Files preserved verbatim:** `services/intelligence/index.ts`, `services/intelligence/decision-generator.ts`, `services/intelligence/anomaly-detection.ts`, `services/intelligence/alert-detection.ts`, `routes/v1/decisions.ts` (still 503-gated), `syncIntegration` Phase 2 closed surface (emitter untouched), every other Inngest function. **Re-enablement procedure documented inline** (deploy decision_runs migration + rebuild decisions table + update CANONICAL AI SYSTEM resolution + remove guard). **Verification:** backend `tsc --noEmit` → 0 errors. **Preserved verbatim:** all HARD LOCK invariants; CANONICAL AI SYSTEM resolution; all closed-slice invariants (Phase 0/1/2/3-linear/4-min/4-Part2/5/Phase 6 Sub-A/B/C/Phase 7 Sub-A0/A0.5/A1a/A1b/A1c/B/D/#22/#23/#24/#25/#26/#27/#28); Phase X broader LOCK; 4 Phase 6 deferred mocked-shells; `/decisions/*` + `/alerts/*` 503 gates; legacy `decisions` table DEPRECATED+DEFERRED state; single-writer backend; org-isolation; canonical envelope; request_id correlator chain. **Contamination analysis:** ZERO migration; ZERO route mounted/unmounted; ZERO 503 gate change; ZERO new phase unlocked; ZERO deferred surface activated; ZERO new abstraction; ZERO Phase 2 closed-surface modification (only the Phase 3 receiver guarded). **Files changed:** `backend/src/jobs/inngest.ts` (+50/-0), `specs/SYSTEM_CONTROL.md` (governance metadata). **Execution-determinism delta:** prior state — every Phase 2 sync produced silent Inngest 42P01 failure. Post-#29 — Phase 2 sync still triggers the event; receiver early-returns cleanly with operator-visible structured log; zero noise; zero 42P01. Runtime correctness now matches CANONICAL AI SYSTEM resolution (Phase 3 anomaly = DEPRECATED+DEFERRED = no executes). **Operator visibility:** operators can grep `[inngest] generate-decisions early-return:` to see all events that would have triggered the orphan chain. **Remaining tracked-followups in CURRENT EXECUTION TARGET:** items M (PHASE2_ENVELOPE_FOLLOWUP), N (STRIPE_SECRET_KEY + Phase 2 OAuth startup-fail-fast), O (Meta `*_budget` Vault dispatch semantic gap) — all require operator authorization per BLOCKER TRIAGE RULE. **Operator-side spec authoring in flight:** 28 spec files (~14,829 lines) expanded by operator from sketch-grade to implementation-grade; uncommitted in working tree; addresses audit blockers B7 (7 of 11 sketches now expanded) + B1 (mcp-orchestration.md filename canonicalized). Governance-meta sync of these blockers DEFERRED to operator commit of the spec expansion work to avoid sync to a moving target.

2026-05-12 (continuation #28) — Item P observability resolution + governance-meta sync. **Code (1 file, ~12 LOC):** `backend/src/services/ai/execute-ai-decision.ts` — extended #23 hook's `.then()` success branch with a symmetric `console.log` that emits `[AI] auto-fire automation rules fired=N ai_decision_id=X org_id=Y trace_id=Z run_ids=[...]` ONLY when `result.rulesFired > 0`. Silent for no-match cases (typical; avoids log flooding for every AI decision that doesn't match a categorical rule trigger). Pure observability extension; ZERO behavior change; preserves CONSTITUTION §3 "Fail Loudly" pattern; symmetric with existing failure-branch logger that has been live since #23. **Doc (specs/SYSTEM_CONTROL.md):** (a) item P at NEXT ACTION marked RESOLVED with cross-reference; (b) CURRENT EXECUTION TARGET closure enum extended to include #26 + #27 + #28; (c) tracked-followups list updated to remove resolved items (label parity + auto-fire success log); (d) backfilled brief continuation log entries for #26 + #27 (previously referenced via item Q at NEXT ACTION but absent from log itself — single-source-of-truth fix per SAFE GOVERNANCE FIXES rule). **Verification:** backend `tsc --noEmit` → 0 errors. **Preserved verbatim:** all HARD LOCK invariants; all closed-slice classifications (Phase 0/1/2/3-linear/4-min/4-Part2/5/Phase 6 Sub-A/B/C/Phase 7 Sub-A0/A0.5/A1a/A1b/A1c/B/D/#22 webhook hardening/#23 auto-fire hook/#24 Path F/#25 reconciliation/#26+#27 label parity); Phase X broader LOCK; 4 Phase 6 deferred mocked-shell pages (per #22); legacy `decisions` table DEPRECATED+DEFERRED; `/decisions/*` + `/alerts/*` 503 gates; single-writer backend; org-isolation; canonical envelope; request_id correlator chain. **Contamination analysis:** ZERO migration; ZERO route mounted/unmounted; ZERO 503 gate change; ZERO new phase unlocked; ZERO deferred surface activated; ZERO new abstraction. **Files changed:** `backend/src/services/ai/execute-ai-decision.ts` (+12/-3), `specs/SYSTEM_CONTROL.md` (governance metadata). **Remaining tracked-followups in CURRENT EXECUTION TARGET:** items M (PHASE2_ENVELOPE_FOLLOWUP), N (STRIPE_SECRET_KEY + Phase 2 OAuth startup-fail-fast), O (Meta `*_budget` Vault dispatch semantic gap). All require operator authorization per BLOCKER TRIAGE RULE.

2026-05-09 (continuation #27) — Action-executor pre-dispatch logging-mode label widening (single-pass parity across all 7 live-capable handler dispatch tuples). Operator-authorized as Candidate α following the parity audit. **Code (1 file, +93/-2):** `backend/src/services/execution/action-executor.ts` — replaced the single-tuple `liveCandidate` conjunction at the pre-dispatch label step with a 7-disjunct OR that mirrors each `ACTION_HANDLERS.<action_type>` dispatch conjunction verbatim (meta.pause_campaign / google.pause_campaign / meta.increase_budget / meta.decrease_budget / send_alert_email / meta.create_campaign / google.create_campaign). Each disjunct has an inline comment line-pointer to the corresponding dispatch site + a parity-rule comment requiring future dispatch edits to update both sites in the same continuation. NO `isLive()` helper; NO `LIVE_DISPATCH_RULES[]` abstraction; preserves the original Phase 4 minimal per-handler explicit-conjunction architecture style + operator's explicit "remain INLINE / remain EXPLICIT" instruction. **Observability-only:** the `mode` label is consumed only by `[exec]` log entries (exec.start, exec.end) and `decision_history.data_used.mode` JSONB field — NEVER consumed by dispatch, idempotency, Vault resolution, or any execution-semantic surface. Truthful runtime mode was already preserved at `decision_history.impact_snapshot` via handler `result_data.mode='live'` since Phase 4 close; this widening corrects the pre-dispatch label so `data_used.mode` matches reality for the 6 live-capable handlers beyond pause_campaign. **Doc (specs/SYSTEM_CONTROL.md):** single-line item Q reconciliation marking RESOLVED with cross-references to #26 + #27. **Verification:** backend `tsc --noEmit` → 0 errors. **Preserved verbatim:** all HARD LOCK invariants; ACTION_HANDLERS dispatch logic; resolveMetaAccessToken; realMeta*/realGoogle* handler internals; env precedence; idempotency `(org_id, execution_id)` partial unique index; META_INCREASE_BUDGET_MAX_PERCENT cap; recipient placeholder filter; closed-slice invariants across Phase 0/1/2/3-linear/4-min/4-Part2/5/6 Sub-A/B/C/7 Sub-A0/A0.5/A1a/A1b/A1c/B/D/#22/#23/#24/#25/#26. **Contamination analysis:** ZERO migration; ZERO route change; ZERO handler dispatch change; ZERO new abstraction; ZERO env/feature-flag change. **Files changed:** `backend/src/services/execution/action-executor.ts` (+93/-2), `specs/SYSTEM_CONTROL.md` (item Q single-line). **Explicitly out-of-scope per operator instruction:** the separate semantic gap for `meta.{increase,decrease,create}_budget` Vault dispatch adoption remains tracked as item O (requires explicit future authorization tied to specs/002 Vault pattern + specs/004 execution-layer).

2026-05-09 (continuation #26) — Action-executor pre-dispatch logging-mode label correction for `meta.pause_campaign` (single-tuple env-gate divergence fix). Operator-authorized as Candidate B following the #21-tracked-followup audit. **Code (1 file, +19/-2):** `backend/src/services/execution/action-executor.ts` — removed `Boolean(META_TEST_ACCESS_TOKEN) &&` from the pre-dispatch `liveCandidate` conjunction at line ~2807. Tracked as the explicit #21 known follow-up: when #21 Part B made the dispatch logic for `pause_campaign` Vault-aware (dropping its env-gate), the pre-dispatch label was not synchronized, causing Vault-only orgs to log `mode: 'simulated'` for actually-live executions. The fix aligns the label with actual dispatch logic at ACTION_HANDLERS.pause_campaign (lines 322-340) which since #21 Part B routes to `realMetaPauseCampaign` whenever flag + allowlist match, then `resolveMetaAccessToken` tries Vault → env fallback. **Observability-only:** verified `mode` label is consumed only by `[exec]` logs + `decision_history.data_used.mode` — not by dispatch/idempotency/Vault/handler internals. NO dispatch logic changed; NO handler internals changed; NO Vault precedence changed; NO env-gate change. **Verification:** backend `tsc --noEmit` → 0 errors. **Preserved verbatim:** all HARD LOCK invariants; ACTION_HANDLERS dispatch logic; resolveMetaAccessToken (Vault-first / env-fallback / none precedence); all closed-slice invariants. **Contamination analysis:** ZERO migration; ZERO new helper; ZERO new abstraction; ZERO behavior change beyond label accuracy. **Files changed:** `backend/src/services/execution/action-executor.ts` (+19/-2). **Scope note:** corrected meta.pause_campaign ONLY; the structural narrowness of the label (only emitting `'live'` for that one tuple while 6 other live-capable handlers always logged `'simulated'`) was the broader observability gap addressed separately at continuation #27.

2026-05-09 (continuation #25) — SYSTEM_CONTROL.md governance-metadata RECONCILIATION pass (operator-authorized as Candidate A). Pure documentation; ZERO runtime code change; ZERO migration; ZERO phase unlock; ZERO surface activation. Resolves drift introduced by continuations #23 (auto-fire post-persist hook) + #24 (Path F category formalization + visibility bridge) not propagating updates downstream into governance-metadata sections. **Sections updated:** (a) Phase 4 Part 2 Status line — "Auto-firing remains DEFERRED-BY-GOVERNANCE" → "RESOLVED 2026-05-09 via #23 + #24"; (b) Phase 4 Part 2 "Governance-BLOCKED" subsection renamed to "Governance-RESOLVED" with auto-firing item marked [x] + cross-references to #23/#24; remaining `meta.*_budget` Vault migration + `process.exit(1)` softening preserved as still-blocked items; (c) Phase 4 Part 2 spec-vs-runtime adaptation log line about `decision.type → result.category` updated to record Path F formalization (column-level + JSONB shim); (d) Phase 4 Part 2 Exit Gate — auto-firing line updated to record resolution; closed-Phase-3-pipeline line updated to clarify it was structurally untouched but additively extended via #24; (e) PATCH QUEUE position #7 trailing clause updated; PATCH QUEUE position #16 added recording the Phase 4/Phase 6 BRIDGE (combined #23 + #24); (f) NEXT ACTION (STRICT) section refreshed to enumerate Phase 6 Sub-A/B/C + Phase 7 Sub-A0 → D + #23/#24 closures; "Closed since last NEXT ACTION" expanded with items J/K/L; "GOVERNANCE-LOCKED" item H marked RESOLVED inline; new tracked items M/N/O/P/Q added for follow-ups; (g) CURRENT EXECUTION TARGET (STRICT) section refreshed to reflect post-#24 closure inventory + updated DO NOT list extending governance-locks to #23/#24/Path-F-Output-Contract surfaces; (h) REAL SYSTEM CAPABILITIES header timestamp updated; AI line records optional category field; AI Logging line notes #23 infra-failure log; Auth line records #21 initial credits JIT grant; Automation Engine line records ACTIVE auto-firing via #23+#24; new "AI Output Contract Category" line added; new "Stripe Monetization" line added; Real Action Surface block consolidated (removed duplicated Phase-4-Part-2-deferred block; added `meta.create_campaign` + `google.create_campaign` from #20). **Preserved verbatim:** all HARD LOCK invariants; all phase locks; all Phase X broader (MCP / tool-governance / log-sink fan-out / strategy_tag enum) governance lock; all 4 Phase 6 deferred mocked-shell page classifications from #22; legacy `decisions` table DEPRECATED+DEFERRED state; `/decisions/*` + `/alerts/*` 503-gating; PATCH QUEUE ordering semantics (positions #1–#15 untouched; #16 appended only); PHASE2_ENVELOPE_FOLLOWUP / PHASE2_OAUTH_ENV_FAILFAST_FOLLOWUP tracked status; per-org Meta `*_budget` Vault migration tracked status. **Contamination analysis:** ZERO runtime code touched; ZERO migration touched; ZERO route mounted/unmounted; ZERO 503 gate lifted; ZERO new phase unlocked; ZERO deferred surface activated. Only `specs/SYSTEM_CONTROL.md` modified. **Verification:** all in-line "#23" / "#24" / "#25" / "Path F" cross-references resolve; auto-firing references in Phase 4 Part 2 + PATCH QUEUE + NEXT ACTION + REAL SYSTEM CAPABILITIES sections now consistent; SYSTEM_CONTROL.md is again runtime-authoritative.

2026-05-09 (continuation #24) — Path F EXECUTED + Operator-Visibility Bridge — AI Output Contract Category Formalization. Activates the dormant auto-fire hook landed in #23. Pre-implementation runtime verification confirmed: (a) `aiValidator.ts` AI Output Contract was `{type, result, confidence_score, reasoning_steps, status}` — no `category`; (b) both `routes/v1/ai.ts` systemPrompts (`/decisions/generate` + `/execute`) demanded exactly four keys and FORBADE any other ("not include … any keys other than the four above"); (c) `automation-engine.ts:extractCategory` returned null when `result.category` absent — so the #23 hook fired but no rule ever auto-matched; (d) `ai_decisions` table had no top-level category column; (e) only 4 internal read paths touch `ai_decisions` (`automation-engine` ×2, `action-executor` linkage lookup, `persistence` INSERT) — no public GET endpoint to coordinate; (f) `automation_runs.ai_decision_id` has real FK → `ai_decisions(id)` per `20260507130000_phase4_part2_automation.sql` line 89 — PostgREST nested-select available without schema change. **Selected Path F (hybrid)** over Path 2 alternatives: nullable column on ai_decisions + fallback shim in extractCategory + prompt-side category emission. Why Path F: column-level addressable (vs JSONB path traversal), backwards-compatible with any in-flight prompt tuning (fallback shim still extracts `result.category`), zero risk to historical rows (nullable; no backfill; pre-Path-F rows can never retro-trigger any rule which is the intended safety property), additive-only migration with trivial rollback (`ALTER TABLE ai_decisions DROP COLUMN category`). **Files modified: 5 backend files + 1 NEW migration + this doc.** (1) NEW `supabase/migrations/20260509130000_phase3_ai_decisions_category.sql` (~57 LOC) — `ALTER TABLE ai_decisions ADD COLUMN IF NOT EXISTS category TEXT NULL;` + partial index `ix_ai_decisions_category_org ON (org_id, category) WHERE category IS NOT NULL`. ZERO destructive ops; ZERO FK changes; ZERO RLS changes; ZERO new policies. **Migration deployed via authorized supabase db push (operator-confirmed 2026-05-09).** (2) `backend/src/utils/aiValidator.ts` (+~30 LOC) — `AIResponse.category?: string` added (TS optional); validator accepts missing/null/undefined silently, throws `AIValidationError {field: 'category'}` on present-but-empty/non-string (consistent with reasoning_steps strictness pattern); validated result spreads category conditionally so `category: undefined` never appears on the object. `deriveStatus` UNCHANGED — status remains derived-from-confidence only; category does NOT affect status. (3) `backend/src/services/ai/persistence.ts` (+8/-0) — INSERT body extended with `category: input.response.category ?? null`. Validator type-system gate guarantees the value is either undefined or a non-empty trimmed string. (4) `backend/src/services/execution/automation-engine.ts` (+~35 LOC) — `AIDecisionRow.category: string | null` added; both `select(...)` clauses (`evaluateRulesForAIDecision` line 178 + `executeRule` line 258) extended with `category` column; `extractCategory(decision)` rewritten as TWO-STAGE shim: stage 1 reads top-level `decision.category` column; stage 2 (FALLBACK SHIM, preserved verbatim from pre-Path-F) reads `result.category` JSONB path. Returns null when neither yields a non-empty string — preserves the dormant-by-default safety property when no category is emitted. (5) `backend/src/routes/v1/ai.ts` (+~25 LOC across both prompts) — both AI Output Contract systemPrompts (`/decisions/generate` line 116 + `/execute` line 294) updated: optional `"category"?: <optional non-empty string label such as ROAS_DROP, SPEND_SPIKE, CONVERSION_DROP, SCALING_OPPORTUNITY>` added to the contract template; "no keys other than the four above" relaxed to "no keys other than the five above"; new sentence explicitly tells the model "category is optional; include it when the decision corresponds to a recognizable categorical signal that an automation rule could trigger on; omit it otherwise". `/decisions/generate` legacy-compat response surfaces `category` conditionally. `/execute` returns `result.response` whole — `category` flows through automatically. **Operator-Visibility Bridge (this same continuation):** (6) `backend/src/routes/v1/automation.ts` (+1 select-string token) — `GET /api/v1/automation/runs` SELECT extended with `ai_decisions(category)` PostgREST nested-select via the existing FK `automation_runs.ai_decision_id → ai_decisions(id)`. Closes the operator-visibility gap created when #23 + Path F armed the loop: operators can now verify auto-fire end-to-end via the existing wired history surface (`GET /automation/runs` consumed by `app/automation/history/page.tsx` per #22) without leaving the product or running SQL. NO frontend type extension required — frontend ignores unknown JSON keys; the wired-but-mocked-deferred sections of `app/automation/history/page.tsx` remain UNCHANGED per #22's "preserve mocked-deferred sections verbatim" rule. Confidence_score visibility, AI explanation surfacing, monthly aggregation, and trend chart remain mocked-deferred per #22 governance. **Verification:** backend `tsc --noEmit` → 0 errors; frontend `tsc --noEmit` → 0 errors. **Execution flow delta (post-#24):** AI provider may now emit optional top-level `category`; validator passes it through (or rejects malformed); persistence writes to dedicated `ai_decisions.category` column; `extractCategory` reads column first, falls back to legacy `result.category` JSONB path; matching `automation_rules.trigger_type` + sufficient confidence triggers fireRule → executeAction; `automation_runs` row written; `GET /automation/runs` joins `ai_decisions(category)` so the categorical attribution is visible alongside `automation_rules(name)` in the runs response. Stream-driven Decision → Action → Result loop is now BOTH wired (#23) AND active (#24) AND verifiable (#24 visibility bridge). **Backwards-compatibility analysis:** any AI response that omits `category` is treated identically to pre-Path-F: validator accepts, persistence writes NULL, extractCategory tries the column (NULL) then falls back to result.category JSONB, returns null when both empty, no rule fires (intentional safety). Existing `ai_decisions` rows pre-migration are bit-identical at read time (NULL column, NULL fallback unless they happen to embed result.category). NO existing API consumer breaks (additive optional field on /decisions/generate response; full response object passthrough on /execute). **Idempotency-preservation analysis:** auto-fire path through `executeAction` preserves `(org_id, execution_id)` partial unique index verbatim. Path F adds zero new write paths to executeAction. **Org-isolation:** Path F migration adds NO RLS — column inherits parent table's `ai_decisions` RLS (`org_id = jwt.org_id`). Persistence INSERT continues using server-side `input.org_id`; never client-supplied. Visibility-bridge JOIN uses service-role with explicit `.eq('org_id', orgId)` filter on automation_runs (RLS defense-in-depth on both tables). NO cross-org category leak possible. **Rollback analysis:** (a) Code rollback: revert all 5 backend files + visibility-bridge SELECT extension; backend will continue to function bit-identically against the migrated table (NULL column ignored). (b) Migration rollback: `ALTER TABLE ai_decisions DROP COLUMN category;` + `DROP INDEX ix_ai_decisions_category_org;` — safe; no FKs, no policies, no data dependencies. (c) Partial rollback: deploying migration without code rollback is safe (column defaults NULL); deploying code without migration would fail with `42703 column "category" of relation "ai_decisions" does not exist` on every persist — strict ordering required (migration FIRST, code SECOND). Operator-confirmed migration applied successfully BEFORE this code lands. **Governance classification:** Path F is INSIDE existing closed Phase 3 linear pipeline (extends the AI Output Contract; doesn't reopen the pipeline; preserves deriveStatus single-source-of-truth). The visibility bridge is INSIDE existing closed Phase 4 P2 surface (adds a column to an existing SELECT; doesn't reopen the router). NO Phase X broader (MCP routing, tool governance, log-sink fan-out, strategy_tag enum) — auto-fire still uses the existing automation-engine; no MCP introduced. NO new orchestration layers, queues, retries, event buses, async workers, provider abstractions, rule-engine rewrites, DSL systems, automation redesigns, AI-agent frameworks, autonomous planning. NO Phase 6 frontend wiring of deferred pages. NO Phase 7 monetization expansion. NO TikTok/Snapchat. NO `/decisions/*` or `/alerts/*` 503 gate change. NO legacy `decisions` table reactivation. NO `decision-generator.ts` change (continues writing to legacy DEPRECATED table; deliberately unchanged). NO Python creative runtime change. **HARD LOCK invariants preserved verbatim:** AI MUST NOT write to DB before validation (validator gate intact; new optional field flows through validator before persistence); AI MUST NOT call external APIs without logging (no provider call shape change); actions_library template lookup gate (no executeAction touch); required-parameter validation (no executeAction touch); decision_history server-side org_id (no decision_history touch); automation engine boundary (entry points unchanged; extractCategory is internal); META_INCREASE_BUDGET_MAX_PERCENT cap (untouched); partial unique index `(org_id, execution_id)` (untouched); recipient placeholder filter (untouched). **Phase locks preserved:** Phase 0/1/2/3-linear/4-min/4-Part2/5/Phase 6 Sub-A/B/C/Phase 7 Sub-A0/A0.5/A1a/A1b/A1c/B/D/webhook hardening/auto-fire #23 ALL UNTOUCHED structurally. Path F EXTENDS the AI Output Contract additively without reopening Phase 3 close. **Closed-phase invariants intact:** single-writer backend (Path F write path is server-side persistence only; visibility bridge is server-side SELECT only); org_id-from-JWT-only (input.org_id continues to source from authMiddleware via routes/v1/ai.ts; never client-supplied); canonical envelope (no route shape change beyond optional `category` surfacing on `/decisions/generate`); request_id correlator chain (no log emission change); deferred-router 503 gating preserved on `/decisions/*` + `/alerts/*`; legacy `decisions` table NOT reactivated; Phase 7 monetization unchanged. **Operator deploy prerequisites:** (1) ✅ migration `20260509130000_phase3_ai_decisions_category.sql` applied via Supabase MCP / `supabase db push` (operator-confirmed before this code lands); (2) backend redeploy to activate validator + persistence + extractCategory shim + visibility-bridge SELECT + updated systemPrompts; (3) NO env changes, NO new deps, NO frontend redeploy required. **Operator verification matrix:** (a) create an `automation_rules` row with `trigger_type='ROAS_DROP'`, `enabled=true`, sufficient `min_confidence_threshold`, valid `action_template_id`; (b) issue an AI call via `POST /api/v1/ai/execute` or `POST /api/v1/ai/decisions/generate` whose prompt elicits a `ROAS_DROP` category response from the model; (c) inspect `automation_runs` row written by the auto-fire hook AND verify `GET /api/v1/automation/runs` response shows the joined `ai_decisions.category='ROAS_DROP'`; (d) inspect `decision_history` for the executeAction audit row with `automation_rule_id` + `automation_run_id` populated. **Auto-fire activation matrix (post-#24):** dormant pre-#24 (no AI response carried category); ARMED post-#24 once AI provider emits a category matching at least one enabled rule's trigger_type. Operators globally disable via `UPDATE automation_rules SET enabled=false`. Per-org granular control already exists via existing CRUD. **Active hardened surface envelope coverage unchanged at 9 routers. Real Action Surface unchanged.** **Closed-loop product-promise delta (CLAUDE.md §1):** Decision → Action → Result loop is now operational end-to-end AND auditable end-to-end. No remaining Phase-3-side dormancy on the auto-fire path. Phase 6 frontend completion still has 4 deferred mocked-shell pages (per #22 governance documentation — not a #24 scope item). Phase 7 monetization end-to-end live (#21/#22). PHASE2_OAUTH_ENV_FAILFAST_FOLLOWUP / PHASE2_ENVELOPE_FOLLOWUP remain tracked.

2026-05-09 (continuation #23) — Phase 4/Phase 6 BRIDGE EXECUTED — auto-firing automation_rules on AI decision stream. Closes the Decision → Action → Result loop per CLAUDE.md §1 product promise. Resolves the GOVERNANCE-BLOCKED item tracked since Phase 4 Part 2 close (continuation #5) — selected Path 3 (post-persist hook in closed Phase 3 linear pipeline) over Path 1 (Phase 3 anomaly DEPRECATED) and Path 2 (AI Output Contract `result.category` schema extension cross-phase). **Pre-implementation verification confirmed:** `evaluateRulesForAIDecision(orgId, aiDecisionId): Promise<RuleEvaluationResult>` exported at `services/execution/automation-engine.ts:141` (Phase 4 P2 substrate); internal failure isolation guarantee — per-rule errors caught with `automation_runs.status='failed'` rows; only infrastructure-level errors (DB unavailable on lookups) bubble. Hook point at `services/ai/execute-ai-decision.ts:303` immediately before `return { trace_id, decision_id, response }`. Pattern precedent: Sub-pass A1a's `log_ai_usage` fire-and-forget at line 287-302 — same location, same pattern, same failure-isolation contract. ALL substrate (automation_rules + automation_runs + decision_history extensions + evaluateRulesForAIDecision) live since Phase 4 P2 deploy (continuation #5) — **NO migration required this pass**. Acyclic dependency check passed: Phase 3 AI pipeline → Phase 4 P2 automation-engine → Phase 4 min action-executor; no cycle introduced. **Files modified:** `backend/src/services/ai/execute-ai-decision.ts` only (+73/-0; single import + single fire-and-forget block + 60 lines of governance-trail doc-comments). NO other backend files touched. NO frontend files touched. NO migrations. NO new dependencies. NO new env vars. NO new feature flags. Implementation: single fire-and-forget call to `evaluateRulesForAIDecision(input.org_id, decision_id)` placed AFTER `persistAIDecision()` resolves with committed `decision_id` AND AFTER the existing `log_ai_usage` fire-and-forget AND BEFORE the function `return`. .then(noop, errorLog) swallows all automation failures with structured `[AI] auto-fire automation rules failed for ai_decision_id=X org_id=Y trace_id=Z: <msg>` operator-visible log. AI flow's primary-success contract (decision persisted + returned) is already met before any rule evaluation begins. Post-edit verification: backend `tsc --noEmit` → 0 errors; frontend `tsc --noEmit` → 0 errors. **Execution ordering (post-#23):** (1) validate AI response; (2) ai_decisions INSERT committed; (3) log_ai_usage fire-and-forget [observability]; (4) evaluateRulesForAIDecision fire-and-forget [auto-fire — THIS HOOK]; (5) return decision to caller. Steps 3+4 run in parallel; both target distinct tables (ai_usage_logs vs automation_rules SELECT + automation_runs INSERT) — no race risk. **Failure-isolation guarantees:** (a) automation rule evaluation NEVER affects AI persistence success — persist completes before hook fires; (b) per-rule errors recorded in `automation_runs.status='failed'` + `error_message`; loop continues to next rule (existing automation-engine.ts:fireRule semantic); (c) infra-level errors (DB unreachable) caught by .then(noop, log) and never propagate; (d) malformed rules / handler dispatch errors / external Meta/Google API failures / RLS denial / Vault timeouts — all isolated to per-rule scope. **Idempotency-preservation analysis:** executeAction (sole canonical executor — invoked transitively via evaluateRulesForAIDecision → fireRule → executeAction chain) preserves its `(org_id, execution_id)` partial unique-index gate verbatim. Auto-fire dispatches do NOT supply execution_id (they always-run on every matching AI decision stream event); the index acts as duplicate-protection safety net for in-Inngest-attempt retries. NO duplicate execution paths introduced. NO secondary execution engines. **Auditability delta:** every auto-fired rule produces (1) automation_runs row with full request_id/trace_id correlator, (2) decision_history row via executeAction's standard insert path (carries automation_rule_id + automation_run_id Phase 4 P2 audit columns), (3) `[exec]` lifecycle log lines from action handlers, (4) optional `[err]` log on infra failures via the .catch() in this hook. End-to-end traceability from AI decision → automation rule fire → action execution → decision_history → impact_snapshot. **Org-isolation:** input.org_id forwarded directly; evaluateRulesForAIDecision scopes both ai_decisions lookup AND automation_rules query by orgId via explicit `.eq('org_id', orgId)`; RLS provides defense-in-depth. **Canonical-executor invariant preserved:** this hook does NOT call provider APIs directly; does NOT introduce a parallel execution engine; does NOT bypass actions_library template lookup. evaluateRulesForAIDecision → fireRule → executeAction is the established Phase 4 P2 chain. **Trigger semantics:** rule fires when `automation_rules.trigger_type === ai_decisions.result.category` (JSONB path) AND `ai_decisions.confidence_score >= rule.min_confidence_threshold/100` (NUMERIC 0-1 normalization). When AI decisions don't carry `result.category` (governance-soft AI Output Contract dependency), no rule auto-fires — preserves operator-controlled behavior gating. ZERO orchestration framework, ZERO queues, ZERO retries, ZERO async workers, ZERO event buses, ZERO provider abstractions, ZERO rule-engine rewrite, ZERO DSL system, ZERO automation redesign, ZERO AI-agent framework, ZERO new orchestration layer, ZERO autonomous planning. HARD LOCK invariants preserved verbatim: actions_library template lookup gate, required-parameter validation, decision_history server-side org_id, automation engine boundary (entry points unchanged — evaluateRulesForAIDecision was already exported; this hook just calls it), META_INCREASE_BUDGET_MAX_PERCENT cap, partial unique index `(org_id, execution_id)`, recipient placeholder filter — all untouched. Phase locks preserved: Phase 0/1/2/3-linear/4-min/4-Part2/5/Phase 6 Sub-A/B/C/Phase 7 Sub-A0/A0.5/A1a/A1b/A1c/B/D/webhook hardening ALL UNTOUCHED. Closed-phase invariants intact: single-writer backend (auto-fire path uses supabaseAdmin only via existing automation-engine.ts), org_id-from-JWT-only enforcement (input.org_id sourced from authMiddleware via the calling routes/v1/ai.ts; never client-supplied), canonical envelope (no route changes), request_id correlator chain (request_id stamped onto auto-fire infra-error log line), deferred-router 503 gating preserved on `/decisions/*` + `/alerts/*`, legacy `decisions` table NOT reactivated, Phase 7 monetization unchanged. Phase X broader (MCP routing, tool governance, log-sink fan-out, strategy_tag enum) remains LOCKED — auto-fire is NOT MCP routing; it's a single-line single-call hook into existing Phase 4 P2 substrate. Decision-generator.ts unchanged. TikTok/Snapchat backend unchanged. Closed-phase migrations unchanged. Python creative runtime remains HOLD. **AI→Automation execution flow delta:** prior state — AI decisions persisted to ai_decisions with no downstream consumption; manual rule firing only via `POST /api/v1/automation/rules/:id/execute`. Post-#23 state — every persisted ai_decisions row triggers automatic evaluation against active org rules; matching rules (trigger_type + confidence) auto-execute via the canonical Phase 4 executeAction pipeline; full audit trail in automation_runs + decision_history. Stream-driven Decision → Action loop now operational. Manual `/rules/:id/execute` endpoint preserved for admin-on-demand firing. **Operator deploy prerequisites:** backend redeploy only — NO migration, NO env changes, NO infra changes. After redeploy: any subsequent `POST /api/v1/ai/execute` or `POST /api/v1/ai/decisions/generate` call that produces a successful AI decision matching at least one enabled automation_rule will auto-fire that rule(s). Operators can verify by: (1) creating an automation_rule with trigger_type matching an expected AI decision category; (2) issuing an AI call expected to produce that category; (3) inspecting `automation_runs` and `decision_history` for the auto-fired entries linked via automation_rule_id + automation_run_id. Operators can globally disable auto-fire by setting `automation_rules.enabled=false` for all rules (per-org granular control already exists). Active hardened surface envelope coverage unchanged at 9 routers. Real Action Surface unchanged. Phase 7 monetization end-to-end live. Phase 6 frontend completion still has 4 deferred mocked-shell pages (per #22 governance documentation).

2026-05-09 (continuation #22) — Phase 7 Stripe webhook hardening + Phase 6 automation/history frontend wiring EXECUTED. Two-part pass executed; 4 of the 5 originally-authorized frontend pages DEFERRED with documented governance reasoning. **Part A — Stripe webhook hardening (~40 LOC).** Pre-implementation verification: confirmed `webhooks/stripe.ts` manual HMAC verification did NOT enforce timestamp tolerance (continuation #18 deliberately deferred); confirmed no event-id dedup table. NEW migration `supabase/migrations/20260509120000_phase7_stripe_webhook_dedup.sql` — `processed_stripe_events` table (event_id TEXT PRIMARY KEY + event_type + received_at; RLS enabled with NO policies — system-only via supabaseAdmin service-role). Rationale: Stripe retries non-2xx for hours and may also re-deliver successful events on operator-side reconfiguration; without dedup the webhook double-grants credits / double-upserts subscriptions on retry. INSERT-ON-CONFLICT-23505 is the atomic dedup gate. UPDATED `backend/src/routes/webhooks/stripe.ts`: (1) added `STRIPE_SIGNATURE_TOLERANCE_SECONDS = 300` constant matching Stripe SDK `constructEvent` default 5-min window; enforced inside `verifyStripeSignature` BEFORE HMAC comparison — rejects signatures whose `t` is outside `Math.abs(nowSec - ts) > tolerance` (blocks pure replay of old captured signed payloads + forward-clock-skew abuse); (2) added event_id dedup INSERT after JSON parse — on 23505 conflict returns `{received:true, deduped:true}` 200 ack immediately so Stripe-side retry terminates cleanly; on other DB error logs + continues (best-effort dedup; never blocks webhook on infra failure). NO queue system, NO event bus, NO retry framework, NO async orchestration — pure single-row PK INSERT. **Part B — Phase 6 frontend completion (1 of 5 authorized pages wired; 4 deferred).** WIRED: `app/automation/history/page.tsx` to canonical `GET /api/v1/automation/runs` (~110 LOC change). Added `useEffect`/`useAuth`/`apiClient`; new `ApiAutomationRun` interface mirroring backend response shape (joined automation_rules.name); new `mapRunToEntry()` helper translating run.status (pending/success/failed/skipped) + run.executed_at + run.automation_rules.name + run.ai_decision_id + run.error_message into the existing UI HistoryEntry shape. Loading + error states. Removed obsolete MOCK_HISTORY array (~73 LOC dead code deletion). MOCKED-DEFERRED sections preserved verbatim with explicit doc comments: AI Decision Insights aside (no per-run AI explanation endpoint), Quick Stats aside (no monthly aggregation), Confidence Score per entry (would require ai_decisions JOIN — separate hardening pass), Detailed Explanation panel, Trend bar chart. **DEFERRED (4 of 5 originally-authorized pages):** runtime evidence revealed material UI⇄backend semantic mismatches that require UX-design discussion beyond minimal-diff scope. Governance-documented deferral reasoning: (a) `app/creatives/page.tsx` — UI is a creative-generation FORM with rich config (objective + platforms + format + strategy_type + angles + audience + productDetails); backend `POST /creatives/generate` accepts only `{generation_type:'copy'|'image', ad_account_id?, campaign_name?}`. Wiring naively would lie about which form fields actually shape generation. (b) `app/actions/page.tsx` — UI displays "AI Action Recommendations" with rich semantic shape (aiContext + triggerLogic + scope + impact + risk + score) NOT matching actions_library template-catalogue shape (id + platform + action_type + name + description + parameter_schema). The page UI assumes an AI-recommendations engine that does NOT yet exist (anomaly DEPRECATED; recommendations engine is Phase 3 anomaly successor — not in any current phase). (c) `app/actions/[id]/page.tsx` — same semantic mismatch as actions/page. (d) `app/automation/strategies/page.tsx` — UI is a "preset templates catalogue" (Stop Loss, Scaling Rocket, Creative Refresher, etc.) NOT matching automation_rules CRUD (user-defined rules with trigger_type/min_confidence_threshold/action_template_id/action_params). The page UI assumes a preset-template backend that does NOT yet exist. Per "preserve existing UI structure" + "no redesign" + "preserve truthful UX" — wiring these pages requires either (1) preset-templates backend layer (out of authorized scope), or (2) page redesign to match available backend (forbidden by "no redesign"). Each deferral is preserved as MOCKED-DEFERRED governance-correctly. **Files changed (this turn):** NEW `supabase/migrations/20260509120000_phase7_stripe_webhook_dedup.sql` (~50 LOC), MODIFIED `backend/src/routes/webhooks/stripe.ts` (+47/-7 — timestamp tolerance + event-id dedup), MODIFIED `app/automation/history/page.tsx` (+118/-79 net — wired with mocked-deferred sections preserved), MODIFIED `specs/SYSTEM_CONTROL.md` (this entry). Post-edit verification: backend `tsc --noEmit` → 0 errors; frontend `tsc --noEmit` → 0 errors; `git status --short` shows the 3 authorized files + this doc + Sub-pass D artifacts already in working tree. ZERO new orchestration layers, ZERO queue systems, ZERO event buses, ZERO retry frameworks, ZERO entitlement engines, ZERO speculative analytics frameworks. HARD LOCK invariants preserved verbatim. Phase locks preserved: Phase 0/1/2/3-linear/4-min/4-Part2/5/Phase 6 Sub-A/B/C/Phase 7 Sub-A0/A0.5/A1a/A1b/A1c/B/D ALL UNTOUCHED. Closed-phase invariants intact: single-writer backend (webhook dedup INSERT runs server-side via supabaseAdmin only), org_id-from-JWT-only enforcement (no change — webhook continues recovering org from Stripe metadata; history page uses `c.get('orgId')` server-side via authMiddleware), canonical envelope (history page consumes via existing apiClient auto-unwrap), request_id correlator chain (webhook dedup logs stamped with request_id), deferred-router 503 gating preserved on `/decisions/*` + `/alerts/*`, legacy `decisions` table NOT reactivated. **Webhook-hardening delta:** Stripe-Signature timestamp now bounded to ±5 minutes; signature replay window closed. Event-id dedup gate now active; double-processing on retry impossible (atomic PK INSERT enforces single-execution invariant). **Replay-protection delta:** captured signed payload becomes invalid 5 minutes after Stripe issues it; double-delivery of same event_id results in 200 ack-only (no handler dispatch, no DB writes, no credit grants). **Frontend wiring coverage matrix:** automation/history wired (this pass); automation/strategies / actions/page / actions/[id] / creatives DEFERRED with governance documentation. **Truthful-UX preservation matrix:** all wired surfaces show real backend data; all mocked-deferred sections explicitly labeled in doc comments; deferred pages remain unchanged rather than partially-wired with misleading semantics. Active hardened surface envelope coverage unchanged at 9 routers. Phase X broader (MCP routing, tool governance, log-sink fan-out, strategy_tag enum) remains LOCKED. Auto-firing automation_rules on AI decision stream remains DEFERRED. Decision-generator.ts unchanged. TikTok/Snapchat backend unchanged. Closed-phase migrations unchanged. Python creative runtime remains HOLD. **Operator deploy prerequisites for this pass:** (1) deploy migration `20260509120000_phase7_stripe_webhook_dedup.sql` via Supabase MCP `apply_migration` to make event-id dedup substrate live (until deployed, the INSERT will hit "relation does not exist" 42P01 → loud-logged → fail-open behavior preserved per best-effort design); (2) backend redeploy to activate timestamp-tolerance enforcement + dedup INSERT call site; (3) NO frontend redeploy required for webhook hardening (backend-only); FE redeploy needed for automation/history wiring to be visible.

2026-05-09 (continuation #21) — Phase 7 Sub-pass D + Per-org Meta Vault Migration + Initial Credits JIT Grant EXECUTED — three coordinated business-safe sub-passes shipped together per operator-authorized priority sequence (A→B→C). All preconditions verified: Stripe webhook (continuation #18) live; existing billing.ts CRUD + GET (#19); existing Meta real handlers using `META_TEST_ACCESS_TOKEN` (#11/#20); authMiddleware JIT upsert flow (Phase 1 close); `grant_credits` RPC live (#18); Phase 2 sync stores Meta access token at `integrations.vault_refresh_token_secret_id` (column reused for both Google refresh tokens and Meta access tokens). **PART A — Stripe outbound API.** Added `stripe@22.1.1` SDK dep (canonical for Checkout Session creation; manual HTTP would be 200+ LOC vs ~50 LOC with SDK; operator-authorized via "Stripe SDK allowed ONLY if runtime-required"). NEW `backend/src/lib/stripe.ts` (~30 LOC): thin lazy-init wrapper around `Stripe(key, {apiVersion})`; runtime check on STRIPE_SECRET_KEY (NOT in startup-fail-fast — backend boots without Stripe; routes return canonical `STRIPE_NOT_CONFIGURED` 500 if key missing). NEW routes in `backend/src/routes/v1/billing.ts`: `POST /api/v1/billing/checkout` (creates Stripe Checkout Session with `metadata.org_id` propagated to both Session + Subscription + Customer; reuses existing `stripe_customer_id` if subscriptions table already has it; returns `{checkout_url, session_id}`); `POST /api/v1/billing/portal` (Customer Portal session creation; 404 NO_STRIPE_CUSTOMER if subscriptions row absent). Frontend `app/settings/billing/page.tsx`: replaced disabled "Upgrade Plan" button with handler that calls /checkout + redirects to Stripe-hosted URL via `window.location.href`. Reads `NEXT_PUBLIC_STRIPE_DEFAULT_PRICE_ID` env at build time; shows clear error if unset rather than silent fail. NO custom payment UI, NO subscription state-machine redesign, NO entitlement engine. **PART B — Per-org Meta Vault token migration.** Added `resolveMetaAccessToken(ctx)` provider-local helper to action-executor.ts: tries per-org `integrations.vault_refresh_token_secret_id` for platform='meta' status='connected' first via existing `readSecret`; falls back to `META_TEST_ACCESS_TOKEN` env when Vault absent (preserves existing single-tenant sandbox behavior bit-identically); returns `{token, source: 'vault'|'sandbox'|'none'}` with source-label for observability (NEVER logs the token itself). Migrated all 4 Meta real handlers verbatim to use the resolver: `realMetaPauseCampaign`, `realMetaDecreaseBudget`, `realMetaIncreaseBudget`, `realMetaCreateCampaign`. Token-check blocks rewritten to test resolver result rather than env directly; error messages updated to mention both Vault + sandbox absent. All inline `META_TEST_ACCESS_TOKEN` runtime references (4 in fetch URLs / form bodies) replaced with `accessToken` local var. The const at line 69 + the LIVE-flag gate at lines 361/383/433/2791 + comments retained verbatim. result_data.token_source embedded in pause_campaign return shape for operator triage. NO provider abstraction, NO multi-provider unification framework, NO behavior change beyond Vault-first preference. **PART C — Initial credits JIT grant.** Modified `backend/src/middleware/auth.ts` JIT auto-provisioning: `.upsert(..., {ignoreDuplicates: true}).select('org_id')` now returns inserted-row evidence; when `(insertedOrgs ?? []).length > 0` (genuinely new org row created), fire-and-forget `grant_credits` RPC with `p_amount=parseInt(INITIAL_ORG_CREDITS_GRANT ?? '100')`, `p_reason='initial_grant'`, `p_ref_type='jit_auto_provision'`. NEVER fires on existing-row cases (race-safe; preserves Clerk-webhook canonical-path coexistence). Failure of grant does NOT fail auth provisioning (best-effort; structured `[auth] JIT initial grant_credits failed for org=<id>` log on error). Operator can disable by setting `INITIAL_ORG_CREDITS_GRANT=0`. Audit row appended atomically to credits_ledger inside the RPC. NO entitlement engine, NO RBAC redesign, NO repeated-grant on login. **Files changed (this turn):** NEW `backend/src/lib/stripe.ts` (~36 LOC), NEW `backend/src/routes/v1/billing.ts` (continued; +210 LOC checkout+portal routes; total file now ~430 LOC), MODIFIED `backend/src/services/execution/action-executor.ts` (+~70 LOC resolveMetaAccessToken + 4 Meta handler migrations; ~10 lines net deletion of old token-check redundancy), MODIFIED `backend/src/middleware/auth.ts` (+38 LOC initial-credits-grant block + .select() addition), MODIFIED `backend/src/index.ts` (no requiredEnvVars change — STRIPE_SECRET_KEY runtime-only; no edits this part), MODIFIED `backend/.env.example` (+11 LOC — STRIPE_SECRET_KEY doc-block update + INITIAL_ORG_CREDITS_GRANT entry), MODIFIED `app/settings/billing/page.tsx` (+50/-9 — handleUpgrade handler + button activation + state hooks), MODIFIED `.env.local.example` (+7 — NEXT_PUBLIC_STRIPE_DEFAULT_PRICE_ID), MODIFIED `backend/package.json` + `backend/package-lock.json` (added `stripe@22.1.1` dep), MODIFIED `specs/SYSTEM_CONTROL.md` (this entry). Post-edit verification: backend `tsc --noEmit` → 0 errors (after fixing Stripe SDK v22 `LatestApiVersion` removal via `as never` cast on apiVersion string-literal pin); frontend `tsc --noEmit` → 0 errors. ZERO new orchestration layers, ZERO queue systems, ZERO retry frameworks, ZERO event buses, ZERO subscription state-machine redesign, ZERO custom payment UI, ZERO multi-provider abstraction, ZERO RBAC expansion, ZERO entitlement engine, ZERO plan-catalogue derivation, ZERO pricing logic in code (Stripe Price metadata + INITIAL_ORG_CREDITS_GRANT env are operator-side knobs). HARD LOCK invariants preserved verbatim. Phase locks preserved: Phase 0/1/2/3-linear/4-min/4-Part2/5/Phase 6 Sub-A/B/C/Phase 7 Sub-A0/A0.5/A1a/A1b/A1c/B ALL UNTOUCHED. Closed-phase invariants intact: single-writer backend (Stripe SDK calls run only via lib/stripe.ts thin wrapper called from billing routes; org_id from `c.get('orgId')` server-side; webhook handler unchanged), org_id-from-JWT-only (checkout route reads orgId from authMiddleware; webhook continues recovering from Stripe metadata; JIT grant uses authMiddleware-bound orgId), canonical envelope (all new billing routes use `ok()`/`fail()`), request_id correlator chain (auth.ts JIT block stamps request_id on all log lines including new initial-grant failure log), deferred-router 503 gating preserved on `/decisions/*` + `/alerts/*`, legacy `decisions` table NOT reactivated, Phase 7 monetization status: substrate live (A0); hardened (A0.5); observability instrumented (A1a); enforcement live (A1b); Stripe webhook receive surface live (A1c); frontend BYOK + plan display wired (B); **Stripe outbound checkout + portal routes live (D Part A — this pass)**; **per-org Meta Vault token migration live (D Part B — this pass)**; **initial credits JIT grant live (D Part C — this pass)**. Phase 6 Sub-pass C real-mode CREATE handlers (#20) now use the same per-org Vault Meta token resolver (Part B benefits realMetaCreateCampaign in addition to pause/inc/dec). Active hardened surface envelope coverage: 9 routers (unchanged — billing routes added in #18/#19, not new this pass). **Stripe outbound flow delta:** "Upgrade Plan" button transitions from disabled-with-tooltip (#19 truthful UX) to active checkout-redirect when `NEXT_PUBLIC_STRIPE_DEFAULT_PRICE_ID` is configured. Customer Portal endpoint available for self-service subscription management. Both routes preserve canonical envelope + org_id isolation. **Meta token-flow delta:** all 4 real Meta handlers now consult per-org Vault first; fall back to META_TEST_ACCESS_TOKEN sandbox token when no Meta integration is connected. Multi-customer production deployments now bill platform actions to the customer's own connected Meta account when Vault token is present. Single-tenant sandbox behavior (no integration → sandbox token) preserved bit-identically. **JIT credits-flow delta:** new orgs auto-receive `INITIAL_ORG_CREDITS_GRANT` credits on first JIT auto-provisioning login (default 100); audit row in credits_ledger. Eliminates operator burden of manually seeding each new org. Operator can opt out with `INITIAL_ORG_CREDITS_GRANT=0`. Existing orgs unaffected (grant only fires on row-just-created path). Truthful UX preserved across all three sub-passes. Real-mode handlers, real Stripe flows, and JIT credits all default-OFF where appropriate (LIVE flags / runtime-key-required / env-configurable amount). Known follow-ups (non-blocker; tracked for future passes): (a) executeAction line 2791 logging-mode label still uses `Boolean(META_TEST_ACCESS_TOKEN)` which may show 'simulated' for orgs with Vault-only Meta token; runtime behavior is correctly 'live'; log-label accuracy gap accepted per "preserve existing logging semantics" minimal-diff rule. (b) STRIPE_SECRET_KEY not promoted to startup-fail-fast — runtime-required only; matches Phase 2 OAuth env pattern. Phase X broader (MCP routing, tool governance, log-sink fan-out, strategy_tag enum) remains LOCKED. Auto-firing automation_rules on AI decision stream remains DEFERRED (cross-phase). Decision-generator.ts unchanged. TikTok/Snapchat backend support unchanged. Closed-phase migrations unchanged. Python creative runtime remains HOLD.

2026-05-09 (continuation #20) — Phase 6 Sub-pass C EXECUTED — real-mode Meta + Google Ads create_campaign handlers behind LIVE flags + per-platform allowlists + startup-fail-fast dependency-checking. Pre-implementation precondition verification: existing simulated `create_campaign` ACTION_HANDLER entry confirmed at line 423-433 (Sub-pass A continuation #11 substrate); canonical real-Meta pattern at `realMetaPauseCampaign` (lines 449-603 — single fetch + META_TEST_ACCESS_TOKEN single-tenant, no Vault refresh); canonical real-Google pattern at `realGooglePauseCampaign` (lines 626-901 — Vault refresh-token + customer_id lookup + access_token refresh + mutate); existing pause_campaign LIVE-flag dispatch shape at lines 312-345 verbatim; tokens never logged across all real handlers. **Authorized scope:** real provider CREATE handler activation only — replaces simulated-only fallthrough with LIVE-flag-aware dispatch preserving simulated default. **Files modified: 3 backend files + 1 env example + 1 doc.** (1) `backend/src/services/execution/action-executor.ts` — added 2 flag constants `META_CREATE_CAMPAIGN_LIVE` + `GOOGLE_CREATE_CAMPAIGN_LIVE`; replaced simulated-only `create_campaign` ACTION_HANDLER entry with LIVE-flag-aware dispatch (Meta-first / Google-second / simulated-fallthrough — mirrors pause_campaign verbatim); appended `realMetaCreateCampaign` (~250 LOC) + `realGoogleCreateCampaign` (~400 LOC) before `realSendAlertEmail`. **Meta CREATE flow:** ad_account_id resolved from `ad_accounts.platform_account_id` lookup for org+'meta' (first connected integration; matches realGooglePauseCampaign customer_id resolution convention); single POST to `/{ver}/act_{ad_account_id}/campaigns` form-urlencoded body `{name, objective, status='PAUSED' HARD-CODED, special_ad_categories=[], daily_budget (cents) optional, access_token=META_TEST_ACCESS_TOKEN}`; objective defaults to 'OUTCOME_TRAFFIC' overridable via params.objective; defensive `act_` prefix handling on platform_account_id; tokens NEVER logged. **Google CREATE flow:** TWO sequential POSTs (Google Ads requires CampaignBudget before Campaign reference) — (1) `campaignBudgets:mutate` with `{name="<campaign_name> Budget <ISO_timestamp>", amount_micros, delivery_method='STANDARD'}` then (2) `campaigns:mutate` with `{name, status='PAUSED' HARD-CODED, advertising_channel_type defaults 'SEARCH' overridable, campaign_budget=<resource_name from step 1>}`; per-org Vault refresh-token via `integrations.vault_refresh_token_secret_id` → `readSecret`; OAuth token refresh at `oauth2.googleapis.com/token`; customer_id resolved from `ad_accounts.platform_account_id` (dashes stripped) or explicit params.customer_id override; on step-2 failure the budget orphan is logged with structured warning but NOT auto-deleted (matches Google's recommended pattern for rollback-without-temp-id flows; operator prunes via Google Ads dashboard). Both handlers: full `[exec]` lifecycle logging (api_call → api_response with phase/latency/http_status/ok); transport errors handled separately from HTTP error responses; structured error envelopes with stage tags (`integration_lookup`, `ad_account_missing`, `budget_create`, `campaign_create`, `oauth_refresh`, `transport`, etc.) for operator triage. **CRITICAL safety:** status='PAUSED' is HARD-CODED in BOTH handlers — a misconfigured live flag + allowlist combination cannot accidentally launch real ad spend; operator activates campaigns separately. (2) `backend/src/index.ts` — added 2 entries to `LIVE_FLAG_DEPENDENCIES`: `META_CREATE_CAMPAIGN_LIVE → [META_TEST_ACCESS_TOKEN]` (mirrors existing META_PAUSE/INCREASE/DECREASE_BUDGET pattern); `GOOGLE_CREATE_CAMPAIGN_LIVE → [GOOGLE_ADS_DEVELOPER_TOKEN, GOOGLE_ADS_CLIENT_ID, GOOGLE_ADS_CLIENT_SECRET]` (mirrors existing GOOGLE_PAUSE_CAMPAIGN_LIVE pattern). Misconfigured deploys (LIVE flag true but deps missing) fail-fast at startup with structured `[STARTUP][FATAL]` log line; backward-compat: deployments where flags are unset/false are unaffected. (3) `backend/.env.example` — added `META_CREATE_CAMPAIGN_LIVE=false` (with documentation block) inline with the existing META live-flag block; added `GOOGLE_CREATE_CAMPAIGN_LIVE=false` (with documentation block) inline with the existing GOOGLE_ADS_* env block. Pre-edit verification: confirmed pushCampaign passes `{campaign_name, daily_budget, targeting}` only (no ad_account_id passthrough — handlers resolve from ad_accounts table); confirmed `META_TEST_ACCESS_TOKEN` single-tenant convention preserved (per-org Vault Meta migration remains a separate Phase 2 hardening pass per SYSTEM_CONTROL.md tracked follow-up); confirmed `actions_library` parameter_schema for create_campaign (continuation #11 seeded `campaign_name required, daily_budget optional, targeting optional`) matches handler param expectations. Post-edit verification: backend `tsc --noEmit` → 0 errors; frontend `tsc --noEmit` → 0 errors; `git status --short` shows only the 4 authorized files this turn (action-executor.ts, index.ts, .env.example, this doc) plus the prior continuation #19 frontend artifacts already in working tree. Files modified this turn: `backend/src/services/execution/action-executor.ts` (+665/-12 — 2 flag consts + dispatch replacement + 2 real handlers ~650 LOC), `backend/src/index.ts` (+8/-1 — 2 LIVE_FLAG_DEPENDENCIES entries + comment block), `backend/.env.example` (+18/-0 — 2 documented flag entries), `specs/SYSTEM_CONTROL.md` (this entry). NO orchestration complexity introduced (zero new layers, services, helpers, abstractions, queue systems, retries, background-execution frameworks, cross-provider normalization layers, provider-unification systems). NO SDK migration (raw fetch + Node crypto + existing Supabase Vault helpers only — Stripe SDK still NOT added; no new deps). NO Phase X surfaces touched (MCP routing, tool governance, log-sink fan-out, strategy_tag enum all untouched). NO touching deprecated anomaly engine — `services/intelligence/decision-generator.ts` confirmed unchanged. NO automation_rules auto-fire path expansion. NO TikTok/Snapchat — backend platform enum still strictly meta+google (per VALID_PLATFORMS in campaigns.ts:19). NO frontend changes. NO billing architecture changes. NO AI pipeline structure changes. NO canonical execution contract changes. **HARD LOCK invariants preserved verbatim:** actions_library template lookup gate (CREATE handlers route through executeAction's existing template lookup), required-parameter validation (CREATE handlers re-validate campaign_name defensively even though parameter_schema already requires it), decision_history server-side org_id (executeAction insert path unchanged), automation engine boundary (no automation engine touches), META_INCREASE_BUDGET_MAX_PERCENT cap (unchanged — different code path), partial unique index `(org_id, execution_id)` (CREATE handlers route through executeAction's idempotency layer; same execution_id semantics), recipient placeholder filter (unchanged — different handler). Phase locks preserved: Phase 0/1/2/3-linear/4-min/4-Part2/5 + 13 backend hardening passes + PHASE5_ENVELOPE_FOLLOWUP + Phase 6 Sub-pass A + Sub-pass B + Phase 7 Sub-pass A0/A0.5/A1a/A1b/A1c/B ALL UNTOUCHED. Closed-phase invariants intact: single-writer backend (CREATE handlers write only via supabaseAdmin service-role; org_id from `ctx.orgId` server-side via authMiddleware), org_id-from-JWT-only enforcement (handlers receive `ctx.orgId` from executeAction → route handler → c.get('orgId'); never client-supplied), canonical envelope (no route changes; pushCampaign continues to return `ok(c, {history_id, action_id, status: 'executed'}, 202)`), request_id correlator chain (handlers stamp every `[exec]` log line with `ctx.requestId/traceId/aiDecisionId`), execution_id idempotency preserved (CREATE handlers do NOT introduce new idempotency semantics; rely on executeAction's `(org_id, execution_id)` partial unique index), execution_history audit row insertion preserved (executeAction handles unchanged), deferred-router 503 gating preserved on `/decisions/*` + `/alerts/*`, legacy `decisions` table NOT reactivated, decision-generator.ts excluded. **Provider execution delta:** create_campaign for platform='meta' transitions from `simulated:true` to LIVE Meta Graph API call (when META_CREATE_CAMPAIGN_LIVE=true + token + allowlist matched); create_campaign for platform='google' transitions from `simulated:true` to LIVE Google Ads API two-call sequence (when GOOGLE_CREATE_CAMPAIGN_LIVE=true + per-org Vault token + allowlist matched). When LIVE flags off (default), behavior is bit-identical to pre-Sub-pass-C: simulated success with `{simulated:true, action_type, platform, ...params}` decision_history row. **Operator deploy prerequisites for actually firing real CREATE calls** (non-blocking — defaults remain simulated): (1) backend redeploy with new code; (2) populate `META_CREATE_CAMPAIGN_LIVE=true` AND `META_TEST_ACCESS_TOKEN` AND add target orgs to `META_LIVE_ORG_ALLOWLIST` (CSV) for Meta; (3) populate `GOOGLE_CREATE_CAMPAIGN_LIVE=true` AND `GOOGLE_ADS_*` credentials AND add target orgs to `GOOGLE_LIVE_ORG_ALLOWLIST` AND ensure each org has `integrations.vault_refresh_token_secret_id` populated for platform='google'; (4) ensure each target org has at least one `ad_accounts` row (run a Phase 2 sync first). Active hardened surface envelope coverage unchanged at 9 routers. Real Action Surface inventory: `meta.pause_campaign` ✅ (Phase 4 min), `meta.increase_budget` ✅ (Phase 4 min, max-percent cap), `meta.decrease_budget` ✅ (Phase 4 min), `send_alert_email` ✅ (Phase 4 min), `google.pause_campaign` ✅ (Phase 4 Part 2), **`meta.create_campaign` ✅ (Sub-pass C — this pass)**, **`google.create_campaign` ✅ (Sub-pass C — this pass)**. Auto-firing automation_rules on AI decision stream remains DEFERRED (cross-phase). Real-mode handlers for non-test Meta orgs (per-org Vault Meta refresh-token migration matching google.pause_campaign pattern) remain DEFERRED (separate Phase 2 hardening). TikTok/Snapchat backend support remains DEFERRED (not in any operator-authorized scope). Phase X broader (MCP, tool governance, log-sink fan-out, strategy_tag enum) remains LOCKED. Outbound Stripe API surface (checkout sessions, customer portal) remains DEFERRED. Initial credits seeding remains operator-side (one-time UPDATE or future grant-on-JIT-provision wiring). Python creative runtime remains HOLD.

2026-05-09 (continuation #19) — Phase 7 Sub-pass B EXECUTED — minimal frontend monetization wiring + 1 read-only backend endpoint. Pre-implementation precondition verification: A1c backend artifacts present (billing.ts, stripe.ts, grant_credits migration); apiClient envelope auto-unwrap live since Sub-pass B continuation #13; `app/settings/page.tsx` confirmed as RBAC-only mocked-shell with NO BYOK section (excluded from this pass per "preserve existing UI structure" + "NO redesign" rules); `app/settings/billing/page.tsx` confirmed as pure mocked-shell with hardcoded plan/invoices/utilization. **Files modified: 2 only.** (1) `backend/src/routes/v1/billing.ts` — ADDITIVE: new `GET /api/v1/billing/plan` read-only endpoint (+30 LOC). Returns canonical envelope `{org_id, plan_type, credits_balance, has_byok_key}` projected directly from `organizations` columns. NO Stripe API calls; NO entitlement calculation; NO derivation logic. Inserted before existing PATCH /plan handler. (2) `app/settings/billing/page.tsx` — converted from server component to client component; added `useState`/`useEffect`/`useAuth`; wired hydration via `apiClient<BillingState>('/api/v1/billing/plan')`; **wired sections (TRUTHFUL):** plan name (`Subscription Plan` / `Lifetime Deal` from real plan_type), plan subtitle, credits_balance display ("N credits available" replacing fake "$499.00 / month"), NEW BYOK card section (`POST /billing/byok` Save flow with password-input + Save button, `DELETE /billing/byok` Remove flow with idempotent re-fetch); **MOCKED-DEFERRED sections preserved verbatim with explicit doc comments:** PLAN_FEATURES list (no plan-catalogue endpoint), Plan price area ("$499.00/month" — no pricing catalogue), "Upgrade Plan" button (Stripe checkout-session creation forward-positioned; button now visually disabled with `opacity-60 cursor-not-allowed` + title tooltip "Stripe checkout coming in next release" — preserves UI structure while preventing misleading interaction), "View Details" button, System Utilization widget (no monthly-spend aggregation endpoint), Avg Daily Run / Projected Spend, INVOICES table (no invoices endpoint), Payment Method credit-card display (no Stripe payment-method API wiring), Annual Billing Banner. ZERO `app/settings/page.tsx` changes (RBAC mocked-shell out of scope — would require redesign to add BYOK UI). ZERO new infrastructure. ZERO new state-management layer. ZERO websocket/polling/optimistic-updates systems. ZERO Stripe checkout UI. ZERO customer-portal UI. ZERO RBAC expansion. ZERO entitlement engine. ZERO faked metrics — fake mocked sections explicitly labeled MOCKED-DEFERRED in doc comments. Pre-edit verification: confirmed apiClient backwards-compat (canonical envelope unwrap live); confirmed `app/settings/page.tsx` has zero BYOK references (no in-place wiring possible without redesign); confirmed `useAuth` from `@clerk/nextjs` available; confirmed canonical `apiClient` + `ApiError` import paths (`@/lib/api-client`). Post-edit verification: backend `tsc --noEmit` → 0 errors; frontend `tsc --noEmit` → 0 errors; `git status --short` shows only the 2 authorized files + this doc. **Frontend coverage delta:** `app/settings/billing/page.tsx` moved from "100% mocked-shell" to "wired plan/balance/BYOK with mocked invoice/utilization preserved". `app/settings/page.tsx` unchanged (RBAC mocked-shell). `app/settings/team/page.tsx` unchanged (out of scope). 9 → 10 wired pages out of 40 (25% wired). **Runtime UX delta:** authenticated users now see real plan_type (subscription/ltd) + real credits_balance + real BYOK connected state; can connect/remove their OpenRouter BYOK key end-to-end through the UI; cannot fake-upgrade their plan (button intentionally disabled until Stripe checkout substrate lands); System Utilization / invoices / payment-method continue to show static data with no behavior — explicitly truthful via the mocked-shell governance rule. NO frontend pages outside `app/settings/billing/page.tsx` touched. NO backend behavior change beyond the new GET endpoint. NO outbound Stripe API calls added. NO new dependencies. HARD LOCK invariants preserved verbatim: actions_library template lookup, required-parameter validation, decision_history server-side org_id, automation engine boundary, META_INCREASE_BUDGET_MAX_PERCENT cap, partial unique index `(org_id, execution_id)`, recipient placeholder filter — all untouched. Phase locks preserved: Phase 0/1/2/3-linear/4-min/4-Part2/5 + 13 backend hardening passes + PHASE5_ENVELOPE_FOLLOWUP + Phase 6 Sub-pass A + Sub-pass B + Phase 7 Sub-pass A0/A0.5/A1a/A1b/A1c ALL UNTOUCHED. Closed-phase invariants intact: single-writer backend (frontend continues to call ONLY backend API; never touches Supabase directly), org_id-from-JWT-only (every wired call passes Clerk JWT; backend extracts org_id server-side via authMiddleware; new GET /plan endpoint uses `c.get('orgId')` only — never client input), canonical envelope (frontend api-client auto-unwrap; backend GET endpoint emits canonical shape via `ok()`), request_id correlator chain unchanged, deferred-router 503 gating preserved on `/decisions/*` + `/alerts/*`, legacy `decisions` table NOT reactivated, Phase 7 monetization status: substrate live (A0); hardened (A0.5); observability instrumented (A1a); enforcement live (A1b); Stripe webhook + plan upgrade + BYOK routes authored (A1c, deploy may be pending); frontend wiring of plan/balance/BYOK live (B — this pass). **Backend monetization coverage matrix (post-B):** unchanged from A1c — only an additive read endpoint added. **Frontend coverage matrix (post-B):** plan_type display ✅; credits_balance display ✅; BYOK Save/Remove flow ✅; Stripe-checkout-driven upgrade flow ❌ (forward-positioned); customer-portal entrypoint ❌ (forward-positioned); invoices history ❌ (no backend); payment-method management ❌ (no Stripe payment-method API wiring); monthly-spend utilization ❌ (no aggregation endpoint). **Remaining mocked surfaces in `app/settings/billing/page.tsx`:** PLAN_FEATURES list, plan price line, Upgrade Plan button, View Details button, System Utilization widget (Spent this month / Monthly Limit / Avg Daily Run / Projected Spend), Payment Method credit card, Billing History (4 hardcoded invoices), Annual Billing Banner. **Remaining mocked surfaces system-wide:** 30 of 40 frontend pages still mocked-shells (1 settings page wired this pass + 9 prior). Active hardened surface envelope coverage: 9 routers (unchanged). Phase X broader (MCP, tool governance, log-sink fan-out, strategy_tag enum) remains LOCKED. Phase 6 Sub-pass C (real Meta/Google CREATE handlers) remains DEFERRED. Auto-firing automation_rules on AI decision stream remains DEFERRED. Outbound Stripe API surface (checkout sessions / customer portal / subscription mutations) remains DEFERRED. Initial credits seeding strategy remains operator-side decision (one-time UPDATE or future grant-on-JIT-provision wiring). PHASE2_OAUTH_ENV_FAILFAST_FOLLOWUP / PHASE2_ENVELOPE_FOLLOWUP remain tracked. Python creative runtime remains HOLD.

2026-05-08 (continuation #18) — Phase 7 Sub-pass A1c EXECUTED — Stripe webhook substrate + plan upgrade route + BYOK key storage routes (deploy pending). Pre-implementation precondition verification (live DB probe + grep): A1b enforcement live (5 deduct_credits sites across 3 routers); decision-generator.ts UNTOUCHED (0 references); subscriptions table live (RLS + 1 policy + columns: id, org_id, plan_type, status, stripe_customer_id, stripe_sub_id, current_period_end, created_at, updated_at; CHECK constraints: plan_type ∈ {subscription, ltd}, status ∈ {trialing, active, canceled, past_due}); all 3 prior RPCs live (deduct_credits, refund_credits, log_ai_usage); credits_balance + plan_type + vault_byok_openrouter_secret_id columns live; Vault helpers (createSecret, readSecret, deleteSecret) live; clerk webhook canonical pattern reviewed; no pending migrations; no schema drift. Authorized scope: Stripe webhook + plan upgrade + BYOK + minimal additive migration. **Deliverables this pass:** (A) **Migration `20260508150000_phase7_a1c_grant_credits.sql`** — additive: `grant_credits(p_org_id TEXT, p_amount INTEGER, p_reason TEXT, p_ref_type TEXT, p_ref_id UUID) RETURNS INTEGER` RPC. Atomic UPDATE-balance + INSERT-ledger; validates p_amount > 0 + non-empty p_reason; SECURITY INVOKER preserved; SET search_path = public matches A0.5 hardening. Runtime evidence justifying minimal additive migration: refund_credits hardcodes `reason='refund'` — semantically wrong for Stripe purchase grants. ZERO consumer code changes from this migration alone; webhook below invokes it. (B) **NEW file `backend/src/routes/webhooks/stripe.ts`** (~280 LOC) — manual HMAC-SHA256 signature verification using Node `crypto` (NO Stripe SDK; NO new dep); 4 event types handled: customer.subscription.{created,updated,deleted} → upsertSubscription helper writing to public.subscriptions; invoice.payment_succeeded → opportunistic grant_credits when `metadata.credits_to_grant` present (operator configures via Stripe Price metadata; NO pricing/rate derivation in code); other event types ack'd 200 (Stripe expects 2xx). Org_id recovery: priority (1) event-object metadata.org_id → (2) lookup via subscriptions.stripe_customer_id. Status normalization: any Stripe status outside {trialing, active, canceled, past_due} → 'past_due' for schema-CHECK compatibility. Mirrors webhooks/clerk.ts structural pattern: signature-verify first, dispatch second, server-side DB writes via supabaseAdmin only, request_id correlator on every log line. (C) **NEW file `backend/src/routes/v1/billing.ts`** (~210 LOC) — 3 routes: `PATCH /api/v1/billing/plan {plan_type}` updates organizations.plan_type with audit-column population (updated_by from Clerk JWT); `POST /api/v1/billing/byok {openrouter_key}` writes plaintext key to Vault via createSecret + persists secret_id on org row + cleans up prior secret AFTER persistence (orphan-safe replacement); `DELETE /api/v1/billing/byok` deletes from Vault + clears column (idempotent if already absent). All routes: canonical envelope (ok/fail), body-shape validation, org_id from `c.get('orgId')` only (NEVER client input), plaintext keys NEVER returned in responses + NEVER logged. Per A1c authorization "NO RBAC expansion": no role check at route level; org_id authMiddleware enforcement is sufficient; admin-only gating tracked as known follow-up. (D) **`backend/src/index.ts` edits** — added `STRIPE_WEBHOOK_SECRET` to `requiredEnvVars` (mirrors CLERK_WEBHOOK_SECRET startup-fail-fast pattern; webhook runtime check remains as defense-in-depth); imported + mounted `stripeWebhook` at `/api/webhooks/stripe` (mirrors clerk webhook mount). (E) **`backend/src/routes/v1/index.ts`** — imported + mounted `billingRouter` at `/billing` after `/ai`. (F) **`backend/.env.example`** — added STRIPE_WEBHOOK_SECRET (REQUIRED at startup) + STRIPE_SECRET_KEY (forward-positioned; not used in this pass) with documentation block explaining the webhook signature verification + future-pass usage. Files modified this turn: `supabase/migrations/20260508150000_phase7_a1c_grant_credits.sql` (NEW, ~85 LOC), `backend/src/routes/webhooks/stripe.ts` (NEW, ~280 LOC), `backend/src/routes/v1/billing.ts` (NEW, ~210 LOC), `backend/src/index.ts` (+10/-1 — 1 import + 1 env-var entry + 1 route mount + comment block), `backend/src/routes/v1/index.ts` (+2 — 1 import + 1 route mount), `backend/.env.example` (+13 — STRIPE block), `specs/SYSTEM_CONTROL.md` (this entry). Post-edit verification: backend `tsc --noEmit` → 0 errors; frontend `tsc --noEmit` → 0 errors; `git status --short` shows the 3 new files + 6 modified backend files (3 from prior A1a/A1b passes already in working tree + 3 from this pass: index.ts, routes/v1/index.ts, .env.example) + this doc. NO frontend changes (zero edits in app/, components/, lib/). NO new dependencies (crypto is Node built-in; supabaseAdmin/Vault helpers pre-existing). NO outbound Stripe API calls (no checkout-session creation, no customer-portal URLs, no subscription mutations from backend → Stripe). NO async orchestration layer. NO queue abstraction. NO event-bus system. NO retry engine. NO subscription state-machine redesign — uses existing CHECK enum verbatim. NO RBAC expansion. NO entitlement engine. NO feature-access matrix. NO usage-tier orchestration. NO pricing derivation in code (credits-per-payment read from Stripe-side metadata only). HARD LOCK invariants preserved verbatim: actions_library template lookup gate, required-parameter validation, decision_history server-side org_id, automation engine boundary, META_INCREASE_BUDGET_MAX_PERCENT cap, partial unique index `(org_id, execution_id)`, recipient placeholder filter — all untouched. Phase locks preserved: Phase 0/1/2/3-linear/4-min/4-Part2/5 + 13 backend hardening passes + PHASE5_ENVELOPE_FOLLOWUP + Phase 6 Sub-pass A + Sub-pass B + Phase 7 Sub-pass A0 + A0.5 + A1a + A1b ALL UNTOUCHED. Closed-phase invariants intact: single-writer backend (webhook + billing routes write via supabaseAdmin service-role only — credit-RPC carve-out matches established creatives.ts/ai.ts/campaigns.ts precedent), org_id-from-JWT-only (every billing route uses `c.get('orgId')` only; webhook recovers org_id from Stripe-customer metadata or subscriptions.stripe_customer_id lookup — NEVER from request body), canonical envelope (all billing routes; webhook uses simple {received:true} matching webhooks/clerk pattern), request_id correlator chain (webhook stamps every log line; billing routes inherit from tracingMiddleware), deferred-router 503 gating preserved on `/decisions/*` + `/alerts/*`, legacy `decisions` table NOT reactivated (decision-generator.ts confirmed unchanged: 0 deduct_credits/log_ai_usage references), Phase 7 monetization status: substrate live (A0); hardened (A0.5); observability instrumented (A1a); enforcement live (A1b); Stripe substrate + plan upgrade + BYOK routes authored (A1c — this pass; deploy pending). Active hardened surface envelope coverage: 9 routers (was 8; billing added). **Backend monetization coverage matrix:** organizations.credits_balance ✅; deduct/refund/log_ai/grant RPCs ✅; ai_usage_logs accrual ✅ (A1a); credits_ledger writes ✅ (A0+A1c); subscriptions table state-tracked via webhook ✅ (A1c); plan upgrade route ✅ (A1c); BYOK store/delete routes ✅ (A1c); Stripe webhook signature-verified entrypoint ✅ (A1c). **Webhook coverage matrix:** customer.subscription.created → INSERT subscriptions ✅; customer.subscription.updated → UPDATE subscriptions ✅; customer.subscription.deleted → status='canceled' ✅; invoice.payment_succeeded → grant_credits (when metadata.credits_to_grant present) ✅; unknown event types → ack 200 + log ✅; signature failure → 400 ✅; missing secret → 500 ✅. **BYOK coverage matrix:** POST creates Vault secret + persists secret_id (orphan-safe replacement) ✅; DELETE Vault-deletes + clears column (idempotent) ✅; existing service-side BYOK_REQUIRED gate at creatives + ai-suggestions unchanged ✅. **Operator deploy prerequisites:** (1) deploy migration `20260508150000_phase7_a1c_grant_credits.sql` via Supabase MCP `apply_migration`; (2) populate `STRIPE_WEBHOOK_SECRET` env var in production (.env / PM2 ecosystem) BEFORE backend redeploy — without it, backend will fail-fast at startup; (3) configure Stripe webhook endpoint in Stripe Dashboard → Developers → Webhooks → endpoint URL `https://<host>/api/webhooks/stripe` with the 4 event types subscribed; (4) configure Stripe Customer creation flow to set `metadata.org_id` (forward-positioned for future checkout-flow pass; until then, org_id recovery falls back to subscriptions.stripe_customer_id lookup); (5) optionally configure Stripe Price metadata `credits_to_grant` to enable per-invoice automatic credit grants (otherwise webhook logs + skips). Phase X broader (MCP routing, tool governance, log-sink fan-out, strategy_tag enum) remains LOCKED. Phase 6 Sub-pass C (real Meta/Google CREATE handlers) DEFERRED. Sub-pass B (settings/billing/BYOK frontend UI) DEFERRED — backend substrate now COMPLETE; frontend wiring is the next user-visible unlock. Auto-firing automation_rules on AI decision stream remains DEFERRED (cross-phase). PHASE2_OAUTH_ENV_FAILFAST_FOLLOWUP, PHASE2_ENVELOPE_FOLLOWUP remain tracked. PHASE2_PARTITION_RLS_FOLLOWUP RESOLVED in A0.5. MIGRATION_TIMESTAMP_RECONCILIATION RESOLVED in continuation #12+repair sequence. Python creative runtime remains HOLD.

2026-05-08 (continuation #17) — Phase 7 Sub-pass A1b EXECUTED — route-level deduct_credits enforcement at canonical AI HTTP entrypoints. Pre-implementation precondition verification: Sub-pass A1a instrumentation present (2 + 2 + 5 = 9 `log_ai_usage` references across the 3 active sites; decision-generator.ts confirmed UNTOUCHED with 0 references); `deduct_credits` + `refund_credits` RPCs LIVE; `organizations.credits_balance` column LIVE; canonical pattern reference at `routes/v1/creatives.ts:80-101 + 117-152` re-read verbatim. Authorized scope: 2 route files. **Files modified: 2 route files only** plus this doc. (1) `backend/src/routes/v1/ai.ts` — added `supabaseAdmin` import + `AI_CALL_COST = 1` constant; doc-block updated to reflect the credit-RPC carve-out (the route now DOES touch supabaseAdmin for credit RPCs only — mirrors the established creatives.ts pattern; AI lifecycle still owned by service layer); 2 routes gated identically: `POST /decisions/generate` (line ~133) and `POST /execute` (line ~275). Each gate: inline plan_type SELECT → if !isLtd → `rpc('deduct_credits', {p_org_id, p_amount: AI_CALL_COST})` → if `newBalance < 0` → fail 402 `INSUFFICIENT_CREDITS` (canonical envelope, phase: 'request') WITHOUT any provider call; else `creditsDeducted = AI_CALL_COST`. Refund logic added in each catch block: `if (creditsDeducted > 0) refund_credits(...)` fire-and-forget BEFORE the existing AIPipelineError mapping. **Successful AI calls do NOT auto-refund** (per operator authorization); only failure paths refund. (2) `backend/src/routes/v1/campaigns.ts` — added `supabaseAdmin` import + `AI_SUGGESTIONS_COST = 1` constant; gate inserted in `POST /:id/ai-suggestions` route (line 309) BEFORE `generateAiSuggestions(orgId, id)` call. Same inline plan_type → !isLtd → deduct → 402-on-insufficient pattern. Refund added in catch block BEFORE existing error-code mapping (BYOK_REQUIRED / INSUFFICIENT_CREDITS / NOT_FOUND / unknown re-throw). The pre-existing INSUFFICIENT_CREDITS code mapping in the catch block (lines 324-326 pre-edit) remains — it now handles the case where downstream service throws INSUFFICIENT_CREDITS (e.g., a future Sub-pass A1c instrumentation inside the service); the route-level gate is the primary path for default-plan orgs in this pass. **Inline plan_type query rationale:** importing `resolveApiKey` from creatives module would introduce BYOK_REQUIRED throw on ai.ts (currently ai.ts uses platform key for everyone, including LTD without vault — a pre-existing behavior) — that is a behavior change beyond credit gating and out of operator-authorized scope. The 5-line inline `SELECT plan_type` query is duplicated across 3 sites (acceptable per "no helper abstraction extraction"); creatives.ts continues to use `resolveApiKey` since it ALSO needs the apiKey return value for actual provider authentication. ZERO frontend changes. ZERO new schema. ZERO new migrations. ZERO new env vars. ZERO new dependencies. ZERO new helpers. ZERO new middleware. ZERO new abstractions. ZERO new queue systems. ZERO retries. ZERO automatic balance seeding. ZERO implicit grants. ZERO pricing logic (constants are fixed integers; no token-rate derivation). ZERO Stripe. ZERO BYOK enforcement extension. ZERO Phase X work. ZERO orchestration. ZERO touching deprecated anomaly engine — `decision-generator.ts` confirmed UNTOUCHED. Pre-edit verification: confirmed 2 instrumentation targets correct (POST /decisions/generate + POST /execute in ai.ts; POST /:id/ai-suggestions in campaigns.ts); confirmed creatives.ts canonical pattern bit-for-bit (creditsDeducted = 0 init, isLtd skip, deduct → newBalance < 0 → 402, refund-on-failure path); confirmed neither route file had `supabaseAdmin` imported pre-edit. Post-edit verification: backend `tsc --noEmit` → 0 errors; frontend `tsc --noEmit` → 0 errors; `git status --short` shows the 2 authorized route files + Sub-pass A1a service files (already in working tree from continuation #16) + this doc. Files modified this turn: `backend/src/routes/v1/ai.ts` (+76/-3 — 1 import + 1 cost constant + 8-line doc carve-out + 2 gate blocks + 2 refund blocks), `backend/src/routes/v1/campaigns.ts` (+45/-1 — 1 import + 1 cost constant + 1 gate block + 1 refund block), `specs/SYSTEM_CONTROL.md` (this entry). HARD LOCK invariants preserved verbatim: actions_library template lookup gate, required-parameter validation, decision_history server-side org_id, automation engine boundary, META_INCREASE_BUDGET_MAX_PERCENT cap, partial unique index `(org_id, execution_id)`, recipient placeholder filter — all untouched. Phase locks preserved: Phase 0/1/2/3-linear/4-min/4-Part2/5 + 13 backend hardening passes + PHASE5_ENVELOPE_FOLLOWUP + Phase 6 Sub-pass A + Sub-pass B + Phase 7 Sub-pass A0 + Sub-pass A0.5 + Sub-pass A1a ALL UNTOUCHED. Closed-phase invariants intact: single-writer backend (RPCs invoked server-side via supabaseAdmin only — credit-gate carve-out matches established creatives.ts precedent), org_id-from-JWT-only (gate uses `c.get('orgId')` only — never client-supplied), canonical envelope (all new fail() calls emit canonical shape), request_id correlator chain unchanged, deferred-router 503 gating preserved on `/decisions/*` + `/alerts/*`, legacy `decisions` table NOT reactivated, decision-generator.ts excluded from instrumentation. Phase 7 monetization status: substrate live (A0); hardened (A0.5); observability instrumented (A1a); enforcement live (A1b — this pass). Active hardened surface envelope coverage unchanged at 8 routers. **Enforcement coverage matrix post-A1b:** ai/decisions/generate ✅; ai/execute ✅; campaigns/:id/ai-suggestions ✅; creatives/generate ✅ (pre-existing since Phase 5). **Refund coverage matrix:** ai routes refund on AI flow failure (any AIPipelineError or unknown error in catch); ai-suggestions refunds on any thrown error (BYOK_REQUIRED, INSUFFICIENT_CREDITS-from-service, NOT_FOUND, unknown); creatives.ts refunds on insert failure or Inngest dispatch failure (pre-existing). Successful calls do NOT auto-refund. **Operator deploy prerequisite:** active orgs with default `credits_balance=0` will receive `INSUFFICIENT_CREDITS` (402) on first AI call after this code lands. Operator decision required before backend redeploy: seed initial credits per org (direct UPDATE, or future grant_credits RPC). This is correct behavior — clean failure mode with structured 402 envelope vs prior unbounded cost path. Real Meta/Google CREATE handlers (Phase 6 Sub-pass C), Stripe webhook + plan upgrade routes + BYOK key storage routes (Sub-pass A1c), settings/billing/BYOK UI (Sub-pass B), automation auto-fire on AI stream all remain DEFERRED-BY-GOVERNANCE. PHASE2_PARTITION_RLS_FOLLOWUP RESOLVED in A0.5. PHASE2_OAUTH_ENV_FAILFAST_FOLLOWUP, PHASE2_ENVELOPE_FOLLOWUP remain tracked. Python creative runtime remains HOLD. Phase X broader remains LOCKED.

2026-05-08 (continuation #16) — Phase 7 Sub-pass A1a EXECUTED — observability-only AI usage instrumentation. Sub-pass A0.5 deploy precondition VERIFIED via live DB probe pre-edit: 15 migrations live (last `20260508140000_phase7_a05_post_deploy_hardening`); RPC search_path hardened (`proconfig: {search_path=public}` on all 3 RPCs); 9/9 partition RLS enabled with org-scoped policies; `ai_logs` table + RLS live. Authorized scope: 4 instrumentation targets identified by audit; 3 instrumented this pass; 1 explicitly EXCLUDED on governance grounds. **Files modified: 3 service files only.** (1) `backend/src/services/ai/execute-ai-decision.ts` — added `supabaseAdmin` import + fire-and-forget `log_ai_usage` call placed inside the persist `try` block AFTER successful `persistAIDecision` return (line ~270); full correlator chain available (org_id, user_id, request_id, trace_id, ai_decision_id, model); tokens 0/0 because `providerCall` is provider-agnostic thunk returning `unknown` (extracting OpenRouter `usage.{prompt_tokens,completion_tokens}` would couple this orchestration to a specific provider — architecture invariant per `ExecuteAIDecisionInput.providerCall` doc block). (2) `backend/src/services/campaigns/ai-suggestions.ts` — extended the `json` type to opportunistically expose OpenRouter `usage` block (this site is hard-coupled to OpenRouter via the hardcoded fetch URL — no provider-agnosticism invariant to violate); fire-and-forget `log_ai_usage` call after successful `validateSuggestions(parsed)` and BEFORE the `campaigns.ai_suggestions` UPDATE (so AI calls that succeed validation but fail persistence are still logged); user_id/request_id/trace_id/ai_decision_id NULL because function signature is `(orgId, campaignId)` only — extending the signature would exceed observability-only scope. (3) `backend/src/services/creatives/creative-generator.ts` — hoisted `copyModel` + `imageModel` constants (no behavior change; same effective values as prior inline UPDATE strings); fire-and-forget `log_ai_usage` calls in BOTH branches (copy + image) immediately after the `creative_generations` model UPDATE; user_id/request_id/trace_id/ai_decision_id NULL (Inngest job context — no HTTP correlators; creative generation writes to creative_generations not ai_decisions); tokens 0/0 (provider helpers don't surface usage). **EXCLUDED from instrumentation: `backend/src/services/intelligence/decision-generator.ts`.** Pre-implementation runtime evidence revealed it writes to the legacy `decisions` table (Phase 3 anomaly DEPRECATED + 503-gated per SYSTEM_CONTROL.md CANONICAL AI SYSTEM) and calls a non-existent RPC `deduct_credit` (singular, separate from Sub-pass A0's `deduct_credits` plural — no such function exists in the live DB). Per "RUNTIME EVIDENCE WINS" + "no closed-phase contamination" + governance prohibition on touching deprecated surfaces, instrumenting this file would: (a) effectively bless the deprecated path with new correlator infrastructure; (b) couple Phase 7 substrate (`log_ai_usage`) into Phase 3 anomaly DEPRECATED; (c) extend maintenance burden on legacy code. Decision documented in this entry; if the operator later authorizes anomaly-engine reactivation, instrumentation can land as part of that pass. Instrumentation pattern uniform across all 3 sites: `void supabaseAdmin.rpc('log_ai_usage', {...}).then(() => null, () => null)` — fire-and-forget, never throws into AI flow, observability writes never fail user-facing path. CONSTITUTION §3 "Fail Loudly" preserved — DB failure on observability writes goes to console (default Supabase client behavior); the AI flow has already completed successfully and returned its decision before this fire-and-forget runs. ZERO behavior changes to AI flow success path. ZERO new credit-deduction gates. ZERO new monetization enforcement. ZERO new schema. ZERO new migrations. ZERO new dependencies. ZERO frontend changes. NO Stripe. NO billing UI. NO orchestration work. NO Phase X unlock. NO provider integrations. p_cost_credits=0 across all sites per scope rule. Pre-edit verification: confirmed `decision-generator.ts` runtime target (deprecated) via grep; confirmed `executeAIDecision` callers (`routes/v1/ai.ts:133, 275` — full HTTP correlator chain); confirmed `generateAiSuggestions` caller (`routes/v1/campaigns.ts:317` — orgId only); confirmed `runGeneration` caller (`jobs/inngest.ts:174` — no HTTP correlators). Post-edit verification: backend `tsc --noEmit` → 0 errors; frontend `tsc --noEmit` → 0 errors; `git status --short` shows only the 3 authorized service files + this doc. Files modified: `backend/src/services/ai/execute-ai-decision.ts` (+33/-0 — 1 import + 1 instrumentation block), `backend/src/services/campaigns/ai-suggestions.ts` (+33/-1 — usage type extension + 1 instrumentation block), `backend/src/services/creatives/creative-generator.ts` (+45/-2 — model hoisting + 2 instrumentation blocks copy+image), `specs/SYSTEM_CONTROL.md` (this entry). HARD LOCK invariants preserved verbatim: actions_library template lookup gate, required-parameter validation, decision_history server-side org_id, automation engine boundary, META_INCREASE_BUDGET_MAX_PERCENT cap, partial unique index `(org_id, execution_id)`, recipient placeholder filter — all untouched. Phase locks preserved: Phase 0/1/2/3-linear/4-min/4-Part2/5 + 13 backend hardening passes + PHASE5_ENVELOPE_FOLLOWUP + Phase 6 Sub-pass A + Sub-pass B + Phase 7 Sub-pass A0 + Sub-pass A0.5 ALL UNTOUCHED. Closed-phase invariants intact: single-writer backend (RPC invoked server-side via supabaseAdmin only), org_id-from-JWT-only (instrumentation receives orgId from existing service-layer parameter chain; never client-supplied), canonical envelope (no route changes), request_id correlator chain (now embedded in ai_usage_logs rows when available at execute-ai-decision.ts site), deferred-router 503 gating preserved on `/decisions/*` + `/alerts/*`, legacy `decisions` table NOT reactivated (decision-generator.ts excluded from instrumentation), Phase 7 monetization NOT YET implemented (this pass is observability only — no Stripe, no billing UI, no plan upgrade flow, no enforcement). Active hardened surface envelope coverage unchanged at 8 routers. AI coverage matrix post-A1a: ai/execute (canonical pipeline) instrumented ✅; campaigns/ai-suggestions instrumented ✅; creative-generator (Inngest copy + image) instrumented ✅; decision-generator (Phase 3 anomaly DEPRECATED) explicitly excluded 🚫. Observability delta: ai_usage_logs DB rows now accumulate per AI call across the 3 active sites (alongside ai_logs which Sub-pass A0.5 already activated). Sub-pass A1b (deduct_credits enforcement on the same 3 sites) DEFERRED pending operator authorization. Sub-pass A1c (Stripe webhook + plan upgrade routes + BYOK key storage) DEFERRED. Sub-pass B (settings/billing/BYOK UI) DEFERRED. Phase 6 Sub-pass C (real Meta/Google CREATE handlers) DEFERRED. Phase X broader remains LOCKED. Python creative runtime remains HOLD.

2026-05-08 (continuation #15) — Phase 7 Sub-pass A0.5 AUTHORED (migration + 1-file backend edit; deploy pending). Three-deliverable post-deploy hardening pass scoped per operator authorization continuation #15: (A) RPC search_path hardening on the 3 RPCs added in Sub-pass A0 (closes 3 advisor `function_search_path_mutable` WARNs introduced this lineage); (B) PHASE2_PARTITION_RLS_FOLLOWUP — RLS+policy on 9 `campaign_metrics_*` partitions (closes 9 advisor `rls_disabled_in_public` ERRORs tracked since Phase 2 close); (C) AI log DB sink installation in `backend/src/index.ts` via the documented `setAILogSink(persistAILog)` hook (activates the documented-but-uninstalled `ai_logs` DB persistence; closes the CONSTITUTION §3 / CLAUDE.md §3 partial-violation observed in continuation #-N audits). Authored canonical migration `supabase/migrations/20260508140000_phase7_a05_post_deploy_hardening.sql` containing both (A) and (B) — purely additive: (A) `CREATE OR REPLACE FUNCTION` for `deduct_credits`, `refund_credits`, `log_ai_usage` with bit-identical bodies to Sub-pass A0 and only `SET search_path = public` added (SECURITY INVOKER preserved — no privilege escalation; only path-injection defense-in-depth); (B) 9 × `ALTER TABLE … ENABLE ROW LEVEL SECURITY` + matching `CREATE POLICY` per-partition with policy clause identical to the parent `campaign_metrics_org_isolation` policy `(org_id = (auth.jwt() ->> 'org_id'))`. Backend behavior unchanged in (A) — RPCs are functionally identical; behavior unchanged in (B) — backend reads `campaign_metrics` via parent table only with service-role which already bypasses RLS. Defense-in-depth posture only. Modified `backend/src/index.ts` (single hook installation block + helper) — added 2 imports (`setAILogSink, type AILogEntry` from `./utils/aiLogger.js`; `persistAILog` from `./services/ai/persistence.js`) + a 41-LOC fan-out sink installation block placed between SMOKE_ENDPOINTS_ENABLED constant and `app = new Hono(...)`. Sink behavior: (1) console output PRESERVED — every `[AI]` line still emits at the prior level (operator surface unchanged); (2) DB persistence ADDED — entries with `entry.org_id` are fire-and-forget written to `ai_logs` via `persistAILog`; entries without `org_id` (system/job-driven AI events without org context) skip the DB write to avoid `persistAILog`'s loud throw on missing org_id (which is itself a CONSTITUTION §3 invariant — must not write to DB without org isolation); (3) Promise from `persistAILog` is explicitly `void`-discarded (sink type is synchronous `void`); (4) `.catch(() => {})` swallow on the sink-side promise — `persistAILog` already emits its own console.error fallback for DB failures (per persistence.ts:217-219), so the sink-side catch is purely to satisfy the file-level unhandled-rejection process handler. NO modifications to closed Phase 3 surfaces — `aiLogger.ts` and `services/ai/persistence.ts` UNTOUCHED. The `safeStringify` helper inside aiLogger.ts is module-private; per minimal-diff principle, the equivalent helper was inlined in index.ts as `safeStringifyForSink` (10 LOC) rather than modifying the closed Phase 3 file to export it. Pre-edit verification: confirmed exact 9 partition list via `pg_inherits` query (`campaign_metrics_2026_q1` through `_2027_q4` + `_default`); confirmed all 9 have `rls_enabled=false` + `policy_count=0`; confirmed parent policy template `campaign_metrics_org_isolation` (cmd=ALL, USING `(org_id = (auth.jwt() ->> 'org_id'))`); confirmed `AILogSink` type is sync void + `persistAILog` is async — adapter required (implemented). Post-edit verification: backend `tsc --noEmit` → 0 errors; frontend `tsc --noEmit` → 0 errors; `git status --short` shows only the migration + `backend/src/index.ts` + this doc + an environmental `supabase/.temp/cli-latest` (CLI-managed temp file, not authored by this pass). NO frontend changes. NO Stripe integration. NO billing UI. NO orchestration work. NO new logging systems. NO parallel observability layer. NO new infra. NO architecture rewrites. NO speculative abstractions. NO Phase X unlock. NO provider integrations. NO campaign execution refactors. HARD LOCK invariants preserved verbatim: actions_library template lookup gate, required-parameter validation, decision_history server-side org_id, automation engine boundary, META_INCREASE_BUDGET_MAX_PERCENT cap, partial unique index `(org_id, execution_id)`, recipient placeholder filter — all untouched. Phase locks preserved: Phase 0/1/2/3-linear/4-min/4-Part2/5 + 13 backend hardening passes + PHASE5_ENVELOPE_FOLLOWUP + Phase 6 Sub-pass A + Sub-pass B + Phase 7 Sub-pass A0 ALL UNTOUCHED. Closed-phase invariants intact: single-writer backend (DB sink writes via `persistAILog` use the same `supabaseAdmin` service-role path; no new write authority), org_id-from-JWT-only (sink only writes when `entry.org_id` is set; never extracts from request body), canonical envelope (no route changes), request_id correlator chain (now embedded in `ai_logs` rows when `entry.request_id` is set), deferred-router 503 gating preserved on `/decisions/*` + `/alerts/*`, legacy `decisions` table NOT reactivated, Phase 7 monetization NOT YET implemented (this pass is hardening only — no Stripe, no billing UI, no plan upgrade flow, no instrumentation of AI call sites). Files modified: `supabase/migrations/20260508140000_phase7_a05_post_deploy_hardening.sql` (NEW, ~165 LOC), `backend/src/index.ts` (+57/-1 — 2 import lines + 41-LOC sink-install block + 14-LOC safeStringifyForSink helper), `specs/SYSTEM_CONTROL.md` (this entry). Phase 7 status: PARTIAL → PARTIAL (Sub-pass A0 substrate live; Sub-pass A0.5 hardening authored). PATCH QUEUE: Phase 7 Sub-pass A0.5 authored; deploy step pending via authorized Supabase MCP `apply_migration`. **NEXT REQUIRED OPERATOR ACTION:** deploy migration `20260508140000_phase7_a05_post_deploy_hardening.sql` via Supabase MCP `apply_migration`. Until deployed: 3 advisor WARNs + 9 advisor ERRORs persist as already-tracked drift; AI log DB sink TS code is in place but only activates after backend redeploy. After backend redeploy: `[AI]` console output preserved; `ai_logs` DB rows begin accumulating for org-scoped AI events. Sub-pass A1 (instrumentation: log_ai_usage at ai/execute + ai-suggestions + decision-generator + creative-generator call sites; deduct_credits enforcement on previously unmetered AI surfaces) DEFERRED pending operator authorization. Sub-pass B (settings/billing/BYOK UI) DEFERRED. Phase 6 Sub-pass C (real-mode Meta/Google CREATE handlers) DEFERRED. Phase X broader remains LOCKED. Python creative runtime remains HOLD.

2026-05-08 (continuation #14) — Phase 7 Sub-pass A0 AUTHORED (migration only; deploy pending). Authored canonical migration `supabase/migrations/20260508130000_phase7_a0_credits_substrate.sql` per the holistic governance recommendation that closing the Phase 5 ↔ Phase 7 silent runtime drift is the highest-priority active-surface gap (RUNTIME ECONOMIC SAFETY RULE + ACTIVE SURFACE PRIORITY RULE). Strategic deviation from PATCH QUEUE strict order #9 (Phase X broader) operator-authorized continuation #11; this pass continues that thread by addressing the runtime-safety/cost-protection/monetization-substrate triad before any feature expansion. Migration is purely additive: (1) `organizations.credits_balance INTEGER NOT NULL DEFAULT 0 CHECK (>= 0)` — additive column on the closed Phase 0/1 organizations table (mirrors Phase 5 precedent of additive plan_type + vault_byok columns); (2) `ai_usage_logs` table with org_id RLS + 3 indexes (per-call AI tracking per Phases.md §Phase 7 NEW "ai_usage_logs, cost_per_call"); (3) `credits_ledger` table with org_id RLS + 2 indexes (immutable audit trail of every credits delta); (4) `deduct_credits(p_org_id TEXT, p_amount INTEGER) RETURNS INTEGER` — atomic conditional UPDATE returning new balance OR -1 if insufficient (matches existing route caller contract `creatives.ts:90 if (newBalance < 0)`); (5) `refund_credits(p_org_id TEXT, p_amount INTEGER) RETURNS INTEGER` — atomic UPDATE add (caller ignores return value); (6) `log_ai_usage(...) RETURNS UUID` — INSERT into ai_usage_logs + conditional credits_ledger row (forward-positioning; no current callers; Sub-pass A1 will instrument call sites). All RPCs validate p_amount > 0; deduct/refund append to credits_ledger atomically with the balance change. ZERO backend code changes — the existing `creatives.ts:86` `supabaseAdmin.rpc('deduct_credits', {p_org_id, p_amount})` and `creatives.ts:121,148 + creative-generator.ts:208` `supabaseAdmin.rpc('refund_credits', ...)` call sites remain unchanged; once this migration is deployed via `supabase db push`, the RPC names resolve to live functions and the active hardened Phase 5 surface becomes operational for default-plan orgs (currently broken: every non-LTD org hitting POST /creatives/generate triggers Postgres 42883 "function deduct_credits does not exist"). Pre-edit verification: confirmed exact runtime contracts via grep on `routes/v1/creatives.ts` and `services/creatives/creative-generator.ts`; confirmed organizations.org_id is TEXT NOT NULL via live DB introspection; confirmed credits_balance column absent + ai_usage_logs/credits_ledger tables absent via live DB introspection; confirmed deduct_credits/refund_credits/log_ai_usage RPCs absent via pg_proc query. Post-edit verification: backend `tsc --noEmit` → 0 errors (no TS modified); frontend `tsc --noEmit` → 0 errors; `git status --short` shows only the new migration file (plus Sub-pass A + B artifacts already in working tree). NO frontend changes. NO Stripe integration. NO new Stripe API client. NO webhook handler. NO billing routes. NO BYOK key storage routes. NO AI usage logging instrumentation (call sites deferred to Sub-pass A1). NO settings/billing/BYOK UI (Sub-pass B). NO refactors. NO new dependencies. NO closed-phase code touched. NO Phase X broader work. NO Phase 6 Sub-pass C work. NO Python runtime expansion. HARD LOCK invariants preserved verbatim: actions_library template lookup gate, required-parameter validation, decision_history server-side org_id, automation engine boundary, META_INCREASE_BUDGET_MAX_PERCENT cap, partial unique index `(org_id, execution_id)`, recipient placeholder filter — all untouched. Phase locks preserved: Phase 0/1/2/3-linear/4-min/4-Part2/5 + 13 backend hardening passes + PHASE5_ENVELOPE_FOLLOWUP closure + Phase 6 Sub-pass A + Sub-pass B closure ALL UNTOUCHED. Closed-phase invariants intact: single-writer backend (RPCs invoked server-side via supabaseAdmin service-role only), org_id-from-JWT-only enforcement (RPCs receive p_org_id from server-side caller; never client-supplied), canonical envelope (no route changes), request_id correlator chain (RPCs do not emit logs; route-level logging unchanged), deferred-router 503 gating preserved on `/decisions/*` + `/alerts/*`, legacy `decisions` table NOT reactivated, Phase 7 monetization NOT YET implemented (this is substrate only — no Stripe, no billing UI, no plan upgrade flow). Files modified: `supabase/migrations/20260508130000_phase7_a0_credits_substrate.sql` (NEW, 198 LOC), `specs/SYSTEM_CONTROL.md` (this entry). Phase 7 status: LOCKED → PARTIAL (Sub-pass A0 substrate authored). PATCH QUEUE: Phase 7 Sub-pass A0 authored; deploy step pending via authorized `supabase db push`. **NEXT REQUIRED OPERATOR ACTION:** deploy migration via Supabase MCP `apply_migration` to make the substrate live. Until deployed, the Phase 5 silent bug remains active. After deploy verification: `creatives.ts:POST /generate` operational for default-plan orgs; ledger + usage-log substrate available for Sub-pass A1 instrumentation. Real Meta/Google CREATE handlers (Sub-pass C), Stripe webhook + plan upgrade routes + BYOK key storage (Sub-pass A1), settings/billing/BYOK UI (Sub-pass B), automation auto-fire on AI stream all remain DEFERRED-BY-GOVERNANCE. PHASE2_PARTITION_RLS_FOLLOWUP, PHASE2_OAUTH_ENV_FAILFAST_FOLLOWUP, PHASE2_ENVELOPE_FOLLOWUP, MIGRATION_TIMESTAMP_RECONCILIATION remain tracked. Python creative runtime remains HOLD. Phase X broader remains LOCKED.

2026-05-08 (continuation #13) — Phase 6 (Campaigns) Sub-pass B EXECUTED — frontend wiring of 3 campaigns pages + opportunistic api-client envelope unification. Files modified: `lib/api-client.ts` (+39/-3 — detection-based canonical-envelope auto-unwrap; backwards-compatible with Phase 2 legacy shapes; folds in PHASE2_ENVELOPE_FOLLOWUP coordinated FE update for the wired-frontend axis), `app/campaigns/page.tsx` (+131/-83 — wired GET /api/v1/campaigns with platform/status server-filters + client-side search; loading/error/empty states; preserved deferred-section mocks: AD_SETS panel / CREATIVE_GRADIENTS preview / AI Insight red card / Quick Actions buttons / Bulk Actions bar / Account Snapshot / AI Strategy / Platform Allocation), `app/campaigns/[id]/page.tsx` (+267/-52 — useParams routing + GET /api/v1/campaigns/:id; wired Launch AI Audit → POST /:id/ai-suggestions with persisted state + suggestions panel; wired Push button → POST /:id/push gated to draft|paused statuses with explicit "simulated mode" banner; replaced TREND_BARS with backend `metrics.trend_14d` normalized bars; preserved deferred-section mocks: ACTIONS recommendation cards / Active Ad Sets table / Top Creatives row / AI Strategic Insight / Recommendations / Risk Analysis), `app/campaigns/create/page.tsx` (+82/-30 — Step 0 Campaign Name input added per backend POST /api/v1/campaigns required-field contract; wired Save Draft → POST /api/v1/campaigns with first-valid-platform fallback ('meta' or 'google' from selected; warns on TikTok/Snapchat-only selection); built `targeting` JSONB from {objective, locations, age_min, age_max, interests, budget_type, bidding_strategy}; on success router.push to `/campaigns/${created.id}`; **Launch button kept UNWIRED** per pre-implementation audit — local state only; defer to Sub-pass C alongside real-mode Meta/Google CREATE handlers per "Keep create_campaign execution simulated-only" constraint), `specs/SYSTEM_CONTROL.md` (this entry). NO backend changes this turn. NO new files. NO new dependencies. NO schema changes. NO new components extracted. NO speculative UI redesign. NO new infra layers. NO Phase X work. NO Phase 7 work. NO Python runtime expansion. Pre-edit verification confirmed: 3 frontend pages had zero `apiClient`/`/api/v1/campaigns` references (true mocked-shells); all 8 Phase 2 wired callers reviewed for envelope-detection backwards-compat (Phase 2 legacy shapes lack `success` key → detection skips → unchanged behavior). Post-edit verification: backend `tsc --noEmit` → 0 errors; frontend `tsc --noEmit` → 0 errors; `git status --short` shows only the 4 authorized frontend files + this doc + Sub-pass A artifacts (3 backend files + migration). Phase 2 wired pages compatibility verified: integrations/page.tsx (bare-array Integration[] → no envelope match → passthrough ✓), integrations connect/start ({authUrl,state} → no `success` key → passthrough ✓), dashboard/overview/page.tsx (MetricsSummary → no `success` key → passthrough ✓), dashboard/channels/page.tsx (ChannelMetrics[] array → not object envelope → passthrough ✓). Side-effect improvements (governance-positive): canonical-enveloped Phase 2 endpoints (overview campaigns list, integrations callback) and the dormant decisions/[id] action-execute call now correctly unwrap to inner data — latent bugs in those code paths now resolved (the decisions/[id] page itself remains 503-gated, so no behavior change at the user-facing layer). HARD LOCK invariants preserved verbatim: `actions_library` template lookup gate, required-parameter validation, `decision_history` server-side org_id, automation engine boundary, META_INCREASE_BUDGET_MAX_PERCENT cap, partial unique index `(org_id, execution_id)`, recipient placeholder filter — all untouched (different files). Phase locks preserved: Phase 0/1/2/3-linear/4-min/4-Part2/5 + 13 backend hardening passes + Phase 6 Sub-pass A closure + PHASE5_ENVELOPE_FOLLOWUP closure ALL UNTOUCHED. Closed-phase invariants intact: single-writer backend (frontend continues to call ONLY backend API; never touches Supabase directly), org_id enforcement (every wired call passes through Clerk JWT → backend extracts org_id server-side), canonical envelope (api-client now unwraps it correctly), request_id correlator chain (backend embeds it in every response), deferred-router 503 gating preserved on `/decisions/*` + `/alerts/*`, legacy `decisions` table NOT reactivated (campaigns service still tolerates schema-drift on overlay reads), Phase 7 monetization NOT implemented (BYOK substrate columns unchanged; ai-suggestions BYOK-aware via existing `resolveApiKey`). Active hardened surface envelope coverage now end-to-end through frontend for: campaigns list/detail/create + ai-suggestions + push. Phase 6 frontend wiring of `app/campaigns/*` complete; Sub-pass C (real-mode handlers) remains pending operator authorization. Phase 6 status: PARTIAL → Sub-pass A + B closed; Sub-pass C still deferred. Real Meta/Google CREATE handlers remain DEFERRED. Realtime channels, adsets/recommendations/risks/ai_insights tables, bulk actions, inline-edit, sort/page query params remain DEFERRED-BY-GOVERNANCE. PHASE2_PARTITION_RLS_FOLLOWUP remains tracked. MIGRATION_TIMESTAMP_RECONCILIATION remains tracked. PHASE2_OAUTH_ENV_FAILFAST_FOLLOWUP remains deferred. Python creative runtime remains HOLD. Phase X broader remains LOCKED. Phase 7 remains LOCKED.

2026-05-08 (continuation #12) — Phase 6 Sub-pass A migration DEPLOYED via authorized Supabase MCP `apply_migration`. Live registry entry: `20260508175638_phase6_create_campaign_templates` (server-side timestamp; differs from local file timestamp `20260508120000` — see Known follow-up below). Pre-deploy state verified: 12 prior migrations + 5 actions_library templates (none with `create_campaign` action_type). Post-deploy state verified: 13 migrations live, 7 actions_library rows total, 2 `create_campaign` rows (one per platform). Natural-key lookup `(platform, action_type='create_campaign')` resolves to live ids `bd2faa18-d4a6-46e2-af1c-f13c33166ca8` (meta) + `7c639eb5-f2ef-47dd-80a5-f0346b036a5f` (google) — service code is now decoupled from any hardcoded UUID. Phase 0–5 invariant verification (post-deploy SQL probe): `actions_library` RLS=true ✅; `campaigns.org_id` NOT NULL=true ✅; `ad_accounts` RLS policy count=1 ✅; `decision_history` `(org_id, execution_id)` partial unique index present=1 ✅; `ai_decisions` exists ✅; `brand_kits` exists ✅; `creatives` exists ✅; `campaigns` exists ✅; `automation_rules` exists ✅. Security advisor lints captured (all PRE-EXISTING, none introduced by this migration): legacy `public.decisions` RLS-disabled with policies (Phase 3 anomaly DEPRECATED — documented drift, governance-tolerated per CANONICAL AI SYSTEM); `public.set_updated_at_now` function search_path mutable (pre-existing, unrelated); `public.campaign_metrics_*` partition RLS-disabled across 9 partitions (Phase 2 inheritance gap — partitions do not auto-inherit parent RLS in this Postgres setup; flagged as PHASE2_PARTITION_RLS_FOLLOWUP, NOT a Phase 6 regression). Phase 6 PHASE COMPLETION STATUS section: Sub-pass A deploy step PENDING → COMPLETE. PATCH QUEUE position #13 advanced; Sub-pass B + Sub-pass C remain pending operator authorization. Phase 6 status remains PARTIAL (frontend wiring + real-mode handlers still deferred). pushCampaign operational path now end-to-end: campaign lookup → status check → cross-account ownership guard → natural-key template resolution → executeAction dispatch → simulated `create_campaign` handler → decision_history `result='success'` row inserted with full request_id/trace_id correlator chain. CROSS_ACCOUNT_FORBIDDEN guard data dependencies intact: `ad_accounts` table live, `ad_accounts.org_id` column NOT NULL, `ad_accounts` RLS policy enforcing `org_id = auth.jwt()->>'org_id'`, single-SELECT `(id, org_id)` lookup remains O(1) via `idx_ad_accounts_org_id` + PRIMARY KEY index. NO HTTP-level smoke test executed (out of session scope; would require live Clerk JWT + live campaign row); operator may smoke-test post-deploy via curl with valid token if desired. Files modified this turn: `specs/SYSTEM_CONTROL.md` only (this entry + Phase 6 PARTIAL→deploy-complete reflection + DATABASE STATE migration entry server-timestamp note). Backend tsc → 0 errors (no code changed); frontend tsc → 0 errors (no code changed). Phase locks preserved: Phase 0/1/2/3-linear/4-min/4-Part2/5 + 13 backend hardening passes + PHASE5_ENVELOPE_FOLLOWUP closure ALL UNTOUCHED. Architecture invariants intact: single-writer backend, org_id enforcement, canonical envelope, request_id correlator chain, deferred-router 503 gating preserved on `/decisions/*` + `/alerts/*`, legacy `decisions` table NOT reactivated (still tolerated-degrade only), Phase 7 monetization NOT implemented. Known follow-ups (non-blocker; tracked): (a) **MIGRATION_TIMESTAMP_RECONCILIATION** — local migration file is named `20260508120000_phase6_create_campaign_templates.sql` but Supabase MCP `apply_migration` recorded the live registry version as `20260508175638` (server-side apply timestamp). On next operator-side `supabase db push`, the CLI may treat these as different migrations. Recommended reconciliation: rename local file to `20260508175638_phase6_create_campaign_templates.sql` before next push, OR resolve via `supabase migration repair --status applied 20260508120000` (Supabase CLI documented path). The migration body itself is idempotent (ON CONFLICT DO NOTHING) so a re-apply is harmless to the data — only the migration history table needs reconciliation. (b) **PHASE2_PARTITION_RLS_FOLLOWUP** — `campaign_metrics_*` partitions did not inherit RLS from the partitioned parent table; 9 partitions surface as advisor ERRORs. Phase 2 hardening item, NOT a Phase 6 regression. Operator decision required: enable RLS+policy per-partition OR migrate to ATTACH PARTITION semantics that propagate parent RLS. Python creative runtime remains HOLD. Phase X broader remains LOCKED. Phase 7 remains LOCKED.

2026-05-08 (continuation #11) — Phase 6 (Campaigns) Sub-pass A AUTHORIZED + EXECUTED (backend bug-fix + governance-invariant only; deploy + frontend wiring deferred). Strategic deviation from PATCH QUEUE strict order #9 (Phase X broader) explicitly authorized by operator: "Productization/business-side progression takes priority before deeper Phase X infrastructure expansion." Deviation governance-justified by (1) runtime evidence override — `pushCampaign` had a silent bug where hardcoded fictional UUIDs (`...000000009`/`...000000010`) were never seeded in `actions_library`, so every push attempt 404'd on template lookup; (2) Phase 6 NEW invariant from Phases.md ("Validate ad account ownership / Prevent cross-account execution") was unmet on `pushCampaign`; (3) Phase X broader's value-prerequisites (real user load on AI orchestration layer) are not yet met. Authored canonical migration `supabase/migrations/20260508120000_phase6_create_campaign_templates.sql` — purely additive: 2 INSERT rows into `actions_library` (`meta.create_campaign` + `google.create_campaign`) idempotent via existing `(platform, action_type)` UNIQUE; no fixed UUIDs (gen_random_uuid() default; service code resolves by natural key); no schema/RLS/FK/index changes. Updated `backend/src/services/campaigns/campaigns.ts:pushCampaign`: removed two hardcoded fictional UUID constants; added natural-key template lookup via `(platform, action_type='create_campaign')` against `actions_library`; added Phase 6 NEW invariant cross-account ownership check that verifies `campaign.ad_account_id` (when set) resolves to an `ad_accounts` row with matching org_id, throwing `CROSS_ACCOUNT_FORBIDDEN` otherwise. Updated `backend/src/routes/v1/campaigns.ts:POST /:id/push` catch block to map `CROSS_ACCOUNT_FORBIDDEN` → 403 with canonical envelope (mirrors existing FORBIDDEN mapping pattern from PATCH /:id admin-only archive transition). Updated `backend/src/services/execution/action-executor.ts` ACTION_HANDLERS with a `create_campaign` simulated-only entry returning success+`simulated:true` (matches established pause_campaign/increase_budget/decrease_budget simulated-fallthrough pattern); decision_history reflects `result='success'` rather than the alternative no-handler graceful-fail with `result='failed'`. Real-mode Meta Ads CREATE + Google Ads CREATE handlers explicitly DEFERRED per operator-authorized criteria (external-write risk, provider complexity, startup-env coupling, surface expansion) — to be added in a later sub-pass alongside frontend wiring (which will reveal real param shape from `app/campaigns/create/page.tsx`). Files modified: `supabase/migrations/20260508120000_phase6_create_campaign_templates.sql` (NEW, 50 LOC), `backend/src/services/campaigns/campaigns.ts` (+62/-3, replaces UUID constants block + adds two new guarded blocks in pushCampaign), `backend/src/routes/v1/campaigns.ts` (+7, single error-mapping branch), `backend/src/services/execution/action-executor.ts` (+22, single ACTION_HANDLERS entry), `specs/SYSTEM_CONTROL.md` (this entry). Frontend: ZERO changes — Phase 6 frontend wiring is Sub-pass B scope (deferred to next operator-authorized pass). Pre-edit verification confirmed: zero `apiClient`/`/api/v1/campaigns` references across `app/campaigns/*` (3 mocked-shell pages remain uncoupled — `app/campaigns/{page,create/page,[id]/page}.tsx`); `executeAction` handler dispatch already non-crashing on unknown action_types (logs `result='failed'` to decision_history); `ad_accounts` schema has both `org_id` + `integration_id` columns enabling single-SELECT cross-account check; `campaigns.ad_account_id` is bare UUID column (NOT FK to `ad_accounts(id)` per 20260503100000:36) — application-layer guard chosen over schema FK addition per minimal-diff principle (FK addition deferred). Post-edit verification: backend `tsc --noEmit` → 0 errors; frontend `tsc --noEmit` → 0 errors; `git status --short` shows only the four authorized files + new migration + this doc. Phase 6 status: DEFERRED → PARTIAL. CURRENT PHASE updated. PATCH QUEUE: Phase 6 sub-pass A executed; deploy step pending (`supabase db push`); Sub-pass B (frontend wiring) pending operator authorization. NOT lifted: no 503 gates required (campaigns router was never gated). NO real-mode external Meta/Google API write paths added. NO new env vars / feature flags / `LIVE_FLAG_DEPENDENCIES` entries. NO automation engine extension (Phase 4 Part 2 boundary preserved). NO post-persist hook in Phase 3 linear pipeline. NO Phase X broader work. NO Phase 7 monetization work. NO Python creative runtime expansion. HARD LOCK invariants preserved verbatim: `actions_library` template lookup gate (now natural-key not fictional UUID), required-parameter validation, decision_history server-side org_id, automation engine boundary, META_INCREASE_BUDGET_MAX_PERCENT cap, partial unique index `(org_id, execution_id)`, recipient placeholder filter — all untouched. Phase locks preserved: Phase 0/1/2/3-linear/4-min/4-Part2/5 + 13 backend hardening passes + PHASE5_ENVELOPE_FOLLOWUP closure state ALL UNTOUCHED. Closed-phase invariants intact: single-writer backend, org_id enforcement (cross-account guard reads `c.get('orgId')` only via `pushCampaign(orgId, ...)`, never trusts client-supplied values), canonical envelope, request_id correlator chain, deferred-router 503 gating preserved on `/decisions/*` + `/alerts/*`, legacy `decisions` table tolerance unchanged, Phase 7 monetization NOT implemented (substrate columns unchanged). Active hardened surface envelope coverage unchanged at 8 routers. NEXT ACTION: operator runs `supabase db push` to deploy migration `20260508120000_phase6_create_campaign_templates.sql` to production; once verified, `pushCampaign` operational path closes the silent bug end-to-end and the Phase 6 NEW invariant becomes live-enforced. Sub-pass B (frontend wiring of `app/campaigns/{page, [id]/page, create/page}.tsx` + opportunistic `lib/api-client.ts` envelope-unwrap unification covering PHASE2_ENVELOPE_FOLLOWUP) requires its own operator authorization. Real-mode Meta/Google CREATE handlers, realtime channels, adsets table, recommendations/risks/ai_insights tables, bulk actions, inline-edit, sort/page query params remain DEFERRED-BY-GOVERNANCE per the holistic strategic recommendation. Python creative runtime remains HOLD. Phase X broader remains LOCKED.

2026-05-08 (continuation #10) — PHASE5_ENVELOPE_FOLLOWUP CLOSED. Authorized scope-limited hardening: canonicalized `backend/src/routes/v1/creatives.ts` + `backend/src/routes/v1/brand-kit.ts` from legacy envelope (`c.json({ error: '...' }, status)` / bare-array / raw-record shapes) onto Phase 1 canonical `ok()/fail()` envelope from `backend/src/utils/response.ts`. 27 sites in creatives.ts + 16 sites in brand-kit.ts converted (43 total replacements + 2 import-line additions). Error responses now carry `code` + (where applicable) `field` extras matching automation.ts/auth.ts/ai.ts/actions.ts/history.ts/campaigns.ts hardened-surface pattern (`INTERNAL`, `INVALID_JSON`, `INVALID_TYPE`, `INVALID_BODY`, `MISSING_PARAMETER`, `NOT_FOUND`, `INSUFFICIENT_CREDITS`, `BYOK_REQUIRED`, `PAYLOAD_TOO_LARGE`). All success responses now flow through `ok(c, data, status?)` and emit `{ success: true, data, request_id }`. Files modified: `backend/src/routes/v1/creatives.ts` (+30/-25), `backend/src/routes/v1/brand-kit.ts` (+15/-18), `specs/SYSTEM_CONTROL.md` (this entry + Phase 5 Pending checkbox flip + NEXT ACTION I-clause flip + CURRENT EXECUTION TARGET non-blocker line update + PATCH QUEUE position #12 closure entry). Pre-edit verification confirmed: zero `apiClient`/`/api/v1/creatives`/`/api/v1/brand-kit` references across `app/` and `lib/` (5 mocked-shell pages remain uncoupled — `app/creatives/{page,archive,brand-kit,editor,results}/page.tsx`); zero schema, zero migrations, zero middleware, zero router-mount changes, zero env changes, zero new dependencies, zero new runtime surfaces. Post-edit verification: backend `tsc --noEmit` → 0 errors; frontend `tsc --noEmit` → 0 errors; `git status --short` shows only the two authorized routes + this doc. HARD LOCK invariants preserved verbatim: actions_library template lookup, required-parameter validation, decision_history server-side org_id, automation engine boundary, META_INCREASE_BUDGET_MAX_PERCENT cap, partial unique index `(org_id, execution_id)`, recipient placeholder filter — all untouched (different files). Phase locks preserved: Phase 0/1/2/3-linear/4-min/4-Part2/5 + 13 backend hardening passes + Python creative runtime tooling HOLD state — ALL UNTOUCHED. Closed-phase invariants intact: single-writer backend, org_id enforcement (every Phase 5 route still reads `c.get('orgId')` only, never trusts client-supplied org_id), canonical envelope on hardened active surface, request_id correlator chain (now extended to Phase 5 routes), deferred-router 503 gating preserved on `/decisions/*` + `/alerts/*`, legacy `decisions` table NOT reactivated, Phase 7 monetization NOT implemented (substrate columns unchanged). Active hardened surface envelope coverage extended from 6 → 8 routers (auth/ai/actions/history/campaigns/automation/+creatives/+brand-kit). Frontend remains uncoupled: when Phase 6 frontend wiring eventually lands, it can adopt the canonical `lib/api-client.ts` unwrap pattern directly (no compatibility shim required). Remaining non-blocker queue items: PHASE2_ENVELOPE_FOLLOWUP (still requires coordinated FE update), PHASE2_OAUTH_ENV_FAILFAST_FOLLOWUP (still requires startup-behavior decision). Python runtime tooling remains GOVERNANCE-SAFE TOOLING / ENVIRONMENT in HOLD state — no expansion this turn.

2026-05-07 (continuation #9) — Python creative runtime DRIFT RECONCILED against the now-canonical `specs/python-creative-runtime.md` (authoritative for Python boundaries, DSPy usage, ffmpeg/media tooling, future diffusion preparation, future API-provider preparation, architecture isolation, governance constraints). Operator-confirmed authorization: option 4a + 5a (keep `requests`, document `test_dspy.py`). Strict TOOLING / ENVIRONMENT scope; zero production runtime modifications. Reconciled three documented drift items: (1) DSPy now pinned in `tools/python-creative/requirements.txt` as `dspy>=2.6,<3.0` matching operator-installed 2.6.27; (2) `dspy` added to REQUIRED_IMPORTS in `tools/python-creative/verify_runtime.py` so a fresh setup verification covers DSPy importability; (3) `requests` added to `specs/python-creative-runtime.md` § CURRENT INSTALLED LIBRARIES with explicit "operator-side HTTP convenience; NEVER imported by production backend" boundary. README dependency table extended with DSPy row + spec-authority cross-references; new "Verification scripts" section documents BOTH `verify_runtime.py` (canonical full verifier) AND `test_dspy.py` (operator-quick-check supplement). README authority pointer updated: canonical spec is now `specs/python-creative-runtime.md`, with `specs/SYSTEM_CONTROL.md` continuation #8 + #9 as execution-lock cross-reference. NO new libraries added beyond DSPy reconciliation. NO provider API keys added. NO diffusers/transformers/torch/etc. (remain governance-PENDING per spec § FUTURE GOVERNED CANDIDATE LIBRARIES). NO HTTP services / workers / queues / schedulers / orchestration logic. NO supabase-py. NO backend code touched. NO frontend code touched. NO migration changes. NO phase unlocks. NO architectural expansion. Files modified: `tools/python-creative/{requirements.txt, verify_runtime.py, README.md}`, `specs/python-creative-runtime.md` (added requests entry), `specs/SYSTEM_CONTROL.md` (this entry). Architecture invariants intact: Frontend → Backend API → Service Layer → Database remains canonical; Python runtime sits BESIDE the architecture, never inside it; Node backend retains sole production authority; org_id-from-JWT-only preserved; canonical envelope on hardened active surface preserved; request_id correlator chain preserved; all 13 prior closed backend hardening passes preserved; Phase 0 + Phase 1 + Phase 2 + Phase 3 (linear) + Phase 4 minimal + Phase 4 Part 2 + Phase 5 closure states UNTOUCHED. Backend tsc → 0 errors (no TS modified). Frontend tsc → 0 errors. Python runtime classification remains GOVERNANCE-SAFE TOOLING / ENVIRONMENT.

2026-05-07 (continuation #8) — Governance-safe Python creative runtime substrate AUTHORIZED + ADDED. TOOLING / ENVIRONMENT scope only — NOT a new phase, NOT a service, NOT a worker, NOT an orchestration layer. Created `tools/python-creative/` containing: (1) `requirements.txt` pinning conservative-major-version creative/media libraries (Pillow, imageio, numpy, requests, python-dotenv, ffmpeg-python) — all operator-tooling-grade; (2) `README.md` documenting purpose + scope + explicit forbidden-uses list + architecture-position diagram showing the Python env runs ONLY on operator local machines and is NEVER imported by production runtime; (3) `verify_runtime.py` read-only verification script that confirms Python ≥ 3.9, every requirements.txt module is importable, and the SYSTEM ffmpeg binary is on PATH — performs ZERO network calls, ZERO DB calls, ZERO filesystem writes outside stdout; (4) `.gitignore` entries for `.venv/`, `__pycache__/`, `*.pyc`, `*.pyo`, `.pytest_cache/`, `.mypy_cache/`, `*.egg-info/` (committed: requirements + README + verify_runtime; ignored: venv + bytecode). Per TOOLING / EXECUTION CONSTRAINT: operates inside repository architecture (`tools/` subdir), supports system control flow without replacing it, creates no architectural drift, does not bypass canonical migrations/runtime/logging/validation, aligned with closed-Phase-5 state. Forbidden patterns explicitly avoided: no FastAPI/Flask/Django service, no Celery/RQ/Dramatiq worker, no queues, no schedulers, no supabase-py production writers, no routing changes, no schema migrations, no frontend wiring, no MCP scaffolding, no automation extensions. Working tree files added: `tools/python-creative/{requirements.txt, README.md, verify_runtime.py}`. Files modified: `.gitignore` (Python venv ignores). NO backend code touched. NO frontend code touched. NO migration changes. NO contract changes. Backend tsc → 0 errors (no TS files modified). Frontend tsc → 0 errors. Phase locks preserved verbatim: Phase 0/1/2/3-linear/4-min/4-Part2/5 + 13 hardening passes ALL UNTOUCHED. The Python environment sits beside the production stack as TOOLING — Frontend → Backend API → Service Layer → Database remains the sole production data-flow per SPAGHETTI PREVENTION RULE; this Python runtime never touches any of those four boxes from a production perspective.

2026-05-07 (continuation #7) — Phase 5 (AI Creatives) CLOSED. Operator confirmed: PR #6 merged into `main` (commit `f5aa8ff`); local main synced; `supabase db push` completed; 3 canonical Phase 5 tables (brand_kits, creative_generations, creatives) + 2 organizations columns (plan_type, vault_byok_openrouter_secret_id) + 1 private storage bucket (`creatives`) verified live in production Supabase. Executed final unlock step: lifted `/creatives/*` + `/brand-kit/*` 503 gates in `backend/src/routes/v1/index.ts` (single multi-line removal at lines 99-100 + governance comment block; +10/-2 LOC; minimal-diff). Preserved: 2 remaining deferred gates (`/decisions/*`, `/alerts/*`); `/creatives` + `/brand-kit` router mounts at lines 121-122 unchanged; all closed-slice invariants verbatim. Phase 5 status: PARTIAL → CLOSED. CURRENT PHASE updated. Phase 5 PHASE COMPLETION STATUS section: Pending → Completed (deployed + verified + gates lifted); Exit Gate all ✅. PATCH QUEUE position #8 closed. NEXT ACTION + CURRENT EXECUTION TARGET reflect closure: Phase X broader (#9) is now next governed item, requiring explicit unlock authorization. DATABASE STATE consolidated: 12 migrations all in unified Live list (Phase 5 promoted from "authored not deployed" to live). LAST UPDATE entry continuation #7 records closure. Backend tsc → 0 errors. Frontend tsc → 0 errors. Phase locks preserved: Phase 4 Part 2 UNTOUCHED; Phase 4 minimal slice UNTOUCHED (HARD LOCK invariants verbatim); Phase 3 linear pipeline UNTOUCHED; Phase 2 UNTOUCHED at column level; legacy `decisions` table NOT reactivated. Architecture invariants intact: single-writer backend, org_id enforcement (every Phase 5 table has `org_id` REFERENCES organizations + RLS policy), canonical envelope on hardened active surface (auth/ai/actions/history/campaigns/automation), request_id correlator chain, all 13 prior closed backend hardening passes preserved. Auto-firing of automation_rules on AI decision stream remains GOVERNANCE-BLOCKED (cross-phase). Phase 5 routes (`creatives.ts`, `brand-kit.ts`) use legacy envelope; canonicalization tracked as PHASE5_ENVELOPE_FOLLOWUP (non-blocker; frontend not coupled).

2026-05-07 (continuation #6) — Phase 5 (AI Creatives) UNLOCK AUTHORIZED + EXECUTED (schema-prep step; deploy + gate-lift pending). Authored canonical migration `supabase/migrations/20260507140000_phase5_creatives.sql` from `specs/005-ai-creatives/spec.md` authority + runtime evidence in existing backend services (`backend/src/services/creatives/{brand-kit,creative-generator,copy-generation,image-generation,storage}.ts`) and routes (`backend/src/routes/v1/{creatives,brand-kit}.ts`). Migration creates 3 new tables: `brand_kits` (one per org, FR-002), `creative_generations` (job ledger with status enum + ROAS source context, FR-003..FR-005, FR-008, FR-013), `creatives` (per-output rows with type discriminator + content_url/content_text + performance_score CHECK 0-100, FR-007/FR-009/FR-010); ADDS 2 organizations columns (`plan_type` TEXT NOT NULL DEFAULT 'subscription' CHECK + `vault_byok_openrouter_secret_id` UUID NULLABLE) as MINIMAL Phase 7 substrate required by Phase 5 FR-011 BYOK gate (documented in `creative-generator.ts:resolveApiKey` defensive comment as the source of currently-active 42703 schema drift); CREATES 1 private storage bucket `creatives` (idempotent ON CONFLICT DO NOTHING) for image creative storage per CLAUDE.md TECH STACK + `services/creatives/storage.ts` expectations. All 3 new tables RLS-enabled with `org_id = auth.jwt()->>'org_id'` policies + per-table indexes. Phase 5 status: DEFERRED → PARTIAL. CURRENT PHASE updated. PATCH QUEUE: position #8 promoted to Phase 5 IN PROGRESS; positions renumbered (Phase X broader → #9). NEXT ACTION + CURRENT EXECUTION TARGET reflect new state. NO 503 gates lifted — gates remain in place on `/creatives/*` and `/brand-kit/*` until `supabase db push` deploy is verified (matches Phase 2 + Phase 4 Part 2 unlock-prep pattern). NO backend code touched (existing scaffolding preserved verbatim — no canonicalization, no rewrites; tracked as future PHASE5_ENVELOPE_FOLLOWUP). NO frontend pages wired (5 mocked-shells preserved). NO new dependencies added. NO Phase 7 monetization implementation (no Stripe, no billing UI, no plan upgrade flow, no credits ledger — only the schema substrate Phase 5 code already references). Phase locks preserved: Phase 4 minimal slice UNTOUCHED; Phase 4 Part 2 UNTOUCHED; Phase 3 linear pipeline UNTOUCHED; Phase 2 UNTOUCHED at column level (FK references added: creative_generations references ad_accounts as upstream context, no schema changes to existing Phase 2 tables); legacy `decisions` table NOT reactivated. Architecture invariants intact: single-writer backend, org_id enforcement (every new table has `org_id` REFERENCES organizations + RLS policy), canonical envelope on hardened active surface (auth/ai/actions/history/campaigns/automation), request_id correlator chain, all 13 prior closed backend hardening passes preserved. Auto-firing on AI decision stream (Phase 4 Part 2 cross-phase dependency) remains GOVERNANCE-BLOCKED.

2026-05-07 (continuation #5) — Phase 4 Part 2 CLOSED. Operator confirmed: PR #5 merged into `main` (commit `df2f243`); local main synced; `supabase db push` completed; 2 canonical Phase 4 Part 2 tables (automation_rules, automation_runs) + 2 nullable decision_history columns (automation_rule_id, automation_run_id) verified live in production Supabase. Read-only governance audit completed (continuation #4½ within continuation #5) confirming: 11 canonical migrations live, 14 active routers mounted, 5 deferred-router 503 gates active, all HARD LOCK invariants intact, backend tsc 0, frontend tsc 0, single-writer/org_id/canonical-envelope/request_id-correlator/PGRST116-discriminator parity all preserved across hardened surface, FINAL SYSTEM STATUS = GOVERNANCE SAFE WITH WARNINGS (warnings all explicitly tracked, no undocumented drift). Executed final unlock step: lifted `/automation/*` 503 gate in `backend/src/routes/v1/index.ts` (single-line removal at line 91 + governance comment block; +8/-1 LOC; minimal-diff). Preserved: 4 remaining deferred gates (`/decisions/*`, `/alerts/*`, `/creatives/*`, `/brand-kit/*`); `/automation` router mount at line 112 unchanged; all closed-slice invariants verbatim. Phase 4 Part 2 status: PARTIAL → CLOSED. CURRENT PHASE updated. SYSTEM STATUS Automation Engine + Per-org rate limit + Real Action Surface (added google.pause_campaign live) + Backend API endpoint list (added 5 new automation endpoints) all updated. PATCH QUEUE position #7 closed; position #8 (Phase X broader) is now next governed item, requiring explicit unlock authorization. NEXT ACTION + CURRENT EXECUTION TARGET reflect closure. Backend tsc → 0 errors. Frontend tsc → 0 errors. Phase locks preserved: Phase 4 minimal slice UNTOUCHED (HARD LOCK invariants verbatim); Phase 3 linear pipeline UNTOUCHED; Phase 2 UNTOUCHED; legacy `decisions` table NOT reactivated. Auto-firing of automation_rules on AI decision stream remains GOVERNANCE-BLOCKED (cross-phase dependency tracked under PATCH QUEUE position #7 G-clause); manual-execute path via `POST /api/v1/automation/rules/:id/execute` operational.

2026-05-07 (continuation #4) — Phase 4 Part 2 UNLOCK AUTHORIZED + EXECUTED (schema/code shipped; deploy + gate-lift pending). Authored canonical migration `supabase/migrations/20260507130000_phase4_part2_automation.sql` (CREATE automation_rules, CREATE automation_runs, ALTER decision_history ADD nullable automation_rule_id + automation_run_id, RLS + indexes). Per "Do NOT reactivate decisions table" governance rule, substituted spec data-model.md `decision_id REFERENCES decisions(id)` with `ai_decision_id REFERENCES ai_decisions(id)` on automation_runs (mirroring Phase 4 minimal close pattern); did NOT add `decision_id` column to decision_history; did NOT alter `decision_runs` (Phase 3 anomaly DEFERRED, table absent). Rewrote `backend/src/services/execution/automation-engine.ts` end-to-end: removed deprecated `decisions` table reference; new exports `evaluateRulesForAIDecision(orgId, aiDecisionId)` + `executeRule(orgId, ruleId, aiDecisionId?)`; legacy `dispatchAutomation(orgId, runId)` retained as dormant spec-conformance shim; confidence comparison normalizes ai_decisions.confidence_score (0–1 NUMERIC) against rule.min_confidence_threshold (0–100 INTEGER); trigger-type matching uses `ai_decisions.result.category` JSONB path (governance-soft dependency). Canonicalized `backend/src/routes/v1/automation.ts` to use ok()/fail() Phase 1 envelope across GET/POST/PATCH/DELETE /rules + GET /runs; added new `POST /rules/:id/execute` for manual rule firing; UUID + body-shape + LIST + INVALID_FILTER + INVALID_TYPE validation parity with rest of active hardened surface. Added `realGooglePauseCampaign` real-mode handler to `backend/src/services/execution/action-executor.ts` (Google Ads API v19 customers/{cid}/campaigns:mutate; per-org Vault refresh-token resolution; OAuth refresh; customer_id from `ad_accounts.platform_account_id`; structured `[exec]` lifecycle logs; tokens never logged); behind `GOOGLE_PAUSE_CAMPAIGN_LIVE` flag + `GOOGLE_LIVE_ORG_ALLOWLIST`, mirroring Meta pattern. Extended `executeAction` with optional `automationRuleId` + `automationRunId` (threaded into decision_history INSERT). Added per-org execution rate limit: `ACTION_EXECUTION_MAX_PER_MINUTE` (default 60); DB-backed count of decision_history inserts in last 60s; throws `code: 'RATE_LIMITED'` with `retryAfterSeconds: 60`; idempotent replays not counted (early-returned). Promoted `GOOGLE_PAUSE_CAMPAIGN_LIVE` to `LIVE_FLAG_DEPENDENCIES` startup-fail-fast (deps: GOOGLE_ADS_DEVELOPER_TOKEN + GOOGLE_ADS_CLIENT_ID + GOOGLE_ADS_CLIENT_SECRET). Phase 4 Part 2 status: DEFERRED → PARTIAL. NOT lifted: `/automation/*` 503 gate (preserved until deploy verified). NOT touched: action-executor.ts existing 4 real handlers (Meta pause/decrease/increase + Resend send_alert_email) + idempotency + impact_snapshot — all Phase 4 minimal close invariants preserved verbatim. NOT touched: legacy `decisions` table (still DEPRECATED). NOT touched: closed Phase 3 linear pipeline `services/ai/execute-ai-decision.ts` (post-persist hook for auto-firing is GOVERNANCE-BLOCKED). Backend tsc → 0 errors. HARD LOCK preserved verbatim. REAL SYSTEM CAPABILITIES preserved (will be updated to add google.pause_campaign live + automation engine status post-deploy). Auto-firing of automation_rules on AI decision stream is GOVERNANCE-BLOCKED behind Phase 3 anomaly DEPRECATED+DEFERRED state — manual-execute path is operational via `POST /api/v1/automation/rules/:id/execute`; cross-phase authorization required for automatic triggering (extend AI Output Contract with category field OR install post-persist hook in execute-ai-decision.ts OR unlock Phase 3 anomaly engine).




TOOLING / EXECUTION CONSTRAINT

AI MUST NOT:

* invent auxiliary infrastructure outside the repository architecture
* create parallel execution systems
* introduce standalone debug frameworks
* create speculative tooling layers
* bypass the canonical migration/runtime/logging flow
* introduce ad-hoc orchestration utilities outside the approved system structure

Python/runtime tooling is ALLOWED only when:

* it operates inside the existing repository architecture
* it supports the current system control flow
* it does not create architectural drift
* it does not bypass canonical migrations, runtime flow, logging, or validation layers
* it remains fully aligned with CURRENT PHASE constraints

FORBIDDEN:

* shadow infrastructure
* parallel audit systems
* speculative execution frameworks
* temporary orchestration layers
* non-canonical runtime paths

If tooling requires introducing a new operational layer:
STOP and report the architectural requirement instead of inventi